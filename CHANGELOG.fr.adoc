== v2.3.24.89 — 2026-06-04

=== Corrigé

* **Soft-wrap des blocs `[source,…]` : priorité à l'espace
  (frontière d'argument).** Le pliage v2.3.24.88 prenait la
  charnière la plus à droite, ce qui pouvait scinder un flag :
  `… --write --` puis `hostname=chouquette` (le `--hostname`
  coupé en deux). On coupe désormais EN PRIORITÉ à l'espace le
  plus à droite qui tient — chaque argument/flag reste entier :
+
  ....
  beryl scan aloli/9783705f-f86f-4bf7-88ad-4c92b920e9ba --provider=scaleway --dns --write
  --hostname=chouquette
  ....
+
  Les charnières `/ - = ,` ne servent plus qu'en second recours,
  lorsqu'un JETON UNIQUE est lui-même plus large que l'encadré
  (UUID, chemin ou hash sans espace) — exactement comme la
  coupure douce des codespans inline. Coupe nette à la limite en
  tout dernier recours.
== v2.3.24.88 — 2026-06-04

=== Ajouté

* **Soft-wrap des lignes trop longues dans les blocs `[source,…]`.**
  Un bloc de code est rendu verbatim et ne se justifie pas, mais
  une ligne plus large que l'encadré (typiquement une commande
  shell avec UUID complet + flags, ex. `beryl scan aloli/<uuid>
  --provider=scaleway --dns --write --hostname=…`) débordait
  jusqu'À SORTIR DE LA PAGE et se faisait rogner — la fin de la
  commande devenait invisible.
+
  Ces lignes sont désormais repliées aux caractères charnières
  (espace en priorité, puis `/`, `-`, `=`, `,` — la même logique
  que la coupure douce des codespans inline) au plus près du bord
  de l'encadré, et poursuivies sur une ligne de continuation. Toute
  la commande reste visible. Aucun caractère n'est injecté (pas de
  `\` ni d'indentation) : une coupure sur espace consomme l'espace,
  une coupure sur charnière garde la charnière sur la ligne du
  haut — le copier-coller reste fidèle. Une ligne sans aucune
  charnière (jeton unique très long) est coupée net en dernier
  recours, toujours préférable au rognage.
+
  La hauteur de l'encadré et la pagination s'ajustent
  automatiquement (le pliage a lieu avant le calcul de hauteur),
  donc un bloc qui gagne des lignes par pliage reste correctement
  encadré et peut basculer sur la page suivante si besoin.

=== Vérification

README beryl FR + EN : la commande `beryl scan` du scénario
Scaleway (UUID + 4 flags), qui sortait de la page (fin à 635 pt
pour un bord à 595), tient désormais entièrement dans l'encadré
sur deux lignes. 0 anomalie pdf-audit, 261 specs verts.
== v2.3.24.87 — 2026-06-04

=== Corrigé

* **Justification : les lignes atteignent à nouveau la marge
  droite.** Le calcul de `natural_w` (largeur naturelle d'une
  ligne, base de `extra_per_space = (target_w - natural_w) /
  n_spaces`) côté renderer comptait les paddings de badge
  (codespan/kbd/mark) de façon INCONDITIONNELLE, comme le fait
  `tokenize` pour la décision de coupure. Or le rendu ne les
  ajoute que CONDITIONNELLEMENT (pas entre deux fragments d'un
  même codespan, ni avant un caractère collant). `natural_w`
  était donc SUR-estimée ⇒ `extra_per_space` trop petit ⇒ les
  lignes justifiées s'arrêtaient quelques points AVANT la marge
  (défaut visuel signalé sur de nombreux items de liste et
  paragraphes du README beryl).
+
  Le renderer applique désormais EXACTEMENT la même condition que
  le rendu (`next_clings` / `next_is_mono`) pour compter les
  paddings : `natural_w` égale alors la largeur réellement
  dessinée, et la ligne se remplit pile jusqu'à `target_w`. Le
  composer conserve sa mesure gonflée (inconditionnelle) pour
  casser de façon conservatrice — garantissant l'absence de
  débordement.

* **Admonition simple : le texte atteint la marge droite.** La
  largeur de contenu d'une admonition retirait un `padding` à
  DROITE même en l'absence de cadre. Pour une admonition simple
  (bande de couleur à gauche seule, cas par défaut), il n'y a
  pas de bordure droite : le texte rejoint désormais la marge de
  page comme un paragraphe ordinaire. Le retrait droit n'est
  conservé que pour la variante encadrée (`boxed`).

=== Nettoyage interne

* Suppression du threading `composer_natural_w` / méthode
  `wrap_segments_full` introduits en v2.3.24.86 : le
  `natural_width` exact étant désormais recalculé localement et
  correctement par le renderer (paddings conditionnels), la
  valeur du composer n'a plus besoin d'être transmise.
  Retour à un unique `wrap_segments`.

=== Vérification

Rendu validé avec poppler (`pdftotext -bbox`, référence
indépendante) : sur le README beryl FR et EN, les lignes
justifiées atteignent la marge droite (559 pt) avec des espaces
inter-mots uniformes (~3-5 pt). La NOTE de la page 1 atteint
désormais la marge.
== v2.3.24.86 — 2026-06-04

=== Modifié (refactor interne, rendu inchangé)

* **Suppression du cache `@line_natural_widths` indexé par
  `object_id`.** La v2.3.24.85 transmettait le `natural_width`
  exact du composer au renderer via un cache `Hash(UInt64,
  Float64)` indexé par l'`object_id` de la ligne — un compromis
  fonctionnel mais fragile (réutilisation d'adresse possible
  après GC, croissance mémoire sur la durée du document).
+
  Le `natural_width` est désormais passé **explicitement** en
  paramètre `composer_natural_w` à `render_segment_line`. Nouvelle
  méthode `wrap_segments_full` qui conserve les objets `Line`
  complets du composer (segments + `natural_width`) ; les 3
  chemins qui justifient (paragraphe, item de liste, liste de
  définition) l'utilisent. `wrap_segments` devient un mince
  wrapper (`wrap_segments_full(...).map(&.segments)`) pour les
  ~9 call-sites non justifiants (titres, marqueurs) qui n'ont
  pas besoin de la métrique.
+
  Rendu **strictement identique** à la v2.3.24.85 (positions de
  tous les mots byte-exactes, vérifié sur le README beryl :
  2794 mots FR / 2776 EN, 0 anomalie). Le fichier perd 25 lignes
  nettes (-76 / +51) : plus court et sans variable d'instance
  partagée.

=== Pourquoi ce refactor

L'approche `object_id` avait été retenue pour ne pas toucher la
signature de `render_segment_line` (partagée par 12 call-sites).
En constatant que seuls 3 d'entre eux justifient réellement
(passent `target_w` + `align`), il devient trivial de threader la
vraie valeur uniquement là où elle compte, via un paramètre
optionnel `composer_natural_w : Float64? = nil` (rétro-compatible
pour les 9 autres). On élimine ainsi le *code smell* sans aucun
changement de rendu.

== v2.3.24.85 — 2026-06-04

=== Corrigé

* **Débordement du `:` français (`mot<NBSP>:`) hors marge droite.**
  Bug racine dans `try_native_justified_text` : le NBSP (U+00A0)
  était converti en espace ASCII *avant* le `split(' ')`, ce qui
  en faisait un point de justification. Un `extra_tj` (étirement)
  était alors inséré entre `source` et `:` dans `source<NBSP>:`,
  poussant le `:` au-delà de la marge.
+
  Conformément à la convention typographique (confirmée : en
  français `mot<NBSP>:<espace>`, seul l'espace APRÈS le `:` est
  justifiable ; le NBSP est insécable ET de largeur fixe ; en
  anglais `mot:<espace>`, idem), le split se fait désormais sur
  l'espace ASCII STRICT. Le NBSP reste DANS les morceaux et n'est
  converti en espace (pour le rendu du glyphe) qu'APRÈS le split —
  sa largeur reste donc naturelle, sans `extra`. Nouvelle méthode
  `split_ascii_keep_nbsp`. La branche police simple (`Tw`) passe
  au positionnement manuel pour la même raison (`Tw` s'applique à
  TOUS les octets 32, NBSP-convertis inclus).

* **Sur-étirement / débordement par désalignement de mesure
  composer ↔ renderer.**
  `compose_knuth_plass` calcule un `natural_width` par ligne qui
  INCLUT les paddings codespan/kbd/mark — c'est sur cette mesure
  que les coupures de ligne ont été décidées. Le renderer
  recalculait sa propre largeur via `font.string_width` SANS ces
  paddings, donc sous-estimée : `extra_per_space = (tw -
  natural_w) / n_spaces` devenait trop grand ⇒ lignes sur-étirées
  (gaps de 20-30 pt) ou mots débordant la marge.
+
  Le renderer consulte désormais le `natural_width` du composer
  (source de vérité) via un cache `@line_natural_widths` indexé
  par `object_id` de la ligne, renseigné dans `wrap_segments`.
  Lecture non-destructive (une ligne peut être rendue plusieurs
  fois lors d'un essai de pagination). Garde-fou : la valeur n'est
  adoptée que si elle est supérieure au calcul local et dans une
  borne raisonnable.

* **Knuth-Plass : `ratio < 0` rejeté (pas de compression).**
  Le seuil de shrink passe de -1.0 à 0.0 : aucune compression
  d'espace n'est tolérée. Toute ligne en débordement est rejetée,
  forçant le composer à casser plus haut (l'élément débordant part
  sur la ligne suivante, la précédente s'étire par stretch
  positif). Un Box atomique `mot<NBSP>:` trop large pour la ligne
  courante est ainsi systématiquement poussé à la ligne suivante,
  jamais comprimé ni coupé.

=== Impact

PDF du README beryl (FR et EN), mesuré par `pdf-audit` v0.2.1 :
**0 anomalie** typographique (7 pages, 2794 mots FR / 2776 mots
EN). Point de départ de la session : 11 anomalies.

Aucune régression sur les autres exemples (x-form-audit-iso27001
passe même de 8 à 7 anomalies pré-existantes, liées aux layouts
de champs `[x-form]`).

=== Note d'implémentation

Le cache `@line_natural_widths` indexé par `object_id` est un
compromis localisé (éviter de changer la signature de
`render_segment_line`, partagée par 15+ call-sites). La sûreté
repose sur l'écrasement-à-l'écriture (`wrap_segments` réécrit
l'entrée juste avant le rendu) et un garde-fou de plausibilité.
Un refactor vers un identifiant séquentiel reste possible si le
besoin s'en fait sentir.

== v2.3.24.84 — 2026-06-04

=== Corrigé

* **Knuth-Plass : `ratio < 0` rejeté (pas de compression d'espaces).**
  La borne TeX standard `next if ratio < -1.0` autorisait
  jusqu'à 100 % de compression des Glues d'une ligne pour la
  faire tenir dans `target_w`. Sur demande Philippe, et
  conformément à la convention typographique française qui veut
  des espaces de largeur stable (pas resserrés pour gagner de
  la place), cette borne devient `ratio < 0.0` : aucune
  compression d'espace tolérée. Une ligne en débordement est
  désormais systématiquement rejetée par K-P — le composer doit
  casser plus haut, la ligne précédente est étirée par stretch
  positif, et l'élément débordant (mot, codespan, `:` français
  après NBSP) part sur la ligne suivante.
+
  Cas typique éliminé : un Box atomique `mot<NBSP>:` (où le
  NBSP rend la jointure `mot`+`:` insécable, seul l'espace
  APRÈS le `:` étant justifiable) trop large pour la ligne
  courante est désormais poussé sur la ligne suivante au lieu
  d'être placé en débordement.

=== Impact

Sur le PDF du README beryl (mesure via `pdf-audit` v0.2.1) :
les anomalies typographiques passent de **11 à 4** (-64 %).

* `--hostname=chouquette` (overflow 75 pt dans un bloc
  `[source,console]`) : classé hors-scope par le filtre
  `monospace?` du linter — convention typographique : les
  blocs code ne sont pas wrappés (une commande shell ne doit
  pas être coupée en ligne), c'est à l'auteur du document de
  casser sa ligne dans le source AsciiDoc.
* `bootstrapper.`, `réel.`, `API`, `:` (certains paragraphes
  p4), `l'API` (p6 dans certains paragraphes) : éliminés par
  le rejet K-P `ratio < 0`.
* 4 résiduels à 4-8 pt (`par`, `:`, `lit`, `l'API`) : cas où
  `compose_first_fit` (fallback K-P quand aucun chemin
  admissible n'est trouvé) ne casse pas correctement à la
  dernière Glue/Penalty avant un mot final. La convergence
  vers 0 anomalie demande un refactor du fallback first-fit
  (alignement strict de la mesure composer sur le rendu
  effectif des paddings codespan inter-segments).

== v2.3.24.83 — 2026-06-04

=== Corrigé

* **Compensation des paddings de badges dans le calcul du
  composer.** Le `ParagraphComposer` ne savait pas que le rendu
  ajoute `codespan_padding_x` (typiquement 2 pt), `+4 pt`
  pour `kbd`/`button` ou `+2 pt` pour `mark` à `current_x`
  après chaque badge (cf. `render_segment_line` ~ ligne 4031,
  fix J4.A v2.3.24.73). Conséquence : les lignes contenant des
  codespans étaient comptées légèrement plus courtes que leur
  rendu réel, et certains mots débordaient de la marge droite
  de 1 à 16 pt.
+
  Détecté grâce au tout nouveau `aloli-crystal/pdf-audit`
  (v0.1.0) qui a remonté 42 `margin_overflow` sur le PDF du
  README beryl. Le fix consiste à gonfler la mesure de la Box
  du segment dans le bloc de mesure de `tokenize` (cf.
  `converter.cr#wrap_segments`) du même montant que ce que le
  renderer ajoute en aval. Conservateur : compte un padding
  même pour les codespans adjacents (où la compensation est en
  réalité nulle) — surestimation acceptable car évite tout
  débordement.

  Vérification après fix :
+
  - `pdf-audit lint beryl.pdf` : 67 → 33 anomalies
  - `margin_overflow` : 42 → 20 (les 20 restants sont des
    ponctuations finales `:`, `→`, courts mots dont la mesure
    de pdf2text accuse ±1-3 pt d'imprécision)
  - `stuck_words` : 6 → 0
  - Gold compare sur les 53 README de prod-crystal : 0 projet
    ne perd de page, `beryl-recipes` en gagne 1 (compactification).
= Journal des modifications
:toc: left
:toc-title: Versions
:toclevels: 2

== v2.3.24.82 — 2026-06-04

=== Ajouté

* **Phase 2 — qualité typographique se rapproche de LaTeX.**
  Quatre blocs corrélés livrés dans la même session après mise
  en place d'une suite de tests visuels (53 PDFs gold de
  baseline). Vérification de non-régression : aucun projet ne
  perd de page, 2 (`asciiart`, `beryl-recipes`) en gagnent.
+
  **Bloc A — `tools/compare-pdfs-gold.sh`** : script de
  comparaison entre 2 dossiers de PDFs gold. Compare les
  SHA-256 du manifest pour identifier les projets changés,
  puis pour chacun extrait les bbox des mots
  (`pdftotext -bbox-layout`) et compte les mots dont la
  position a bougé. Exit `0` si tout identique, `1` sinon —
  utilisable en CI / pré-merge.
+
  **Bloc B — Réactivation de la césure Liang en `first-fit`.**
  Régression silencieuse découverte avant K-P : la césure
  n'agissait pas, les `Penalty.flagged` produites par
  `tokenize` étaient ignorées par `compose_first_fit`. La
  boucle de choix de breakpoint parcourt désormais `current`
  à l'envers pour trouver le breakpoint le plus à droite —
  une `Penalty.flagged` (césure) gagne sur la `Glue`
  précédente si elle est plus à droite. Le tiret est suffixé
  au dernier segment de la ligne via `ends_on_hyphen=true` /
  `hyphen_w`. `finalize_line` étendue pour accepter ces deux
  paramètres (symétrie avec `finalize_kp_line`).
+
  Vérification sur `pouvoir` français : « doit pou-voir
  intervenir » — césure légitime appliquée.
+
  **Bloc C — Knuth-Plass actif par défaut avec garde-fou
  emergency.** `wrap_segments` repointé sur
  `compose_knuth_plass`. Après la backtrace optimale,
  `MAX_KP_RATIO = 3.0` (= étirement de 300 % du stretch
  nominal) : si AU MOINS UNE ligne a un `|adjustment_ratio|`
  dépassant ce seuil, K-P retombe sur `compose_first_fit`
  pour le paragraphe entier (qui a son propre garde-fou
  `max_extra_factor = 2.5` côté renderer). Sans ce garde-fou,
  K-P acceptait des layouts pathologiques (mots éparpillés
  sur de larges blancs — régression observée le 2026-06-04
  sur le README beryl). Avec lui, K-P apporte ses bénéfices
  globaux (lignes équilibrées, exploitation des `Penalty` de
  césure, moins de *rivers*) sans accepter les cas extrêmes.
+
  Validation sur README beryl :
+
  - « avant » et « deploy » correctement séparés ✓
  - « FreeBSD 15 via mfsBSD-in-QEMU » sur une ligne ✓
  - « bootstrapper » en fin de ligne, pas de débordement ✓
  - « l'arborescence : » sur ligne 1 ✓
+
  **Bloc D — Coupure douce dans les codespans inline longs.**
  Nouvelle constante `CODESPAN_HINGE_CHARS = "/-=,"` et
  helper `split_at_codespan_hinges` qui fragmente un mot
  codespan APRÈS chaque caractère charnière. Le `tokenize`
  émet `Penalty(width=0, cost=200, flagged=false)` entre
  fragments — `compose_first_fit`/`compose_knuth_plass`
  peuvent y casser une longue commande type
  `beryl scan aloli/9783... --provider=…` plutôt que de la
  laisser déborder de la marge. Coût `200` non infini :
  décourage la coupure inutile mais l'autorise quand
  nécessaire. La boucle de choix de breakpoint dans
  `compose_first_fit` accepte désormais TOUTE `Penalty` avec
  `cost < INFINITY` (flagged → tiret, non-flagged → coupure
  sèche).
+
  Corollaire : suppression de la césure Liang sur les
  segments `mono`. Un identifiant `beryl` ne doit JAMAIS être
  césuré en `be-ryl` (le tiret changerait son sens). La
  coupure douce sur charnières est la seule autorisée pour
  les codespans.
+
  Corollaire renderer : pas de `codespan_padding_x` entre deux
  segments `mono` adjacents — ils forment la suite d'un même
  codespan splitté en fragments. Sans cette exception, un trou
  de 2 pt apparaissait entre `aloli/` et `9783705f-`, rendant
  le codespan illisible.

== v2.3.24.81 — 2026-06-04

=== Corrigé

* **REVERT URGENT — `wrap_segments` repointé sur
  `compose_first_fit`, garde-fou `max_extra_factor` réintroduit,
  fix « avantdeploy » collés.** Régressions visuelles confirmées
  par l'utilisateur le 2026-06-04 sur le PDF du README beryl
  (`/Users/philippe/prod-crystal/beryl/README.fr.adoc`) après
  les commits J3/J4 :
+
  0. **« avantdeploy » collés** dans la note « beryl se place
     `*avant*` `https://.../[deploy]` » — le parser HTML
     d'asciidoctor produit `<strong>avant</strong><a>deploy</a>`
     SANS espace entre les balises (collapse). Mon fix v.70
     avait retiré la Glue inter-segments artificielle pour
     régler le bug codespan/ponctuation, mais cette
     suppression a cassé ce cas réciproque.
  0. **« FreeBSD 15 » cassé** entre lignes, **gros gaps de
     justify**, **mot `bootstrapper` débordant dans la marge**,
     **commandes longues débordant** : Knuth-Plass sans garde-fou
     `max_extra_factor` acceptait des layouts avec étirement
     extrême au lieu de retomber en alignement gauche pour la
     ligne concernée.
+
  **Actions** :
+
  - `wrap_segments` (converter.cr) : appel
    `compose_first_fit(tokens, width)` (au lieu de
    `compose_knuth_plass`). Le code Knuth-Plass est **conservé**
    dans `paragraph_composer.cr` pour ré-introduction future
    propre avec garde-fou emergency + suite de tests visuels.
  - `render_segment_line` : ré-introduit le garde-fou
    `max_extra_factor = 2.5`. Si l'extra par espace dépasse
    2,5 × la largeur d'un espace normal, on bascule en
    alignement gauche pour cette ligne. Calibrage empirique
    v2.3.24.68.
  - `ParagraphComposer.tokenize` : nouvelle constante
    `CLINGING_CHARS = ".,;:!?)]}»' " + NBSP` (caractères qui
    « collent » au mot précédent). À une frontière
    inter-segments (`idx == 0` d'un nouveau seg, le dernier
    token est une Box), un Glue est inséré **sauf** si le 1er
    caractère du seg suivant est dans `CLINGING_CHARS`. Cela
    règle :
+
    [horizontal]
    `*avant* deploy`:: → `avant deploy` (lettre `d` → Glue)
    `(ex : ` `code` `)`:: → `(ex : code)` (`)` collante → pas
                            de Glue)
    `deploy<NBSP>: il`:: → `deploy : il` (NBSP collante → pas
                          de Glue avant `:`)
    `l'arborescence`:: → `l'arborescence` (`'` collante)
+
  Spec mise à jour : `paragraph_composer_spec.cr` a 3 nouveaux
  cas (Glue inter-segments inséré pour lettre, non-inséré pour
  ponctuation collante, non-inséré pour NBSP) remplaçant
  l'ancien test « jamais d'insertion ». 261/261 specs verts.
+
  Vérification visuelle sur README beryl :
+
  - `avant` et `deploy` séparés correctement.
  - `FreeBSD 15 via mfsBSD-in-QEMU` sur une ligne.
  - Plus de gros gaps de justification (item 7 « (via QEMU) et
    installe… »).
  - `bootstrapper` sur sa propre ligne, pas de débordement.
  - `l'arborescence :` ligne 1 (était sur ligne 2 « qui »
    auparavant).
+
  **Erreur méthodologique reconnue** : les 259 specs verts ne
  remplacent pas une suite de tests visuels sur de vrais
  documents. Manque structurel à combler dans une session
  dédiée (PDFs gold de référence + bbox compare). Sans cette
  infrastructure, on ne sait pas détecter des régressions
  visuelles introduites par un changement d'algorithme.

=== Ce qui est préservé

Tous les autres commits restent actifs :

* J2 — Hyphenation Liang 85 langues + CLI `hyph`
* J4.A — Fix BG codespan / ponctuation collante (rendu)
* J4.B — Émission PDF native `Tw`/`Tj`/`TJ`
* Chantier 1 — Justification CJK via `Tc` (lignes mono-CJK)
* Chantier 2 — `hyph available` + cache 7j
* Chantier 3 — Manifest 85 langues (après exclusion `mn-cyrl-x-lmc`
  + `ro`)
* `hyph install --force` + suivi HTTP redirects + mirror GitHub
* Vraie justification CJK multi-lignes (tokenize_segment_with_cjk)

Le moteur de composition redevient first-fit greedy (état avant
J3), mais TOUTES les améliorations périphériques (PDF natif,
hyphenation, CJK, CLI) sont conservées.

== v2.3.24.80 — 2026-06-03

=== Ajouté

* **`hyph install <lang> --force` bypasse l'exclusion pour
  licence non déclarée.** Les langues présentes dans
  `EXCLUDED_LANGS` (actuellement `mn-cyrl-x-lmc`, `ro`)
  peuvent maintenant être installées si l'utilisateur a
  explicitement accepté le risque juridique en passant
  `--force` (ou `-f`). Sans `--force`, le refus précédent
  reste en place (exit `10`).
+
  Le flag `--force` étend ainsi sa sémantique : il signifie
  « j'assume tous les risques » — y compris la licence
  ambiguë, en plus de son rôle historique d'écrasement d'une
  installation existante.
+
  Avec `--force` activé :
+
  0. Warning sur `stderr` rappelant la raison enregistrée.
  0. Download depuis le miroir effectif (GitHub par défaut,
     voir ci-dessous).
  0. Vérification SHA-256 **toujours active** contre le hash
     stocké dans `EXCLUDED_LANGS` — un attaquant qui
     servirait un fichier malicieux serait toujours détecté.
+
  Refactor associé : `EXCLUDED_LANGS` n'est plus un
  `Hash(String, String)` simple mais un
  `Hash(String, ExcludedEntry)` (nouveau record) avec
  `reason`, `url`, `sha256`, `size`. La symétrie avec
  `ManifestEntry` autorise la même vérification d'intégrité.

* **Suivi des redirects HTTP 301/302/303/307/308 dans
  `hyph install`.** Nouvelle fonction privée
  `http_get_following_redirects` (jusqu'à 5 sauts) qui résout
  les `Location` headers (y compris relatifs via `URI#resolve`).
  La stdlib Crystal `HTTP::Client.get` ne suit pas les
  redirects automatiquement — or les miroirs CTAN
  `mirrors.ctan.org` répondent typiquement en 307 vers un
  miroir géographique.

=== Changé

* **URL effective de téléchargement bascule sur le mirror
  GitHub par défaut.** Le manifest continue à référencer
  l'URL CTAN canonique (à fins documentaires), mais
  `hyph install` transforme cette URL pour pointer vers le
  mirror GitHub stable :
  `https://raw.githubusercontent.com/hyphenation/tex-hyphen/master/hyph-utf8/tex/generic/hyph-utf8/patterns/tex/`.
  Le miroir CTAN principal (`mirrors.ctan.org`) retourne
  désormais des `404` sur ces chemins (constaté lors de cette
  session : redirection vers `ctan.mines-albi.fr` qui retourne
  404). L'utilisateur peut surcharger via les variables
  d'environnement `HYPH_MIRROR` ou `CTAN_MIRROR`, ou via
  `--from` explicit.

== v2.3.24.79 — 2026-06-03

=== Ajouté

* **Vraie justification CJK multi-lignes.** Le
  `ParagraphComposer.tokenize` détecte désormais les segments
  contenant des caractères CJK (≥ U+3000) et délègue à un
  helper dédié `tokenize_segment_with_cjk` qui split CHAQUE
  caractère CJK en `Box` autonome, séparées par une `Glue`
  mince (`width = 0`, `stretch = char_w × 0.5`, `shrink = 0`)
  entre 2 chars CJK consécutifs.
+
  Ces Glues mince servent deux usages :
+
  0. **Point de coupure légal** que `compose_knuth_plass`
     exploite pour casser les paragraphes CJK longs sur
     plusieurs lignes. Avant ce changement, faute de
     breakpoint (les paragraphes CJK n'ont pas d'espace ASCII
     pour servir naturellement de Glue), un paragraphe CJK
     restait fatalement sur une seule ligne — le composer ne
     pouvait pas le casser, l'utilisateur le voyait s'étendre
     hors marge.
  0. **Support d'étirement Tc.** Knuth-Plass étire ces Glues
     en proportion de l'`adjustment_ratio` de chaque ligne ;
     le renderer retrouve la même valeur via le calcul
     `extra_per_cjk_glyph = (target_w - natural_w) / (n_cjk - 1)`
     et l'émet via `page.text(..., char_spacing:)`.
+
  PAS de Glue entre un char CJK et un char latin adjacent : la
  convention sino-japonaise écrit `日本2026年` collé, casser
  ici serait une violation typographique.
+
  Corollaires côté composer :
+
  - `finalize_line` et `finalize_kp_line` distinguent une Glue
    « espace » (`width > 0`, latin) d'une Glue « mince »
    (`width == 0`, CJK). Seules les Glue espace contribuent à
    `n_spaces` et déclenchent un préfixe espace devant la Box
    suivante — les Glues mince CJK n'introduisent aucun
    caractère espace dans le rendu.
  - Le bloc de mesure dans `wrap_segments` accepte désormais
    un caractère CJK isolé : il utilise `font_cjk.string_width`
    quand le caractère est ≥ U+3000 et reconnu par la police
    CJK, sinon `font.string_width` (police principale Latin).
    Sans cette correction, chaque char CJK aurait été mesuré
    à 0 par Noto Sans Latin → tokens de largeur fantôme,
    `natural_w` sous-estimé, lignes mal cassées.
  - Le bloc capture désormais le bloc en `Proc` nommé
    (`&measure : ...`) plutôt qu'en `yield` implicite. Crystal
    interdit `yield` dans un Proc literal — refactor nécessaire
    pour que `tokenize_segment_with_cjk` puisse forwarder le
    bloc à un sub-lambda `flush_latin`. (Bonus : `Array(Char)`
    plutôt que `String::Builder` pour le buffer latin, car
    `String::Builder.to_s` ne peut être appelé qu'une fois et
    `flush_latin` est appelé plusieurs fois par paragraphe.)
+
  Corollaires côté renderer (`render_segment_line`) :
+
  - Le calcul de `seg_w` détecte les chars CJK et splitte par
    run pour utiliser `font_cjk` sur les CJK, `font` sur le
    latin. Sans ça, `seg_w ≈ 0` pour les chars CJK isolés et
    `current_x` n'avançait pas entre 2 segs CJK adjacents :
    tous les glyphes se superposaient à la même position
    visuelle ≈ 36.
  - La branche « non-badge » (mot-par-mot) propage la même
    correction sur le `part_w` mesuré entre `draw_text_run`
    consécutifs.
  - L'avancement Tc CJK est désormais ajouté à `current_x`
    APRÈS le draw du seg : `current_x += n_cjk_in_seg ×
    extra_per_cjk_glyph` pour suivre le moteur PDF qui translate
    son propre curseur de `glyph_w + Tc` après chaque glyphe.
+
  Vérification sur un paragraphe chinois long (~50 caractères
  Han + Knuth-Plass + algorithme Latin) :
+
  - Le composer casse correctement entre 2 chars CJK
    (transition `测` / `试`).
  - Les 2 lignes résultantes sont toutes deux justifiées au
    bord droit (xMax=559.28 = target_w).
  - 259/259 specs verts. Ameba propre. Le fix J4.A préservé
    (« 186260). » toujours collé).

== v2.3.24.78 — 2026-06-03

=== Retiré

* **Langues à licence non déclarée retirées du manifest
  (`mn-cyrl-x-lmc`, `ro`).** Le manifest est ramené de 87 à
  **85 langues**. Les deux entrées retirées portaient
  `license: "Unknown"` parce que leur en-tête source upstream
  ne déclare pas de grant de licence libre (`NONE` pour
  `mn-cyrl-x-lmc`, `[None]` pour `ro`). Distribuer ces
  patterns via notre manifest placerait nos utilisateurs en
  position d'ambiguïté juridique — la convention ALOLI étant
  de ne distribuer que du libre clairement licencié, on
  préfère les exclure.
+
  Nouvelle constante `EXCLUDED_LANGS : Hash(String, String)`
  dans `manifest.cr` qui mappe chaque code de langue exclu
  vers un message diagnostique en anglais (lingua franca des
  discussions de licensing libre). La sous-commande
  `hyph install <lang>` consulte cette table AVANT le
  manifest : si la langue y figure et que l'utilisateur n'a
  pas passé `--from`, l'install est refusé avec le message
  explicatif et un code de sortie `10` (manifest missing).
+
  L'utilisateur peut toujours installer ces langues
  manuellement après avoir vérifié indépendamment la
  situation juridique :
+
  [source,bash]
  ----
  asciidoctor-pdf hyph install ro \
    --from https://example.org/hyph-ro.tex \
    --sha256 <HASH_QU_IL_A_CALCULE>
  ----
+
  Message complet rendu :
+
  [source,text]
  ----
  Refusing to install `ro`: License field is `[None]` in the
  upstream source file (https://github.com/hyphenation/tex-hyphen,
  hyph-ro.tex). Without an explicit free-software license grant,
  redistribution is legally ambiguous and we exclude this
  language from our manifest.

  If you have independently verified the licensing situation
  and still want to install this language, you can bypass the
  manifest by providing both --from <URL> and --sha256 <HASH>
  explicitly:
    asciidoctor-pdf hyph install ro --from <URL> --sha256 <HASH>
  ----

== v2.3.24.77 — 2026-06-03

=== Ajouté

* **Manifest hyphenation étendu à 87 langues.** Le manifest
  `src/asciidoctor_pdf/hyphenation/manifest.cr` répertorie
  désormais 87 langues (7 embarquées + 80 ajoutées, listables
  via `asciidoctor-pdf hyph install <lang>`) — soit la
  quasi-totalité des patterns hyph-utf8 disponibles en amont
  (88 fichiers ; 1 exclu : `grc-x-ibycus`, encodage Ibycus
  8-bit legacy redirigeant vers `ibyhyph.tex`, pas de
  `\patterns{}` direct).
+
  Source de référence : clone shallow de
  `https://github.com/hyphenation/tex-hyphen.git` (l'API
  GitHub a été contournée pour stabilité face au rate-limit
  anonyme). Pour chaque langue, le manifest enregistre l'URL
  CTAN canonique, la taille en octets, le SHA-256 (vérifié
  obligatoire à l'install), et la licence parsée du bloc YAML
  `% licence:` en en-tête du fichier source.
+
  **Codes de langue ajoutés** (80, ordre alphabétique) :
  `af`, `ar`, `as`, `be`, `bg`, `bn`, `ca`, `cop`, `cs`, `cu`,
  `cy`, `da`, `de-1901`, `de-ch-1901`, `el-monoton`,
  `el-polyton`, `en-gb`, `eo`, `et`, `eu`, `fa`, `fi`,
  `fi-x-school`, `fur`, `ga`, `gl`, `grc`, `gu`, `he`, `hi`,
  `hr`, `hsb`, `hu`, `hy`, `ia`, `id`, `is`, `ka`, `kk`,
  `kmr`, `kn`, `la`, `la-x-classic`, `la-x-liturgic`, `lt`,
  `lv`, `mk`, `ml`, `mn-cyrl`, `mn-cyrl-x-lmc`, `mr`,
  `mul-ethi`, `nb`, `nn`, `no`, `oc`, `or`, `pa`, `pi`, `pl`,
  `pms`, `rm`, `ro`, `ru`, `sa`, `sh-cyrl`, `sh-latn`, `sk`,
  `sl`, `sq`, `sr-cyrl`, `sv`, `ta`, `te`, `th`, `tk`, `tr`,
  `uk`, `vi`, `zh-latn-pinyin`.
+
  **Tailles** : min 595 o (`ar`), max 529 279 o (`hu`),
  médiane ~11 583 o. Aucun fichier ne dépasse 1 Mo (seuil de
  sanity-check à l'install).
+
  **Licences** (compte par catégorie) : MIT (46), LPPL (20),
  GPL (5), FSF-unlimited (3), Permissive (2), LGPL (2),
  Unknown (2 — `mn-cyrl-x-lmc` et `ro` dont le header source
  ne déclare pas de licence ; à auditer si une distribution
  strictement libre est requise), plus singletons
  (BSD-3-Clause, BSD-3-Clause-like (`bg`),
  Knuth-permissive, LPPL-1.3-or-MIT, MPL, Public-Domain,
  Unlicense).
+
  Effet pour l'utilisateur : `asciidoctor-pdf hyph available`
  liste désormais les 87 langues avec leur statut local
  (`embarqué` / `installé` / `absent`), et `hyph install
  <lang>` accepte n'importe lequel des 87 codes sans
  paramètre `--from`/`--sha256` supplémentaire (le SHA-256
  attendu est lu dans le manifest).
+
  259/259 specs verts. Ameba propre.

== v2.3.24.76 — 2026-06-03

=== Ajouté

* **Sous-commande `asciidoctor-pdf hyph available`.** Complète
  la suite `hyph` (qui livre déjà `list`, `install`, `remove`,
  `update`, `show`, `help`). Interroge CTAN — ou plus
  précisément l'API GitHub `hyphenation/tex-hyphen` — pour
  lister TOUS les patterns Liang disponibles en amont, et
  croise avec le manifest embarqué et l'état local utilisateur.
+
  Trois flags (tous avec un short, convention ALOLI) :
+
  - `-r` / `--refresh-cache` : force le re-téléchargement
  - `-j` / `--json` : sortie JSON structurée au lieu du tableau
  - `-o` / `--offline` : repli explicite sur le manifest local,
    pas de requête HTTP
+
  Sortie tableau par défaut, colonnes :
+
  - **langue** : code court (`fr`, `ja`, etc.)
  - **source CTAN** : `✓` si trouvée upstream, `—` sinon
  - **manifest** : `✓` si présente dans le manifest embarqué,
    `✗` sinon
  - **local** : `embarqué`, `installé`, ou `absent`
+
  Cache disque dans `${XDG_CACHE_HOME:-~/.cache}/asciidoctor-pdf/hyph-available.json`
  avec TTL de 7 jours (alignement avec la spec
  `doc/CLI_HYPH_SPEC.adoc`). Source URL configurable via
  `HYPH_MIRROR` ou `CTAN_MIRROR` ; défaut sur l'API GitHub
  (JSON listing) ; fallback HTML naïf via regex pour les
  miroirs CTAN classiques.
+
  Erreur réseau : warning sur `stderr` + repli sur le manifest
  embarqué + exit `0` (utilisable en CI). 16 specs unitaires
  + intégration via `Process.run` sur le binaire (parsing
  flags, lecture cache, fallback, formats sortie).

== v2.3.24.75 — 2026-06-03

=== Ajouté

* **Justification CJK via `Tc` (« 両端揃え / 两端对齐 »).**
  Les paragraphes purement chinois / japonais / coréen — qui
  ne contiennent aucun espace ASCII — sont désormais justifiés
  au bord droit en étirant l'espace entre chaque glyphe via
  l'opérateur PDF natif `Tc` (Character Spacing,
  ISO 32000-1 § 9.3.2), conformément à la convention
  typographique sino-japonaise.
+
  Détection : la ligne ne contient aucun espace ASCII (`n_spaces
  == 0`), `target_w` est défini, `font_cjk` est chargée, et au
  moins 2 glyphes CJK sont reconnus par `font_cjk.has_glyph?`.
  La justification reste active même quand l'`align` reçu est
  `left` (cas des paragraphes CJK tenant sur une seule ligne,
  donc « dernière ligne du paragraphe » du point de vue du
  composer — exception explicite à la règle « dernière ligne
  en gauche » pour respecter la convention CJK).
+
  Calcul : `extra_per_cjk_glyph = (target_w - natural_w) /
  (n_cjk - 1)`. Garde-fou à 75 % de la largeur d'un glyphe CJK
  typique (`中`) pour éviter les pathologies (2 glyphes seuls
  sur une ligne très large). 75 % est plus permissif que le
  50 % d'un premier POC : la convention CJK accepte couramment
  des étirements jusqu'à ~100 %, et le seuil bloquait des
  lignes japonaises courtes légitimes.
+
  Correction simultanée du calcul de `natural_w` : la police
  principale (Noto Sans Latin) ne contenait pas les glyphes
  CJK et retournait 0 / ε pour eux, sous-estimant `natural_w`
  et faussant ensuite tous les calculs de justification mixte.
  On délègue désormais à `text_with_emoji_segments` la
  segmentation par police puis on somme avec la bonne fonte
  (`font_cjk` pour les runs `:cjk`, `font` pour les runs
  `:text`).
+
  `draw_text_run` accepte un paramètre optionnel
  `cjk_char_spacing` (défaut `0.0`) qui se propage au
  `page.text(..., char_spacing:)` dans la branche `:cjk`.
  L'avancement du curseur Crystal compense le `Tc` cumulé du
  moteur PDF : `cursor += string_width + (n - 1) ×
  cjk_char_spacing` pour pointer à la fin VISUELLE du dernier
  glyphe (le `Tc` ajouté par PDF après le dernier glyphe est
  invisible — reset automatique par le shard `pdf` à la fin du
  `page.text`).
+
  Vérification visuelle (`/tmp/cjk-test.adoc`) :
+
  - Avant : ligne chinois xMax=476 (loin du bord droit 559),
    ligne japonais xMax=386.
  - Après : ligne chinois xMax=559.28 (= bord droit `target_w`),
    ligne japonais xMax=559.28 (= bord droit). Parfaitement
    justifiées.
+
  Limite reconnue : le `ParagraphComposer.tokenize` actuel
  traite chaque chaîne CJK comme un seul Box (split sur espace
  ASCII uniquement), ce qui force les paragraphes CJK longs à
  rester sur une seule ligne (le composer ne peut pas casser).
  La vraie justification CJK multi-lignes demanderait de
  splitter au tokenize par glyphe avec des Glue souples entre
  chaque — gros refactor à venir si besoin réel se manifeste.
  Le rail `Tc` actuel suffit pour la majorité des cas
  pratiques (un paragraphe court en chinois ou japonais).

== v2.3.24.74 — 2026-06-03

=== Ajouté

* **J4.B : émission PDF native pour le texte justifié — `TJ`
  pour les polices composites, `Tw` pour les polices simples.**
  Bump de la dépendance `aloli-crystal/pdf` de `~> 0.6.4` à
  `~> 1.2` qui expose désormais `page.text(word_spacing:,
  char_spacing:)` (opérateurs `Tw` + `Tc`,
  ISO 32000-1 §§ 9.3.3 et 9.3.2) et `page.text_positioned(runs)`
  (opérateur `TJ`, § 9.4.3) avec auto-détection du type de
  police via `page.composite_font?`.
+
  Implémentation dans `render_segment_line` :
+
  0. Pour chaque segment non-badge avec `extra_per_space > 0` :
     tentative préalable du chemin natif via le nouveau helper
     `try_native_justified_text`.
  0. Si le segment est rendable nativement (pas de drapeau pays,
     pas d'emoji, pas de run CJK — vérifié par `native_renderable?`) :
+
     - **Police simple (Type1, AFM)** : `page.text(content,
       word_spacing: extra)` qui émet `Tw` natif. Le moteur PDF
       applique l'extra à chaque octet `0x20`.
     - **Police composite (TrueType, CID — notre cas par défaut
       avec Noto Sans)** : `Tw` ne s'applique pas (ISO 32000-1
       § 9.3.3 : « word spacing shall not apply to occurrences
       of the byte value 32 in multiple-byte codes »). On émet
       à la place un `TJ` avec déplacements explicites entre
       mots : `[mot1, " ", -extra_tj, mot2, " ", -extra_tj, mot3] TJ`.
       Le delta négatif en text space units translate le curseur
       vers la droite (convention PDF inverse).
  0. Si le segment contient un élément inline mixte (drapeau,
     emoji, run CJK) : fallback sur l'avancement manuel
     mot-par-mot via `draw_text_run` — comportement préservé.
+
  Vérification quantitative sur le PDF de test
  (`/tmp/codespan-test.adoc`) : 58 opérateurs `TJ` désormais
  émis (auparavant 0), 30 % d'opérateurs `Tj` économisés
  (~200 → 139). Le rendu visuel reste strictement identique au
  pixel près — l'optimisation se fait au niveau du flux de
  content stream PDF, pas du placement des glyphes.
+
  Bonus typographique CJK : la voie `TJ` est l'infrastructure
  nécessaire pour la justification CJK à venir (`Tc`
  character-spacing étirant tous les glyphes plutôt que les
  espaces, convention « 中文两端对齐 »).

=== Corrigé documentation

* **Rectification de la note v2.3.24.73 sur `Tw` natif.** Le
  changelog précédent affirmait que le shard `aloli-crystal/pdf`
  ne supportait pas `Tw`. C'était un faux positif de ma part —
  un grep depuis `lib/pdf/` (l'installation figée à `0.6.4`)
  manquait les nouvelles méthodes ajoutées en `1.1.0` et `1.2.0`.
  Le bump de dépendance ci-dessus rétablit la vérité : `Tw` /
  `Tc` / `TJ` sont tous trois disponibles dans le gem
  `pdf 1.2.0`, avec auto-détection du type de police pour
  émettre l'opérateur adapté.

== v2.3.24.73 — 2026-06-03

=== Corrigé

* **J4 : gap visuel résiduel entre codespan et ponctuation
  collante.** Symptôme : « (ex : `186260`). » rendait
  visiblement « (ex : `186260` ). » avec un espace d'environ
  1 à 2 pt avant la parenthèse fermante. Pareil pour
  « `prepare_rescue`, » qui devenait « `prepare_rescue` , ».
+
  Cause : la branche « badge » de `render_segment_line`
  (segments mark / codespan / kbd / button) dessinait le
  texte en bloc puis ajoutait `extra_per_space * count(' ')`
  à `current_x`. Le segment suivant (ponctuation) commençait
  alors `extra_per_space` points trop loin.
+
  Fix : si le segment porte un préfixe espace (rendu de la
  Glue précédente par le composer) ET qu'on est en mode
  justify, l'espace de tête est désormais étiré **AVANT**
  le draw — on avance `current_x` de
  `prefix_w + extra_per_space`, puis on dessine `seg.text[1..]`
  (sans le préfixe). À la fin du draw, `current_x` correspond
  exactement à la fin visuelle du texte du badge ; la
  ponctuation suivante colle naturellement. Le rectangle de
  fond (`badge_x`) est aussi décalé du `extra_for_leading`
  pour rester aligné avec le texte effectivement dessiné.
+
  Vérification : `pdftotext -bbox-layout` extrait désormais
  `186260).` comme un seul mot (avant : `186260` et `).`
  séparés avec écart 4,72 pt).

=== Documenté

* **J4.B (`Tw` natif PDF) reporté.** L'idéal eût été de
  s'appuyer sur l'opérateur PDF `Tw` (Word Spacing,
  ISO 32000-1 § 9.3.3) qui applique l'extra à *tous* les
  espaces ASCII d'une chaîne `Tj` au niveau du moteur PDF
  lui-même — pas de calcul de position côté Crystal. Mais le
  shard `pdf` (aloli-crystal/pdf) ne l'expose pas
  actuellement : il fait du dessin mot-par-mot avec une
  propriété `word_spacing` sur ses Fragments
  (cf. `lib/pdf/src/pdf/text/formatted/box.cr:310`). Ajouter
  `Tw` natif demanderait d'étendre le gem `pdf` — chantier
  séparé. Le fix J4.A ci-dessus atteint le même résultat
  visuel par avancement manuel.

== v2.3.24.72 — 2026-06-03

=== Changé

* **J3 : Knuth-Plass devient le moteur de composition par
  défaut.** On reste pour l'instant sur le 4ᵉ chiffre (`24.71`
  → `24.72`) — alignement maintenu avec la gem Ruby
  asciidoctor-pdf 2.3.24, en attendant de décider explicitement
  si ce shard, qui s'éloigne progressivement du fonctionnement
  de la gem (Knuth-Plass natif, hyphenation Liang, CLI
  spécifique, etc.), doit basculer sur un versioning autonome
  (bump du 3ᵉ chiffre voire passage à un nouveau schéma
  SemVer).
+
  `ParagraphComposer.compose_knuth_plass` implémente
  l'algorithme de Knuth & Plass (*Breaking Paragraphs into
  Lines*, Software Practice & Experience vol. 11, p. 1119-1184,
  1981), l'algorithme historique de TeX. Principe :
+
  0. Identification des breakpoints légaux (chaque `Glue` qui
     suit une `Box`, chaque `Penalty` non infinie, fin du
     paragraphe).
  0. Pour chaque breakpoint candidat B, programmation dynamique
     sur tous les breakpoints A précédant B : calcul de la
     `badness` de la ligne A→B (`100 · |adjustment_ratio|³`,
     formule TeX), des `demerits` (`(1 + badness)² +
     penalty² + double_hyphen_demerits`), mémorisation du
     chemin optimal.
  0. Backtrace pour reconstruire la séquence optimale.
  0. Fallback gracieux sur first-fit si aucun chemin admissible
     (paragraphe pathologique : mot trop long, target_w trop
     petit).
+
  Bénéfices visibles :
+
  0. **Lignes équilibrées** dans un paragraphe (les
     `adjustment_ratio` sont proches d'une ligne à l'autre)
     contre first-fit qui produit des « tasse-puis-vide ».
  0. **Exploitation des Penalty de césure J2** : quand un mot
     long ferait un étirement extrême, K-P préfère couper le
     mot (Penalty flagged, tiret ajouté) plutôt qu'écarteler
     tous les espaces de la ligne.
  0. **Moins de rivers** (alignements verticaux d'espaces
     gênants) grâce à l'harmonisation.
  0. **Pénalité de double césure** (`10000` demerits) pour
     éviter deux lignes consécutives terminées par un tiret.

* **Suppression du garde-fou `max_extra_factor = 2.5`** dans
  `render_segment_line`. Cette heuristique faisait basculer
  silencieusement en alignement gauche quand l'`extra_per_space`
  first-fit dépassait 2,5× la largeur d'un espace normal —
  pansement sur le mauvais comportement de first-fit qui
  remplissait chaque ligne au maximum sans regarder la suite.
  Avec Knuth-Plass, l'étirement reste raisonnable par
  construction (K-P minimise la badness² globalement), et
  l'utilisateur retrouve toujours la justification demandée
  par son thème. Les rares paragraphes pathologiques (mot
  unique trop long sur une page très large) produisent
  désormais un étirement visible mais correct, plutôt qu'un
  silent fallback en `left` qui rompait l'apparence du document.

== v2.3.24.71 — 2026-06-03

=== Ajouté

* **J2 : module `Hyphenation` (algorithme Liang).** Nouvelle
  implémentation de l'algorithme de césure par patterns Liang
  (Franklin Mark Liang, *Word Hy-phen-a-tion by Com-pu-ter*,
  thèse Stanford, 1983) — l'algorithme historique de TeX.
+
  Trois sous-modules :
+
  0. **`Hyphenation::Trie`** : structure de patterns Liang.
     `insert(pattern)` accepte les chiffres-valeurs entre les
     lettres (`1ba`, `ab3a`, `.ad4d`), `scores_for(word)`
     calcule les scores Liang à chaque position du mot encadré
     de `.`.
  0. **`Hyphenation::Hyphenator`** : algorithme principal.
     `hyphenate(word)` retourne les positions de césure légales
     en respectant `LEFT_MIN = 2` et `RIGHT_MIN = 3` (valeurs
     TeX par défaut), avec support des exceptions
     (`\hyphenation{syl-la-bus}`).
  0. **`Hyphenation::Loader`** : cascade de résolution
     projet `./hyphenation/hyph-<lang>.tex` →
     XDG `${XDG_CONFIG_HOME:-~/.config}/asciidoctor-pdf/hyphenation/`
     → embarqué au build (7 langues incluses). Cache
     class-level, alias `en` → `en-us`, `de` → `de-1996`.

* **7 patterns Liang embarqués au build via `read_file`** :
  FR (~ 30 ko), EN-US (~ 33 ko), DE-1996 (~ 268 ko), ES (~ 42 ko),
  IT (~ 5 ko), PT (~ 5 ko), NL (~ 86 ko). Total : ~ 470 ko
  embarqués au binaire. Sources : CTAN (paquet hyph-utf8),
  licences MIT / LPPL-1.3 / Knuth-permissive / BSD-3-Clause selon
  la langue (cf. `src/asciidoctor_pdf/hyphenation/manifest.cr`
  pour le détail).

* **Manifest des langues CTAN connues**
  (`src/asciidoctor_pdf/hyphenation/manifest.cr`) : pour chaque
  pattern, URL canonique CTAN, SHA-256 attendu, taille, licence,
  flag `embedded`. 7 entrées pour l'instant (les 7 langues
  embarquées) ; à étendre vers les ~ 80 langues hyph-utf8 dans
  une itération ultérieure.

* **Intégration dans `ParagraphComposer.tokenize`** : le
  composer accepte un paramètre `hyphenator` optionnel. Chaque
  mot dont le cœur alphabétique fait au moins
  `LEFT_MIN + RIGHT_MIN` lettres est fragmenté en sous-Box
  reliées par `Penalty(cost = 50, flagged = true,
  width = hyphen_w)`. La ponctuation de tête / queue reste
  collée à la première / dernière syllabe. En
  `compose_first_fit` (J1), les Penalty sont ignorées (rendu
  identique au mot monolithique) — la valeur viendra à J3 avec
  Knuth-Plass.

* **Détection de la langue du document** dans `Converter` :
  attribut AsciiDoc `:lang:`. Stocké dans `@document_lang`,
  utilisé par `wrap_segments` pour résoudre l'hyphenator via
  `Hyphenation::Loader.for(@document_lang)`. Pas de défaut
  implicite : si `:lang:` n'est pas posé, pas de césure
  (composition gracieuse).

* **Sous-commande CLI `hyph`**
  (`bin/crystal-asciidoctor-pdf hyph <sub>`). 7 sous-commandes
  conformes à `doc/CLI_HYPH_SPEC.adoc` et aux conventions ALOLI
  (`help [<sub>]` obligatoire, flags long + court, refus
  explicite des sous-commandes inconnues, XDG strict) :
+
  0. **`list [-j|--json]`** : liste les patterns disponibles
     (embarqués + installés via XDG).
  0. **`available`** : liste les patterns du manifest local.
  0. **`install <lang> [-s|--sha256] [-u|--from] [-f|--force]`** :
     télécharge depuis CTAN, vérifie SHA-256 contre le manifest
     embarqué (refus si mismatch), valide le contenu
     (`\patterns{` ou `\hyphenation{` présent), refuse les
     fichiers > 1 Mo, écrit dans XDG.
  0. **`remove <lang>`** : supprime un pattern installé. Refuse
     de toucher les patterns embarqués au build.
  0. **`update [<lang>]`** : re-télécharge un ou tous les
     patterns installés.
  0. **`show <lang>`** : affiche les métadonnées (URL, SHA-256,
     licence, taille, statut).
  0. **`help [<sub>]`** : aide générale ou ciblée.
+
  Codes de sortie numérotés (0 / 2 / 3 / 4 / 5 / 6 / 7 / 8 / 9 / 10)
  pour scripting. Inspiration `tlmgr` (TeX Live Manager) : pas
  de téléchargement automatique au runtime, action toujours
  explicite, vérification SHA-256 obligatoire.

=== Tests

* 17 nouveaux specs : 7 sur `Hyphenation::Trie/Hyphenator/Loader`,
  5 sur l'intégration `ParagraphComposer.tokenize` avec
  hyphenator. Total : 237 / 237 verts.

== v2.3.24.70 — 2026-06-03

=== Corrigé

* **Fond gris du codespan débordait sur l'espace de tête.**
  Quand le `ParagraphComposer` rend un codespan (ou kbd / button
  / mark) précédé d'une Glue de justification, le segment porte
  un préfixe d'espace ASCII (` code` au lieu de `code`). Le
  rectangle de fond du badge couvrait ce préfixe, ce qui faisait
  déborder le fond gris à gauche du premier vrai caractère. Le
  rectangle est désormais décalé d'un `leading_space_w` calculé
  à la mesure de la police courante : le fond démarre au premier
  caractère et s'arrête au dernier, l'espace de tête reste
  blanc et joue son rôle de séparateur visuel.

* **Espace fantôme inséré entre `` `code` `` et la ponctuation
  collante (`)`, `.`, `,`, `;`, `:`, `!`, `?`, `]`, `}`, `»`).**
  Deux causes en cascade :
+
  0. `ParagraphComposer#tokenize` injectait un Glue artificiel
     entre deux Box de segments contigus (« compatible bug-pour-
     bug » avec l'ancien `wrap_segments`). Cette branche est
     supprimée : le HTML d'asciidoctor contient déjà les espaces
     nécessaires dans le texte des segments, on ne doit pas en
     fabriquer. Les segments sans espace inter-frontière sont
     désormais rendus contigus (`<strong>foo</strong>bar` →
     `foobar`).
  0. `render_segment_line` ajoutait `codespan_padding_x` (et
     équivalents kbd / button / mark) à `current_x` après chaque
     badge, pour empêcher le prochain mot de chevaucher le BG du
     badge. Ce padding est désormais conditionnel : il n'est
     pas appliqué si le segment suivant commence par un espace
     (séparation déjà fournie) ou par une ponctuation collante.
     Avant le fix : « `code` ). » (visible). Après : « `code`). ».

  Spec dédiée `paragraph_composer_spec` : nouveau cas
  « préserve le `)` collé après un codespan » qui valide la
  séquence de tokens attendue pour `(ex : ` `` `code` `` `)`.

  Note : un écart résiduel de ~1-2 pt peut subsister sur les
  lignes très justifiées (l'`extra_per_space` first-fit est
  appliqué pour l'espace de tête du codespan, ce qui décale
  la ponctuation suivante). Sera réglé par J3 (Knuth-Plass)
  qui supprime cette heuristique au profit d'une distribution
  globale optimale.

== v2.3.24.69 — 2026-06-03

=== Corrigé

* **NBSP française écrasée avant d'atteindre le wrap.** La regex
  de normalisation des whitespace dans `InlineRenderer.parse`
  (`gsub(/\s+/, " ")`) traitait l'espace insécable U+00A0 comme
  un whitespace ordinaire (PCRE2 en mode Unicode classe la NBSP
  dans `\s`). Résultat : toutes les NBSP fraîchement insérées
  par `apply_french_typography` (devant `:`, `;`, `!`, `?`, `»`)
  étaient remplacées par un espace ASCII une étape plus loin,
  et `wrap_segments` cassait la ligne dessus. La règle de
  l'Imprimerie nationale n'était pas respectée : « deploy : il »
  pouvait se voir séparé en « deploy » sur une ligne et « : il »
  sur la suivante.

  La regex devient `gsub(/[^\S\x{00A0}]+/, " ")` — « tout
  whitespace SAUF la NBSP ». Les NBSP sont désormais préservées
  tout au long du pipeline et atteignent `wrap_segments` qui ne
  splittait déjà que sur l'espace ASCII strict.

=== Ajouté

* **Moteur de composition de paragraphes structuré
  (`ParagraphComposer`).** Refactor de `wrap_segments` qui
  délègue désormais à un nouveau module
  `src/asciidoctor_pdf/paragraph_composer.cr` modélisant le flux
  de texte en tokens TeX : `Box` (mot ou groupe NBSP-lié,
  largeur fixe non-cassable), `Glue` (espace élastique, point
  de coupure légal), `Penalty` (saut forcé / interdit /
  conditionnel). L'algorithme `compose_first_fit` reproduit
  fidèlement le comportement greedy historique — 217/217 specs
  passent. Bénéfice immédiat : l'insécabilité des NBSP est
  garantie par construction (elles sont absorbées dans la Box
  voisine et ne peuvent plus être des break-points).

  Référence : Knuth & Plass, *Breaking Paragraphs into Lines*,
  Software Practice & Experience, vol. 11, p. 1119-1184, 1981.

  Cette structure est la fondation typée pour les jalons
  suivants : J2 (patterns Liang de césure pour FR/EN/DE/ES/IT/
  PT/NL embarqués + sous-commande CLI `hyph` pour ajouter
  d'autres langues), J3 (algorithme Knuth-Plass — optimisation
  globale du paragraphe minimisant la badness), J4 (sortie PDF
  via l'opérateur natif `Tw` ISO 32000-1 § 9.3.3).

=== Documentation

* **`doc/CLI_HYPH_SPEC.adoc`** : spécification de la
  sous-commande CLI `hyph` à venir (7 sous-commandes : `list`,
  `available`, `install`, `remove`, `update`, `show`, `help`).
  Inspiration `tlmgr` (TeX Live Manager) — pas de
  téléchargement automatique au runtime, vérification SHA-256
  obligatoire contre un manifest embarqué, cascade de
  résolution projet → XDG → embarqué. Implémentation
  ultérieure, indépendante du chantier composer.

== v2.3.24.68 — 2026-06-02

=== Corrigé

* **Admonition (NOTE/TIP/…) : 1re ligne décalée à droite & non
  justifiée.** Quand `crystal-asciidoctor` rend le corps d'une
  admonition, il enveloppe l'HTML inline dans
  `<div class="paragraph">\n<p>…</p>\n</div>`. Le `\n` entre
  `<div>` et `<p>` était conservé par `InlineRenderer.parse_html`
  comme un segment initial blanc-seulement, qui se propageait
  ensuite dans `wrap_segments` comme un espace de tête —
  décalant visuellement le premier mot (ex. `beryl`) d'environ
  une largeur d'espace par rapport aux lignes suivantes.
  `parse_html` ignore désormais un buffer blanc-seulement
  **uniquement quand c'est le tout premier segment** (sans
  impact sur les espaces inter-mots plus loin dans le flux).

* **Corps d'admonition non justifié.** `convert_admonition`
  appelait `render_segment_line` sans `target_w` / `align`,
  donc le pipeline de justification (`max_extra_factor`,
  distribution par espace) ne se déclenchait jamais. Il passe
  désormais `content_w` et `@theme.base_text_align`, avec la
  dernière ligne forcée à `"left"` (cohérent avec
  `render_inline_lines`).

* **Garde-fou de justification trop strict sur les contenus
  étroits.** `max_extra_factor` relevé de `1.5` à `2.5` × la
  largeur normale d'espace. Le plafond à 1.5 bloquait des
  justifications légitimes sur des lignes peu espacées ou avec
  mots longs (ex. `un serveur bare-metal…`,
  candidat 5,16 pt > limite 4,77 pt). Le plafond à 2.5 attrape
  toujours les vrais cas pathologiques (mot isolé seul, mise en
  page cassée) sans rejeter de vrais paragraphes.

== v2.3.24.67 — 2026-06-02

=== Modifié

* Dépendance `crystal-asciidoctor` bumpée à **`~> 2.0.26.8`** :
  fix de la regex `HardLineBreakRx` qui ne capturait qu'un seul
  hard break par paragraphe (Crystal interprète le flag `m` comme
  « `.` matche aussi `\n` » → `(.*)` greedy sur plusieurs lignes).
  Désormais tous les `+` en fin de ligne d'un paragraphe sont
  correctement convertis en `<br>`.

== v2.3.24.66 — 2026-06-02

=== Modifié

* Dépendance `crystal-asciidoctor` bumpée à **`~> 2.0.26.7`** —
  retrait du hack `0.` répété pour les listes ordonnées. La
  convention canonique AsciiDoc (`.` répété pour
  auto-numérotation, ou numérotation manuelle `1.` `2.` `3.`)
  redevient la seule voie. Cf. note mémoire ALOLI
  `feedback_asciidoc_ordered_lists.md`.

== v2.3.24.65 — 2026-06-02

=== Ajouté

* **Rendu du code inline (codespan).** Nouvelle catégorie de
  thème `codespan_*` portée de la spec `asciidoctor-pdf` Ruby
  2.3.24 : fond grisé + bordure optionnelle + couleur de texte +
  padding configurable derrière les segments inline en `` `text` ``.
  Avant ce port, le code inline était rendu en police mono mais
  sans aucun habillage visuel — `_default.yml` ressemblait à du
  texte ordinaire.

* **Typographie française (extension `x-`).** Nouvelle propriété
  de thème `x_french_typography` (`true` par défaut dans le thème
  `fr`, `false` ailleurs). Quand active, le converter remplace
  l'espace ordinaire devant `:`, `;`, `!`, `?`, `»` et après `«`
  par un espace insécable U+00A0 — respecte la règle de
  l'Imprimerie nationale et évite les retours à la ligne
  inappropriés au milieu d'une ponctuation à deux parties.
+
Les blocs de code (`[source,...]`, ` ```), les blocs literal et
les attributs (style, language) sont **préservés** car ils ne
passent pas par le pipeline inline parser.

* **Markup inline préservé dans les items de liste.** Auparavant
  `render_list` passait le texte de chaque item par
  `strip_inline_markup` + `draw_text_run` brut — résultat : tous
  les italiques, gras, code inline, liens, etc. étaient *strippés*
  et rendus comme du texte plat. Désormais le pipeline complet
  (`InlineRenderer.parse` + `wrap_segments` + `render_segment_line`)
  est utilisé, identique à celui des paragraphes. Le justify
  s'applique aussi aux items longs (lignes non-terminales).
+
Propriétés exposées dans le thème (defaults conformes au défaut
asciidoctor-pdf upstream) :
+
[cols="2,2,3"]
|===
| Propriété | Défaut | Effet

| `codespan_background_color`
| `f5f5f5`
| Fond gris très clair derrière le code

| `codespan_border_color`
| _vide_
| Bordure désactivée par défaut

| `codespan_border_width`
| `0.0`
| Idem

| `codespan_font_color`
| _vide_
| Hérite de la couleur de texte courante

| `codespan_padding_x`, `codespan_padding_y`
| `2.0`, `1.0`
| Espace autour du texte (points)
|===

* **Paragraphes justifiés par défaut** (`base_text_align: "justify"`).
  Le rendu distribue l'espace inutilisé d'une ligne entre les
  espaces inter-mots ; la dernière ligne d'un paragraphe reste
  alignée à gauche pour ne pas étirer un orphelin. Configurable
  via la propriété `base_text_align` du thème (valeurs : `"left"`,
  `"center"`, `"right"`, `"justify"`). Conforme à
  l'asciidoctor-pdf Ruby où `base_align: justify` est le défaut.
+
Algorithme : pour chaque ligne sauf la dernière, on calcule la
largeur naturelle des segments, on compte les espaces internes,
puis on répartit `(cible − naturelle) / n_espaces` après chaque
segment proportionnellement au nombre d'espaces qu'il contient.
Approximation simple (les espaces *entre* segments sont étirés,
ceux internes à un seul segment restent fixes) — suffisante pour
les paragraphes ordinaires avec markup léger (italique, code
inline, etc.).

=== Modifié

* Dépendance `crystal-asciidoctor` bumpée à **`~> 2.0.26.6`** —
  les blocs hybrides `[source,LANG]` + `\`\`\`` (forme acceptée par
  Asciidoctor Ruby en mode de compatibilité Markdown) reçoivent
  désormais correctement leur attribut `language`. Conséquence :
  coloration syntaxique + fond grisé + classe `language-LANG`
  appliqués comme pour la forme canonique `[source,LANG] ----`.

== v2.3.24.64 — 2026-06-02

=== Modifié

* **Les fontes CJK se rendent désormais nativement.** Contrainte
  `pdf` passée à `~> 0.6.4`, qui ajoute le subsetting et l'embedding
  natifs des fontes OpenType/CFF (`.otf`). Toutes les Noto CJK sont
  distribuées en `.otf` ; jusqu'ici elles déclenchaient
  `UnsupportedFontFormat` et les caractères CJK retombaient sur `?`.
  Avec pdf 0.6.4, une fonte Noto CJK en cache est chargée
  paresseusement au premier caractère CJK et embarquée en subset
  `CIDFontType0` — les idéogrammes se rendent et le texte est
  extractible.
* Commentaires de l'accesseur paresseux `font_cjk` mis à jour : le
  `rescue UnsupportedFontFormat` est désormais un filet de sécurité
  pour une fonte réellement corrompue / exotique, plus le chemin
  attendu du CJK.

=== Corrigé

* La spec d'intégration `non_winansi_chars_spec` ne code plus en dur
  l'attente obsolète « CJK → `?` ». Elle teste désormais le
  comportement selon la présence d'une fonte Noto CJK en cache
  (rendue, pas de `?`) ou son absence (fallback `?`). La limite
  Identity-H du helper d'extraction de texte est documentée (il lit
  les GID bruts, pas le ToUnicode, donc il ne peut pas reproduire le
  texte CJK exact — seulement la présence / absence du `?`).

== v2.3.24.63 — 2026-06-02

=== Corrigé

* **Blocs attachés aux items de liste rendus correctement.** Quand
  un item de liste numérotée (ou à puces) avait un bloc attaché
  via `+` (typiquement un bloc source `[source,console] ----`,
  une admonition, un tableau, un autre paragraphe), `render_list`
  itérait uniquement sur les *sous-listes* et ignorait
  silencieusement tous les autres types de blocs enfants.
+
Conséquence : dans les recettes de type « 1. Étape avec sa
commande / 2. Étape suivante avec sa commande » (très fréquent
dans les READMEs aloli-crystal), seuls les libellés des étapes
apparaissaient dans le PDF, les commandes disparaissaient.
+
Fix : tous les blocs enfants des items sont désormais routés via
le dispatcher général (`dispatch(sub_block, sub_block.context.to_s)`)
qui appelle le bon `convert_*` selon le type.

== v2.3.24.62 — 2026-06-01

=== Modifié

* Dépendance `crystal-asciidoctor` bumpée à **`~> 2.0.26.5`** —
  accepte désormais la convention ALOLI `0.` répété pour les
  listes ordonnées auto-numérotées :
+
[source,asciidoc]
----
0. Premier item
0. Deuxième item
0. Troisième item
----
+
Rendu : « 1. Premier item », « 2. Deuxième item », « 3. Troisième
item ». L'auteur peut réorganiser ses items sans renuméroter
chaque ligne, et le marqueur `0.` reste visible dans l'éditeur
(contrairement à `.` répété, canonique mais plus discret).

== v2.3.24.61 — 2026-06-01

=== Modifié

* Dépendance `crystal-asciidoctor` bumpée à **`~> 2.0.26.4`** — les
  warnings du parser sur les listes ordonnées affichent désormais
  le numéro de ligne du source où ils ont été détectés :
+
[source]
----
asciidoctor: WARNING: list item index: expected 2, got 1 (line 6)
asciidoctor: WARNING: list item index: expected 3, got 1 (line 7)
----
+
Auparavant, ces warnings étaient anonymes et l'auteur devait
fouiller à la main pour identifier la liste fautive — pénible
sur les documents longs (README, manuels d'audit, etc.).

== v2.3.24.60 — 2026-06-01

=== Corrigé

* **Chargement de la police CJK véritablement paresseux.** En
  v2.3.24.59, l'accesseur `font_cjk` était paresseux, mais
  `text_with_emoji_segments` l'appelait **dès le premier run de
  texte** du document — ce qui déclenchait l'avertissement
  `UnsupportedFontFormat` (.otf CFF non supporté) pour *tout*
  document, y compris ceux n'ayant aucun caractère CJK.
* Désormais, `font_cjk` n'est sollicitée que pour un caractère
  *plausiblement CJK* (`ord >= 0x3000` — début de l'espace
  CJK Symbols and Punctuation). En-dessous, on est sur du Latin
  ou de la ponctuation typographique européenne (apostrophe
  courbe `'`, tiret cadratin `—`, points de suspension `…`, etc.)
  que la fonte CJK ne couvre pas — inutile de la charger pour
  s'en rendre compte. Conséquence : les documents européens
  sans CJK ne déclenchent plus l'avertissement.
* L'avertissement reste émis **une seule fois** quand un vrai
  caractère CJK (japonais, chinois, coréen, etc.) est rencontré
  dans le document.

== v2.3.24.59 — 2026-06-01

=== Ajouté

* **Type `signature`** dans `[x-form]` — désormais pleinement
  fonctionnel côté PDF. Émet un widget `/Sig` AcroForm valide avec
  `/SigFlags 3` sur le formulaire racine. Le slot est cliquable
  dans les visualiseurs PDF (Acrobat, Preview, Foxit). La signature
  électronique elle-même (PKCS#7, PAdES) reste à charge de
  `aloli-crystal/pdf-signature` (chantier en cours).
* **Démo PDF d'audit ISO 27001** : `examples/x-form-audit-iso27001.adoc`
  + son rendu PDF interactif (`.pdf`). Couvre les types text,
  email, date, radio (Hash options), textarea, checkbox, select,
  select-multi, et le layout multi-colonnes sur la section de
  synthèse.

=== Modifié

* Dépendance `pdf` bumpée à **`~> 0.5.9`** (apporte `signature_field`
  AcroForm + apparences custom de Dropdown/RadioGroup + OutputIntent
  ICCBased — trajectoire J1 ISO PDF).
* **Chargement paresseux de la police CJK** : `noto-cjk` n'est
  désormais chargée qu'au premier caractère CJK rencontré dans le
  document. Bénéfices : zéro coût pour les documents sans CJK
  (99 % des usages ALOLI), évite la levée immédiate de
  `UnsupportedFontFormat` pour les `.otf` (CFF non encore
  subsettable — cf. note mémoire `roadmap_pdf_cff_subsetting.md`).

== v2.3.24.58 — 2026-06-01

=== Ajouté

* **Extension `[x-form]`** — formulaires PDF interactifs (AcroForm,
  ISO 32000-1 §12.7) décrits en YAML embarqué dans un bloc literal
  AsciiDoc. Conformes aux exigences ALOLI : audits ISO 27001, quiz
  Quiméo offline, formulaires RGPD/consentement.
* Grammaire complète documentée dans `doc/x-form-spec.adoc` :
** 10 types de champs : `text`, `email`, `url`, `tel`, `password`,
   `textarea`, `number`, `date`, `checkbox`, `radio`, `select`,
   `select-multi` (le type `signature` est *refusé* en v1, attend
   `pdf-signature`).
** Organisation : `fields:` à plat *OU* `sections:` titrées
   (mutuellement exclusifs).
** Options : liste simple `[A, B, C]` ou Hash `{code: label}`
   (Hash YAML natif, ordre préservé, /Opt en paires `[export, display]`).
** *Layout multi-colonnes* : `columns: N` au niveau form ou
   section, `cols: M` (span) au niveau champ, flow auto
   gauche → droite.
** Validation atomique : regex, intervalle min/max (`required_if`
   et `visible_if` reportés en v2 avec JavaScript PDF).
** Boutons : `submit:` (un seul), `reset:`, `buttons:` custom
   (refusés en v1 — Btn pushbutton attend pdf v0.6+).
* Implémentation : modules `AsciidoctorPDF::FormBuilder` (parser
  YAML + validation), `AsciidoctorPDF::FormRenderer` (helpers de
  layout), intégration dans `convert_listing` (détecte
  `attributes["style"]? == "x-form"`).
* Couverture : 92 specs ajoutées (66 FormBuilder + 13 FormRenderer
  + 13 end-to-end PDF avec /AcroForm validé).

=== Modifié

* Dépendance `pdf` épinglée sur **`~> 0.5.7`** (apporte AcroForm
  + rejet propre des fontes OpenType/CFF, prérequis pour `[x-form]`).
* Police CJK (`noto-cjk`) tolère désormais les fontes OpenType/CFF
  (`.otf`) qui ne sont pas encore subsettables : un avertissement
  est émis et `@font_cjk` reste nil — le converter retombe sur le
  fallback `?` pour les CJK. Cf. note mémoire ALOLI
  `roadmap_pdf_cff_subsetting.md` (subsetting CFF prévu).

== v2.3.24.57 — 2026-05-08

=== Ajouté

* **Configuration utilisateur (XDG Base Directory)** : le CLI lit
  désormais `${XDG_CONFIG_HOME:-~/.config}/crystal-asciidoctor-pdf/config.yml`
  pour fixer une fois pour toutes les préférences récurrentes :
** `theme:` — thème par défaut (nom embarqué, fichier dans
   `themes/`, ou chemin absolu)
** `attributes:` — attributs AsciiDoc injectés systématiquement
   (`title-page-toc`, `toc`, `pdf-page-size`, ...)
** `author:`, `email:`, `organization:` — méta auteur par défaut
* Cascade de priorité documentée : flags CLI > attributs du
  document `.adoc` > user config > defaults compilés. Le document
  garde toujours le dernier mot.
* Nouveau flag `-N` / `--no-user-config` pour reproduire le
  comportement « vanilla » (CI, debug).
* Sous-répertoire `themes/` au sein du répertoire XDG : permet de
  référencer un thème utilisateur par simple nom (ex : `theme: aloli`
  → `~/.config/crystal-asciidoctor-pdf/themes/aloli.yml`).
* `data/config.example.yml` : modèle prêt à copier.
* Section _Configuration utilisateur_ dans le README.
* Convention XDG à généraliser à tous les autres shards
  aloli-crystal qui stockent une configuration (crystal-beryl
  notamment).

== v2.3.24.16 — 2026-04-29

=== Modifié

* **Références de modules mises à jour** suite au renommage amont
  des modules internes (convention Aloli : retrait du préfixe
  `Crystal` sur les noms de modules internes) :
** `CrystalEmojis::*` → `Emojis::*`
** `CrystalFlags::*` → `Flags::*`
** `CrystalNotoCJK::*` → `NotoCjk::*`
* Épinglages `shard.yml` re-pinés :
** emojis `~> 0.2.2`, flags `~> 0.1.2`, noto-cjk `~> 0.1.2`.
* Aucun changement fonctionnel.

== v2.3.24.15 — 2026-04-29

=== Modifié

* **Dépendances mises à jour** suite au renommage en cascade des
  shards de niveau 0 (convention Aloli : les créations originales
  abandonnent le préfixe `crystal-` sur l'URL GitHub) :
** `pdf` → `pdf`
** `flags` → `flags`
** `emojis` → `emojis`
** `noto-cjk` → `noto-cjk`
* Clés de dépendance dans `shard.yml`, chemins `require` internes et
  URLs documentaires mis à jour. Aucun changement fonctionnel.
* Le shard crystal-asciidoctor-pdf lui-même est un portage de gem
  Ruby et *conserve* son préfixe `crystal-` selon la convention.

== v2.3.24.14 — 2026-04-26

**Rendu des glyphes CJK** via le nouveau shard
https://github.com/aloli-crystal/noto-cjk[noto-cjk].
Quand l'utilisateur a peuplé le cache CJK (via
`noto-cjk pull` ou `CrystalNotoCJK::Cache.pull`), le
converter détecte automatiquement les caractères CJK dans les
runs de texte, bascule la police active sur Noto Sans CJK pour
ces segments uniquement, et remet la police principale pour le
reste. Même pattern que les emojis qui basculent sur `page.svg`
pour les glyphes individuels.

* Nouvelle dépendance d'exécution : `noto-cjk ~> 0.1`.
* `text_with_emoji_segments` retourne désormais trois types de
  segments : `:text` (police par défaut), `:emoji` (page.svg),
  et `:cjk` (bascule de police vers @font_cjk). Le changement
  de police mid-run est fait inline dans `draw_text_run`.
* `load_theme_fonts` charge opportunistement
  `CrystalNotoCJK.font_path` si une variante est en cache.
  Aucun téléchargement n'est déclenché automatiquement —
  l'utilisateur peuple explicitement le cache.
* Le bilan en fin de conversion pointe désormais aussi vers
  `noto-cjk pull` avec les commandes concrètes pour
  chaque variante + le status actuel du cache.
* Aucune régression : 94 tests, tous verts.

== v2.3.24.13 — 2026-04-26

**Le pipeline emoji atteint les paragraphes** plus message
d'aide en fin d'exécution qui indique à l'auteur exactement quoi
faire quand des caractères ont été substitués par `?`.

* `render_segment_line` (le chemin utilisé pour le texte des
  paragraphes) route désormais via `draw_text_run` au lieu
  d'appeler `page.text` directement. Effet net : les emojis
  dans le texte des paragraphes sont rendus en couleur via
  emojis (avant, seuls les titres et quelques autres
  chemins étaient emoji-aware).
* Bilan en fin de conversion : quand `@warned_chars` n'est pas
  vide, un message multi-ligne est affiché sur STDERR avec le
  compte, un échantillon des caractères substitués, et les
  commandes exactes à lancer pour obtenir une couverture
  complète (`emojis pull`, plus une note sur le futur
  `noto-cjk` pour le CJK).
* Dépendance renommée `emojis-lite` → `emojis`
  (~> 0.2). Même nom de module `CrystalEmojis`, même surface
  d'API, plus le nouveau module `Cache` qui permet à
  l'application hôte de populer le cache disque
  programmatiquement.
* Aucune régression : 94 tests, tous verts.

== v2.3.24.12 — 2026-04-26

**Meilleure détection de la couverture de la police** — corrige
une substitution trop agressive de v2.3.24.10 qui transformait
en `?` des caractères valides (Cyrillique Привет, Grec Καλημέρα,
Hébreu שלום, dingbats simples ✓ ✗ ★) alors que DejaVu Sans (la
police embarquée par défaut) pouvait parfaitement les rendre.

L'ancien `safe_text` faisait un test WinAnsi statique en
supposant Type1 Helvetica/Courier — mais le converter utilise
en fait DejaVu Sans TrueType embarquée par défaut, qui couvre
une plage de codepoints bien plus large. Le nouveau pipeline
interroge directement la police active via la nouvelle méthode
`TrueTypeFont#has_glyph?(char)` de `pdf v0.3.5`, et ne
retombe sur la table WinAnsi statique que pour les polices Type1
(rare — uniquement quand DejaVu est désactivée dans le thème).

* Bump dep `pdf` à `~> 0.3.5`.
* `Converter#safe_text(text, font_name = @fn_body)` prend
  désormais le nom de la police et route la couverture
  par-caractère via la requête TrueType `has_glyph?`.
* `font_object_for(font_name)` extrait du `case` dupliqué de
  `set_font` — source unique de vérité pour résoudre un nom de
  police vers son instance `PDF::Fonts::Base`.
* Message d'avertissement mis à jour : « caractère « X » (U+NNNN)
  sans glyphe dans la police active » (avant : « absent de
  WinAnsi »).
* 94 tests, toujours verts, aucune régression.

== v2.3.24.11 — 2026-04-26

**Rendu des emojis en couleur** pour les ~208 emojis couverts par
`emojis-lite`. Les glyphes comme ✅ ❌ ⚠ 🔴 🟢 🟡 🔵 💻 🔑 ⚙
sont maintenant rendus comme SVG embarqués (Twemoji, en couleur)
au lieu d'être substitués par `?`. Les emojis hors set lite
retombent toujours sur `?` + warning (comportement v2.3.24.10) —
pour une couverture complète, remplacer `emojis-lite` par
`emojis-full` dans l'arbre de dépendances.

* Nouvelle dépendance d'exécution :
  https://github.com/aloli-crystal/emojis-lite[emojis-lite]
  ~> 0.1.
* Nouvel helper privé `Converter#text_with_emoji_segments(text)`
  qui découpe une chaîne en alternance de segments `:text` et
  `:emoji`, permettant à `draw_text_run` d'aiguiller chaque type
  vers le bon pipeline (texte → `page.text` + sanitize WinAnsi ;
  emoji → `page.svg` avec les mêmes dimensions que les drapeaux
  de `flags`).
* Mise à jour du test de non-régression du tofu silencieux : ✅
  et ❌ ne se retrouvent plus comme `?` dans le flux texte WinAnsi ;
  les caractères CJK et les emojis hors set lite y restent.
* **94 tests, tous verts.**

== v2.3.24.10 — 2026-04-26

Met fin aux carrés tofu silencieux pour les caractères hors plage
WinAnsi. Les polices Type1 standard (Helvetica, Courier — défauts
du converter) ne couvrent que ~220 codepoints (ASCII + Latin-1 +
quelques extras Windows-1252). Tout le reste — emojis (✅ ❌ 🔴),
dingbats (✓ ✗ ★), idéogrammes CJK, etc. — sortait jusqu'ici en
rectangles vides sans aucun avertissement à l'auteur.

Le correctif :

* Nouveau module `AsciidoctorPDF::WinAnsi` avec les helpers
  `representable?(char)` et `sanitize(text) { |char| … }`.
* Nouvelle méthode `Converter#safe_text(text)` qui substitue tout
  caractère hors WinAnsi par `?` et émet un avertissement dédupliqué
  sur STDERR (« Avertissement : caractère « ✅ » (U+2705) absent
  de WinAnsi, remplacé par « ? » dans le PDF. »). Un seul warning
  par caractère et par conversion — pas de spam si un même emoji
  apparaît N fois.
* `draw_text_run` passe tout run de texte par `safe_text` avant
  l'appel à `page.text`.
* Nouveaux tests : 11 specs unitaires pour `WinAnsi`, 2 specs
  d'intégration prouvant que les emojis ne plantent plus le
  converter et que le texte purement WinAnsi reste intact.
  **93 tests au total, tous verts.**

C'est un correctif conservateur qui supprime le mode d'échec
silencieux. L'étape suivante (prévue comme un shard à part) est
`emojis`, qui rendra les vrais **emojis colorés** comme
glyphes SVG embarqués (même pattern que `flags` pour les
drapeaux).

== v2.3.24.9 — 2026-04-25

Entrées de TOC cliquables. Chaque ligne de la table des matières
est désormais une annotation PDF de type Link qui amène le lecteur
directement sur le titre de la section correspondante — fini de
scroller à travers 80 pages pour trouver le chapitre 12. Le panneau
d'outline PDF (signets dans la barre latérale du viewer) bénéficie
de la même amélioration : un clic sur un signet positionne le
lecteur sur le titre, plus seulement en haut de la page.

* `convert_section` enregistre une destination nommée PDF pour
  chaque section, indexée sur l'id AsciiDoc (auto-généré par le
  parseur, ex. `_overview`, ou fourni via `[[ancre]]`). La
  destination utilise `/XYZ` pour que le lecteur défile précisément
  au sommet du titre.
* `render_toc` émet une annotation `/Subtype /Link` par entrée
  couvrant toute la ligne (titre + pointillés + numéro de page),
  pointant vers la destination nommée. Pas de bordure visible —
  l'annotation est invisible, seul le curseur change au survol.
* `generate_pdf_outline` utilise désormais des destinations `/XYZ`
  au lieu de `/Fit`, pour que la navigation par signets atterrisse
  sur le titre — cohérent avec le comportement de la TOC interne.
* Correctif bonus : les entrées de TOC étaient enregistrées *avant*
  le `check_page_break` qui peut forcer une nouvelle page, ce qui
  produisait des numéros de page off-by-one pour les sections qui
  débordaient. L'entrée est désormais enregistrée après le saut de
  page, donc la TOC affiche toujours la page sur laquelle le titre
  est effectivement dessiné.
* 4 nouveaux tests d'intégration dans
  `spec/integration/toc_anchors_spec.cr` qui vérifient au niveau
  octet la présence des annotations `/Subtype /Link` et des
  destinations `/XYZ`. **34 tests d'intégration, 46 tests
  unitaires, 80 au total — tous verts.**

== v2.3.24.8 — 2026-04-23

Suite de tests d'intégration bout-en-bout. Le pipeline adoc → PDF
est désormais exercé via `spec/integration/*_spec.cr`, qui font
circuler de petits extraits AsciiDoc à travers le vrai converter
et inspectent le PDF produit via `PDF::Reader`.

* 7 fichiers de specs couvrant : sanity, espaces insécables,
  titres de section, tableaux, admonitions
  (NOTE/TIP/IMPORTANT/WARNING/CAUTION + forme bloc), listes
  (non ordonnée, ordonnée, définition), emphase inline
  (gras/italique/monospace), drapeaux de pays (y compris la
  régression BR qui bouclait), et métadonnées de titre du PDF.
  **30 tests d'intégration, 46 tests unitaires, 76 au total —
  tous verts.**
* Nouveau helper `IntegrationHelper` dans
  `spec/integration/spec_helper.cr` qui :
  - convertit une chaîne AsciiDoc via le vrai
    `AsciidoctorPDF::Converter`,
  - extrait le texte lisible des flux de contenu PDF (supporte à
    la fois les littéraux Type1 WinAnsi `(…)` et les chaînes hex
    TrueType Identity-H `<…>`, scan tolérant octet par octet),
  - lit le champ `/Info` `/Title` du PDF et décode le
    UTF-16BE-avec-BOM si nécessaire,
  - force les polices Type1 standard dans les runs de test pour
    que le texte extrait soit lisible en ASCII (le pipeline de
    police réel du converter est quand même testé — simplement
    basculé sur un encodage extractible).
* Pur Crystal, zéro dépendance sur un outil externe (pas de
  `pdftotext`, `unzip`, …) — le runner CI n'a besoin de rien
  d'autre que ce que `shards install` fournit.

== v2.3.24.7 — 2026-04-23

Étendre la gestion des espaces insécables à tous les renderers
qui émettaient encore des entités HTML brutes ou des U+00A0 bruts.

* **Titre du document** (`@document_title`) : maintenant décodé
  via `decode_html_entities` une seule fois dans
  `convert_document`, donc tous les consommateurs (page de
  titre, en-tête/pied de page, métadonnées PDF `/Title`, titre
  de l'index, etc.) voient un titre en texte brut. Un `{nbsp}`
  dans le titre AsciiDoc apparaissait auparavant littéralement
  comme `&#160;` partout — visible en particulier sur la page
  de garde et dans le pied de page.
* **Templates d'en-tête / pied de page** : `resolve_page_vars`
  remplace aussi tout U+00A0 subsistant par un espace ASCII,
  pour que l'en-tête/pied (qui ne passe pas par `draw_text_run`)
  n'affiche plus un tofu à la place d'un NBSP.
* **Page de titre** : `render_title_page` utilise désormais
  `draw_text_run` pour le titre, le sous-titre, l'auteur et la
  date au lieu d'appeler `page.text` directement ; tout NBSP
  hérité du source est rendu en espace visuel, et d'éventuels
  drapeaux inline dans ces champs fonctionneraient aussi.
* **Titres flottants, entrées de TOC, captions de tableau** :
  même bascule vers `draw_text_run` pour la cohérence.

Résultat : `{nbsp}` et `&nbsp;` dans *tout* texte côté auteur —
y compris le titre du document et la chrome — sont maintenant
rendus comme un espace visible, tandis que `wrap_text` continue
de les traiter comme séparateurs insécables.

== v2.3.24.6 — 2026-04-23

Préserver la propriété insécable de `&nbsp;` / `&#160;` lors du
wrap.

Le correctif de la v2.3.24.5 normalisait tout espace insécable en
espace ASCII *avant* le wrap, ce qui éliminait les tofus mais
supprimait aussi la propriété « ne pas couper » de la séquence —
les utilisateurs qui font un usage intensif de `&nbsp;` pouvaient
voir un saut de ligne précisément là où ils l'avaient écrit pour
l'empêcher.

Le pipeline devient :

* Les entités HTML `&#160;` et `&nbsp;` sont décodées vers le
  vrai caractère **U+00A0** (espace insécable). Un U+00A0 déjà
  présent dans l'entrée est conservé tel quel.
* `wrap_text` casse uniquement sur l'espace ASCII (la regex
  `/\s/` ne matche pas U+00A0 en Crystal), donc une paire de
  mots jointe par U+00A0 reste sur la même ligne.
* Juste avant chaque `page.text` (dans `draw_text_run` du
  converter et dans `InlineRenderer.render_line`), U+00A0 est
  remplacé par un espace ASCII dans la chaîne dessinée — le
  PDF est visuellement propre que la police active ait ou non
  un glyphe NBSP drawable.

Résultat : le rendu visuel est identique à la v2.3.24.5 (pas de
tofu), mais les sauts de ligne respectent maintenant les espaces
insécables de l'auteur.

== v2.3.24.5 — 2026-04-23

Extension du rendu des drapeaux + correctifs de glyphes d'espacement.

* **Drapeaux dans les paragraphes, admonitions et listes** aussi —
  plus seulement dans les tableaux. `draw_text_run` est désormais
  appelé depuis `convert_paragraph` (via `InlineRenderer.render_line`),
  `render_admonition`, `convert_dlist` et `render_list`. Toute paire
  d'indicateurs régionaux dans le corps du texte est maintenant
  rendue comme un vrai drapeau SVG en couleur.
* **Correction des carrés tofu provenant de `<br>`** : le renderer
  inline traduisait un `<br>` par un `\n` littéral dans le buffer
  de rendu, qui s'affichait en tofu car les polices Type1 n'ont pas
  de glyphe pour le retour à la ligne. Un espace normal est émis à
  la place ; les hard line breaks sont donc traités comme des
  séparateurs de mots. Une vraie mise en page multi-ligne pour
  `<br>` est laissée pour une passe ultérieure.
* **Correction des carrés tofu provenant des retours à la ligne
  souples** : le HTML généré par AsciiDoctor préserve les sauts de
  ligne du source dans les paragraphes. Le renderer inline et
  `strip_inline_markup` réduisent maintenant toute séquence
  d'espaces à un espace unique avant le dessin du texte.
* **Correction des carrés tofu d'espaces insécables** : `&#160;`,
  `&nbsp;` et U+00A0 brut sont désormais normalisés en espace
  ordinaire (la propriété « non séparable » est perdue, compromis
  acceptable étant donné que les polices Type1 standard n'exposent
  pas toujours un glyphe drawable pour le NBSP).
* **Mise à jour de dépendance** : `pdf` → utilise v0.3.3 via
  `~> 0.3`, qui ajoute un cache d'instances `SVG::Parser` au niveau
  du document pour que les drapeaux répétés dans un même document
  ne soient parsés qu'une fois.

== v2.3.24.4 — 2026-04-23

Vrais drapeaux de pays dans les cellules de tableau, via un nouveau
pipeline de rendu SVG.

* **Nouvelle dépendance** `flags` (~> 0.1) — 271 drapeaux
  SVG ISO 3166-1 alpha-2 issus de lipis/flag-icons, embarqués à la
  compilation.
* **Rendu inline des drapeaux dans les cellules de tableau** : les
  paires d'indicateurs régionaux Unicode (p.ex. `🇫🇷`, `🇩🇪`) sont
  détectées dans le texte des cellules et dessinées comme de vrais
  glyphes SVG en couleur sur la ligne de base du texte. Auparavant
  ces codepoints étaient rendus en tofu (DejaVu Sans ne contient
  pas de glyphe pour eux) et faussaient le calcul de largeur de
  colonne.
* **Mise en page** : `text_width` et `wrap_text` comptent désormais
  chaque drapeau comme une largeur fixe dérivée de la taille de
  police, afin que les colonnes réservent l'espace correct plutôt
  que la largeur du glyphe `.notdef` de la police texte.
* **Mise à jour de dépendance** : `pdf` → `~> 0.3` (utilise
  le correctif de boucle du path-parser et le correctif de
  remplissage des formes fermées livrés dans pdf v0.3.1 et
  v0.3.2).
* Nouveau module `AsciidoctorPDF::InlineFlags` avec `segments`,
  `flag_width` et `flag_height`.

== v2.3.24.3 — 2026-04-23

Corrections de mise en page et de typographie constatées sur des
documents réels.

* **Admonitions** (NOTE, TIP, IMPORTANT, WARNING, CAUTION) : un
  espacement correct sépare désormais le label du texte.
  Précédemment le label tenait dans une zone fixe de 60 pt quelle que
  soit sa largeur réelle, ce qui faisait visuellement coller des
  labels longs (`IMPORTANT`) au premier mot du message.
* **Titres de section** : les titres longs sont désormais découpés
  sur plusieurs lignes au lieu d'être rendus au-delà de la marge
  droite (et tronqués). Utilise l'assistant existant `wrap_text`
  avec la police des titres.
* **Cellules de tableau** : les mots qui ne tiennent pas dans leur
  colonne (typique pour les noms de pays dans des colonnes étroites
  proportionnelles, ex. `Allemagne` ou `Pays-Bas`) sont désormais
  découpés en sous-morceaux. Priorité de césure : tirets internes
  (`Pays-` + `Bas`), frontières camelCase, puis en dernier recours
  coupe caractère par caractère. Plus aucun débordement visuel dans
  la colonne voisine.
* **Taille de police du corps** : abaissée de 10.5 pt à 10 pt.
  Essentiellement cosmétique — les tableaux denses tiennent mieux en
  A4 sans changer la hiérarchie des titres.

== v2.3.24.2 — 2026-04-22

* Correction des accents et apostrophes français dans les messages
  du CLI et de l'aide (`PDF généré`, `thème`,
  `Générer le document de référence`, `l'aide`,
  `Erreur : aucun fichier d'entrée spécifié`, etc.).
* Quelques concaténations de chaînes converties en interpolations.

== v2.3.24.1 — 2026-04-22

* Restructuration du projet pour produire un vrai binaire natif.
  Le script source de la CLI est passé de `bin/crystal-asciidoctor-pdf`
  à `src/cli.cr` ; `shards build --release crystal-asciidoctor-pdf`
  produit désormais un binaire ELF/Mach-O dans `bin/crystal-asciidoctor-pdf`
  sans écraser aucun fichier source, rendant l'outil directement
  utilisable après `shards build`.
* Mise à jour du `.gitignore` : suppression du contournement
  `/crystal-asciidoctor-pdf-bin` et ignorage simple de `/bin/*`.
* Ajout du linter *ameba* à la CI avec configuration `.ameba.yml`
  partagée alignée sur l'écosystème Crystal.
* Ajout du journal des modifications bilingue (CHANGELOG.adoc en
  anglais, CHANGELOG.fr.adoc en français) ; suppression d'un
  CHANGELOG.md résiduel.
* Reformatage via `crystal tool format` de `inline_renderer.cr`,
  `theme.cr`, `theme_loader.cr`.

== 0.1.1 (2026-03-16)

* Renommage du binaire CLI de `asciidoctor-pdf` à `crystal-asciidoctor-pdf`.
* Modification du comportement de sortie par défaut : `mon_document.adoc`
  produit désormais `mon_document.adoc.pdf`.

== 0.1.0 (2026-03-13)

* Version initiale.
* Conversion de base des paragraphes, sections, listes et tableaux.
