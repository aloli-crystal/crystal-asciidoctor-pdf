= Changelog
:toc: left
:toc-title: Versions
:toclevels: 2

== v2.3.24.101 — 2026-06-25

=== Fixed (converting several files)

* **`crystal-asciidoctor-pdf *.adoc` only converted the first file.**
  The CLI only kept the FIRST positional argument
  (`unknown_args { |args| input_file = args.first? }`): a shell glob
  `*.adoc` produced a single PDF, the other files were silently ignored.
  The CLI now accepts a LIST of files and converts them all, each
  producing its own `<name>.adoc.pdf`.
+
  - `-o`/`--out-file` stays reserved for the single-file case
    (incompatible with multiple inputs, which would overwrite each
    other): explicit error otherwise.
  - A missing file no longer aborts the batch: it is reported, the others
    are converted, and the final exit code is non-zero.
+
  3 integration specs added (via the binary).

== v2.3.24.100 — 2026-06-22

=== Fixed

* **Article doctitle now shares the first page with its TOC.** For an
  `article` (default doctype, no title page) requesting a TOC
  (`:toc:`, `:toc: left`, …), the TOC took the whole first page on its
  own and the doctitle — title, author, date — landed at the top of
  the next page, so the TOC appeared "title-less".
+
  `convert_document` now renders the doctitle inline at the top of the
  first page, then reserves the rest of that page for the TOC (drawn
  below the title via `render_toc(..., y_start:)`); the body starts on
  the following page. This matches AsciiDoc's `auto` TOC placement for
  an article (title → TOC → content). Books (`:doctype: book`) and
  documents with an explicit `:title-page:` keep their title page and
  dedicated/embedded TOC unchanged. New test helper
  `IntegrationHelper.page_text` + one guard spec.

== v2.3.24.99 — 2026-06-19

=== Added

* **Repeat the table header on every page of a multi-page table.**
  When a table overflows onto the next page, its header row(s) (column
  labels) are redrawn at the top of each new page so the columns stay
  readable. Implemented via a `@repeat_table_header` hook set by
  `convert_table` and invoked by `check_page_break` right after a page
  break (disarmed during the re-render to avoid infinite recursion).
  Integration test added.

== v2.3.24.98 — 2026-06-16

=== Fixed

* **Bold-italic invisible in `[quote]` blocks.** Inside a quote
  block (which forces italic on every segment), any inline bold
  marker like `*mécanique*` should render as bold-italic — but
  the bold-italic variant of DejaVu Sans was missing from
  `data/fonts/`, and the theme's `base_font_bold_italic_path`
  was set to `nil`. The renderer fell back to Type1
  `Helvetica-BoldOblique` whose WinAnsi encoding can't draw the
  non-ASCII glyphs (`é`, `—`, …) that DejaVu produced elsewhere,
  so the segment was silently dropped — leaving a visible
  white gap in the line.

  Fix: bundle `DejaVuSans-BoldOblique.ttf` (dejavu-fonts 2.37,
  same family/license as the three other TTFs already shipped)
  and point `base_font_bold_italic_path` at it. Bold-italic
  segments now embed and render as expected.

* **Hard break `+\n` swallowed inside `[quote]` blocks.** Two
  cascading bugs:
+
  - `render_indented_block` rebuilt every `InlineSegment`
    to force italic, but forgot to carry `line_break: seg.line_break`
    over — so the marker segment that represents `+\n` (text empty,
    `line_break: true`) lost its flag and became a silently-ignored
    empty segment.
+
  - More importantly, `ParagraphComposer.compose_knuth_plass`
    treated `Penalty(NEG_INFINITY)` as a *candidate* breakpoint
    rather than as a *mandatory* one. Knuth-Plass would freely
    consider line-candidate pairs `(a, b)` that crossed over a
    forced break, picking the option with the lowest demerits —
    which typically meant skipping the break entirely. The
    `compose_first_fit` fallback handled forced breaks correctly
    (since v2.3.24.82, KP is the default for body wrapping,
    so this bug only surfaced now).
+
  Fix: precompute `next_forced[i]` = position of the next
  `Penalty(NEG_INFINITY)` at or after token `i` (O(n) backward
  scan). Before evaluating a candidate pair, reject it if
  `next_forced[start_idx] < b` — i.e. a mandatory break lives
  strictly inside the candidate line. The forced break becomes
  the only legal `b` to leave from `start_idx`'s region.

=== Notes

* Symptom that triggered the investigation: the
  « Conseil de fond » page in a real `tmp/strategie-saas.adoc`
  rendered `*mécanique*` as a blank space inside the quote, and
  the `+\n` after « PDF signé » in the same paragraph was lost
  (« PDF signé + » → « PDF signé auditable » as one line).
  Both fixed; visual diff confirms bold-italic word renders
  and the forced line break appears.

== v2.3.24.97 — 2026-06-08

=== Fixed (inline markup in table cells)

* **Codespans `` `…` ``, bold, italic and links are now rendered WITH
  their style inside table cells.** Previously `render_table_row`
  flattened every cell through `strip_inline_markup`: a password
  written `` `qwertyuiop` `` showed up as running text, with no
  monospace font and no shaded background — giving the impression that
  "the code wasn't displayed".
+
  The cell now goes through the same pipeline as paragraphs:
  `parse_inline` → `wrap_segments` → `render_segment_line`. The bold of
  headers / footers is propagated to the segments via `copy_with` (the
  font being chosen per segment by `resolve_inline_font`), and the row
  color is injected as the default segment color. New helper
  `measure_segment_line` for `halign=center|right` alignment.
+
  Preserved: bold headers, clickable links (`/URI` annotation),
  hyphenation of long words in a narrow column. 2 integration specs
  added (mono codespan + bold-header / mono-body coexistence).

== v2.3.24.96 — 2026-06-08

=== Fixed (title-page logo `:title-logo-image:`)

Four causes prevented a title-page logo from being displayed.

* **`image:` form (single `:`) supported.** `parse_title_logo_macro`
  only stripped the `image::` prefix (double `:`, block macro). The
  standard asciidoctor-pdf form — `image:logo.svg[…]` (single `:`,
  inline macro) — left the prefix glued to the path, resolution failed
  and the logo did not appear (silent failure).

* **Length units (`pdfwidth=4cm`).** The width was parsed by raw
  `to_f?`: `"4cm".to_f?` = `nil` ⇒ default width. A new
  `parse_logo_length` converts `cm`/`mm`/`in`/`pt` (and the bare
  number) into PDF points.

* **Relative path resolution (`safe=unsafe`).** asciidoctor's safe mode
  (API default `secure`) EMPTIES `docdir` and relativizes `docfile`,
  making it impossible to resolve a relative path outside the current
  directory. The CLI now forces `safe=unsafe` by default — like the
  `asciidoctor` CLI — for local files (overridable).
  `resolve_image_path_str` additionally falls back to the `docfile`
  folder.

* **Vertical positioning (via `pdf` ~> 1.20.1).** SVG rendering offset
  `<rect>` elements upward by their height; a logo at the top of the
  page ended up off-page. Fixed in the `pdf` shard.

=== Reminder

The title-page logo is only rendered on a TITLE PAGE. An `article`
(default doctype) must therefore declare `:title-page:` in its header
for a title page — and thus the logo — to be produced.

== v2.3.24.95 — 2026-06-08

=== Fixed (documentation)

* Removed an erroneous note from the v2.3.24.94 CHANGELOG claiming that
  links did not work on single-page documents. This was a false
  positive from a faulty `qpdf --qdf` check: raw-byte inspection
  confirms that the page does carry its `/Annots` and that the URLs are
  clickable, regardless of the number of pages.

== v2.3.24.94 — 2026-06-08

=== Fixed / Added

* **Bare URLs correctly linked** (via `crystal-asciidoctor`
  ~> 2.0.26.9). An auto-detected URL (`https://exemple.fr`) produced an
  `href` truncated to the scheme (`https://`) with the domain returned
  in the text. The parser fixes the full `href`; the URL now displays
  and points correctly.

* **Clickable links in table cells.** Cell rendering displays plain
  text (`strip_inline_markup`); we now overlay a `/URI` annotation on
  the text area of a cell that contains an external link
  (http/https/ftp/mailto). Covers the common "cell = URL" case.

Links are clickable both on a single-page and a multi-page document
(verified on the raw bytes of the PDF: the page carries an `/Annots`
referencing the `/URI` annotations).

== v2.3.24.93 — 2026-06-08

=== Changed

* **Title page aligned with standard asciidoctor.** A title page is now
  produced ONLY for a book (`:doctype: book`) or when an explicit
  `:title-page:` is set. An `article` (default doctype) no longer has
  an automatic title page: its title is rendered at the top of the
  first page, directly followed by the content — standard AsciiDoc
  behavior.
+
  Previously, the theme default `title_page_enabled: true` applied to
  ALL documents, inserting a title page even for short articles (the
  content ended up on page 2). `title_page_enabled` remains a master
  switch (disables the title page even for a book).
+
  To keep a title page on an article: add `:title-page:` to the
  document header.

=== Migration

Articles that relied on the automatic title page must add
`:title-page:` to their header to keep it (e.g. beryl's README).

== v2.3.24.92 — 2026-06-04

=== Fixed

* **`:toc!:` properly disables any table of contents.** Two causes:
+
  0. The global config (`~/.config/.../config.yml`) injected its
     attributes (`toc`, `x-title-page-toc`…) as HARD-SET, locking the
     attribute: a `:toc!:` in a document had no effect.
     `UserConfig#merge_into` now passes them as SOFT-SET (`@` suffix,
     asciidoctor convention) — the config provides DEFAULTS that the
     document header can override.
  0. `title_page_toc_enabled?` only consulted `x-title-page-toc` (TOC
     placement), never `toc` (presence). It now returns `false` as soon
     as `toc` is disabled — no TOC on the title page either.

* **Title-page subtitle wrapped.** `render_title_page_standard`
  rendered the subtitle in a single `draw_text_run` that overflowed
  off-page. It is now wrapped (`wrap_segments` + `render_segment_line`,
  like the title and the inline-mode subtitle), and the separator line
  is placed BELOW the last line of the subtitle (instead of a fixed
  offset that crossed through it).

* **Table header rendered.** Rendering tested `has_header_option`,
  which could be `false` even though the parser had populated
  `rows.head` (the `options="header"` case with a header row + an empty
  row). We now rely on `rows.head`: if a header row exists, it is
  rendered (background + bold).

=== Known (outside this shard)

* Autolinking a URL followed by a space in a cell produces a truncated
  `href` (`https://`) on the crystal-asciidoctor parser side — to be
  fixed in that shard.
* pdf-audit reports `huge_gap` inside table cells (false positives: it
  does not model columns).

== v2.3.24.91 — 2026-06-04

=== Changed

* **The code-wrap `WARNING` now cites the source LINE NUMBER.** The
  v2.3.24.90 message echoed the start of the line, which was not very
  useful. It now directly indicates the line of the `.adoc` file:
+
  ----
  asciidoctor: WARNING: ligne de code repliée : trop longue pour l'encadré (ligne 280)
  ----
+
  The author immediately locates the line and decides what to do
  (shorten it, or add a continuation proper to their language — a `\`
  in shell).

* **`sourcemap` enabled by default.** Required so that each parsed
  block carries its line number (`node.lineno`). Overridable via
  `-a sourcemap=false`. The line number of the content of a delimited
  block is `node.lineno + 1` (the line of the opening `----` delimiter
  plus one); the index of the wrapped chunk within the block is added
  to it.

== v2.3.24.90 — 2026-06-04

=== Added

* **Warning on wrapped code lines.** A `[source,…]` block line that is
  automatically wrapped (v2.3.24.88) stays on two lines on screen, but
  copy-pasting it embeds a line break — the command is then not
  executable as-is. Now double-flagged:
+
  0. **Visual marker**: a small amber "↳" hook is drawn in the left
     gutter of the box, at the start of each continuation line. Drawn
     as VECTOR (two segments + arrowhead), so (a) no dependency on the
     mono font's coverage, and (b) ABSENT from copy-paste — the reader
     gets only the code, never the marker. Color configurable via the
     theme (`code_wrap_marker_color`, amber by default).
  0. **`WARNING` at generation time**: a stderr message is emitted for
     each wrapped source line ("copy-paste will insert a line break"),
     inviting the author to shorten the line in the AsciiDoc source if
     they wish.

=== Note

`[source,…]` block lines are not — and never have been — justified:
they are rendered in fixed-pitch (mono), left-aligned, with original
spaces preserved. Only prose (including the inline codespans it
contains) is justified.

== v2.3.24.89 — 2026-06-04

=== Fixed

* **Soft-wrap of `[source,…]` blocks: priority to the space (argument
  boundary).** The v2.3.24.88 wrapping took the rightmost hinge, which
  could split a flag: `… --write --` then `hostname=chouquette` (the
  `--hostname` cut in two). We now break IN PRIORITY at the rightmost
  space that fits — each argument/flag stays whole:
+
  ....
  beryl scan aloli/9783705f-f86f-4bf7-88ad-4c92b920e9ba --provider=scaleway --dns --write
  --hostname=chouquette
  ....
+
  The hinges `/ - = ,` are now only used as a second resort, when a
  SINGLE TOKEN is itself wider than the box (UUID, path or hash without
  a space) — exactly like the soft-break of inline codespans. A hard
  cut at the limit as a last resort.

== v2.3.24.88 — 2026-06-04

=== Added

* **Soft-wrap of overly long lines in `[source,…]` blocks.** A code
  block is rendered verbatim and is not justified, but a line wider
  than the box (typically a shell command with a full UUID + flags,
  e.g. `beryl scan aloli/<uuid> --provider=scaleway --dns --write
  --hostname=…`) overflowed all the way OFF THE PAGE and got clipped —
  the end of the command became invisible.
+
  These lines are now wrapped at hinge characters (space first, then
  `/`, `-`, `=`, `,` — the same logic as the soft-break of inline
  codespans) as close as possible to the edge of the box, and continued
  on a continuation line. The whole command stays visible. No character
  is injected (no `\`, no indentation): a break on a space consumes the
  space, a break on a hinge keeps the hinge on the top line — copy-paste
  stays faithful. A line without any hinge (a single very long token)
  is cut hard as a last resort, still preferable to clipping.
+
  The box height and pagination adjust automatically (the wrapping
  happens before the height computation), so a block that gains lines
  through wrapping stays correctly boxed and can spill onto the next
  page if needed.

=== Verification

beryl README FR + EN: the `beryl scan` command of the Scaleway scenario
(UUID + 4 flags), which ran off the page (ending at 635 pt for an edge
at 595), now fits entirely within the box on two lines. 0 pdf-audit
anomaly, 261 specs green.

== v2.3.24.87 — 2026-06-04

=== Fixed

* **Justification: lines reach the right margin again.** The
  computation of `natural_w` (the natural width of a line, the basis of
  `extra_per_space = (target_w - natural_w) / n_spaces`) on the
  renderer side counted badge paddings (codespan/kbd/mark)
  UNCONDITIONALLY, the way `tokenize` does for the break decision. But
  the rendering only adds them CONDITIONALLY (not between two fragments
  of the same codespan, nor before a clinging character). `natural_w`
  was therefore OVER-estimated ⇒ `extra_per_space` too small ⇒
  justified lines stopped a few points BEFORE the margin (visual defect
  reported on many list items and paragraphs of the beryl README).
+
  The renderer now applies EXACTLY the same condition as the rendering
  (`next_clings` / `next_is_mono`) to count the paddings: `natural_w`
  then equals the width actually drawn, and the line fills exactly up to
  `target_w`. The composer keeps its inflated (unconditional) measure to
  break conservatively — guaranteeing the absence of overflow.

* **Simple admonition: text reaches the right margin.** The content
  width of an admonition removed a `padding` on the RIGHT even in the
  absence of a frame. For a simple admonition (left color bar only, the
  default case), there is no right border: the text now reaches the page
  margin like an ordinary paragraph. The right inset is kept only for
  the boxed variant (`boxed`).

=== Internal cleanup

* Removal of the `composer_natural_w` threading / `wrap_segments_full`
  method introduced in v2.3.24.86: the exact `natural_width` now being
  recomputed locally and correctly by the renderer (conditional
  paddings), the composer's value no longer needs to be passed. Back to
  a single `wrap_segments`.

=== Verification

Rendering validated with poppler (`pdftotext -bbox`, independent
reference): on the beryl README FR and EN, the justified lines reach the
right margin (559 pt) with uniform inter-word spaces (~3-5 pt). The NOTE
on page 1 now reaches the margin.

== v2.3.24.86 — 2026-06-04

=== Changed (internal refactor, rendering unchanged)

* **Removal of the `@line_natural_widths` cache indexed by
  `object_id`.** v2.3.24.85 passed the composer's exact `natural_width`
  to the renderer via a `Hash(UInt64, Float64)` cache indexed by the
  line's `object_id` — a functional but fragile compromise (address
  reuse possible after GC, memory growth over the lifetime of the
  document).
+
  The `natural_width` is now passed **explicitly** as a
  `composer_natural_w` parameter to `render_segment_line`. A new
  `wrap_segments_full` method keeps the composer's complete `Line`
  objects (segments + `natural_width`); the 3 paths that justify
  (paragraph, list item, definition list) use it. `wrap_segments`
  becomes a thin wrapper (`wrap_segments_full(...).map(&.segments)`) for
  the ~9 non-justifying call-sites (titles, markers) that do not need
  the metric.
+
  Rendering **strictly identical** to v2.3.24.85 (positions of all
  words byte-exact, verified on the beryl README: 2794 FR words /
  2776 EN, 0 anomaly). The file loses 25 net lines (-76 / +51): shorter
  and without a shared instance variable.

=== Why this refactor

The `object_id` approach had been chosen so as not to touch the
signature of `render_segment_line` (shared by 12 call-sites). Realizing
that only 3 of them actually justify (pass `target_w` + `align`), it
becomes trivial to thread the real value only where it matters, via an
optional `composer_natural_w : Float64? = nil` parameter
(backward-compatible for the other 9). This eliminates the *code smell*
without any change in rendering.

== v2.3.24.85 — 2026-06-04

=== Fixed

* **Overflow of the French `:` (`mot<NBSP>:`) past the right margin.**
  Root bug in `try_native_justified_text`: the NBSP (U+00A0) was
  converted to an ASCII space *before* the `split(' ')`, which turned
  it into a justification point. An `extra_tj` (stretch) was then
  inserted between `source` and `:` in `source<NBSP>:`, pushing the `:`
  beyond the margin.
+
  In accordance with the typographic convention (confirmed: in French
  `mot<NBSP>:<space>`, only the space AFTER the `:` is justifiable; the
  NBSP is non-breaking AND of fixed width; in English `mot:<space>`,
  same), the split is now done on the STRICT ASCII space. The NBSP stays
  IN the chunks and is converted to a space (for glyph rendering) only
  AFTER the split — so its width stays natural, with no `extra`. New
  method `split_ascii_keep_nbsp`. The simple-font branch (`Tw`) switches
  to manual positioning for the same reason (`Tw` applies to ALL byte-32
  characters, NBSP-converted included).

* **Over-stretching / overflow from a composer ↔ renderer measure
  mismatch.**
  `compose_knuth_plass` computes a per-line `natural_width` that
  INCLUDES the codespan/kbd/mark paddings — it is on this measure that
  the line breaks were decided. The renderer recomputed its own width
  via `font.string_width` WITHOUT these paddings, so it was
  underestimated: `extra_per_space = (tw - natural_w) / n_spaces` became
  too large ⇒ over-stretched lines (gaps of 20-30 pt) or words
  overflowing the margin.
+
  The renderer now consults the composer's `natural_width` (source of
  truth) via a `@line_natural_widths` cache indexed by the line's
  `object_id`, filled in `wrap_segments`. Non-destructive read (a line
  may be rendered several times during a pagination attempt). Safeguard:
  the value is only adopted if it is greater than the local computation
  and within a reasonable bound.

* **Knuth-Plass: `ratio < 0` rejected (no compression).** The shrink
  threshold goes from -1.0 to 0.0: no space compression is tolerated.
  Any overflowing line is rejected, forcing the composer to break
  earlier (the overflowing element moves to the next line, the previous
  one stretches by positive stretch). An atomic Box `mot<NBSP>:` too
  wide for the current line is thus systematically pushed to the next
  line, never compressed nor cut.

=== Impact

PDF of the beryl README (FR and EN), measured by `pdf-audit` v0.2.1:
**0** typographic **anomaly** (7 pages, 2794 FR words / 2776 EN words).
Starting point of the session: 11 anomalies.

No regression on the other examples (x-form-audit-iso27001 even goes
from 8 to 7 pre-existing anomalies, related to the `[x-form]` field
layouts).

=== Implementation note

The `@line_natural_widths` cache indexed by `object_id` is a localized
compromise (avoiding changing the signature of `render_segment_line`,
shared by 15+ call-sites). Safety rests on overwrite-at-write
(`wrap_segments` rewrites the entry just before rendering) and a
plausibility safeguard. A refactor toward a sequential identifier
remains possible if the need arises.

== v2.3.24.84 — 2026-06-04

=== Fixed

* **Knuth-Plass: `ratio < 0` rejected (no space compression).** The
  standard TeX bound `next if ratio < -1.0` allowed up to 100%
  compression of a line's Glues to make it fit within `target_w`. At
  Philippe's request, and in accordance with the French typographic
  convention which wants spaces of stable width (not tightened to gain
  room), this bound becomes `ratio < 0.0`: no space compression
  tolerated. An overflowing line is now systematically rejected by K-P
  — the composer must break earlier, the previous line is stretched by
  positive stretch, and the overflowing element (word, codespan, French
  `:` after NBSP) moves to the next line.
+
  Typical case eliminated: an atomic Box `mot<NBSP>:` (where the NBSP
  makes the `mot`+`:` joint non-breaking, only the space AFTER the `:`
  being justifiable) too wide for the current line is now pushed onto
  the next line instead of being placed in overflow.

=== Impact

On the PDF of the beryl README (measured via `pdf-audit` v0.2.1): the
typographic anomalies go from **11 to 4** (-64%).

* `--hostname=chouquette` (75 pt overflow in a `[source,console]`
  block): classified out-of-scope by the linter's `monospace?` filter —
  typographic convention: code blocks are not wrapped (a shell command
  must not be cut across lines), it is up to the document author to
  break their line in the AsciiDoc source.
* `bootstrapper.`, `réel.`, `API`, `:` (some p4 paragraphs), `l'API`
  (p6 in some paragraphs): eliminated by the K-P `ratio < 0` rejection.
* 4 residuals at 4-8 pt (`par`, `:`, `lit`, `l'API`): cases where
  `compose_first_fit` (K-P fallback when no admissible path is found)
  does not break correctly at the last Glue/Penalty before a final word.
  Converging toward 0 anomaly requires a refactor of the first-fit
  fallback (strict alignment of the composer measure with the actual
  rendering of inter-segment codespan paddings).

== v2.3.24.83 — 2026-06-04

=== Fixed

* **Compensation of badge paddings in the composer's computation.** The
  `ParagraphComposer` did not know that the rendering adds
  `codespan_padding_x` (typically 2 pt), `+4 pt` for `kbd`/`button` or
  `+2 pt` for `mark` to `current_x` after each badge (cf.
  `render_segment_line` ~ line 4031, fix J4.A v2.3.24.73).
  Consequence: lines containing codespans were counted slightly shorter
  than their actual rendering, and some words overflowed the right
  margin by 1 to 16 pt.
+
  Detected thanks to the brand-new `aloli-crystal/pdf-audit` (v0.1.0)
  which reported 42 `margin_overflow` on the PDF of the beryl README.
  The fix consists in inflating the measure of the segment's Box in the
  measurement block of `tokenize` (cf. `converter.cr#wrap_segments`) by
  the same amount as what the renderer adds downstream. Conservative:
  counts a padding even for adjacent codespans (where the compensation
  is actually nil) — an acceptable overestimation since it avoids any
  overflow.

  Verification after fix:
+
  - `pdf-audit lint beryl.pdf`: 67 → 33 anomalies
  - `margin_overflow`: 42 → 20 (the 20 remaining are final punctuation
    `:`, `→`, short words whose pdf2text measure shows ±1-3 pt of
    imprecision)
  - `stuck_words`: 6 → 0
  - Gold compare on the 53 prod-crystal READMEs: 0 project loses a page,
    `beryl-recipes` gains 1 (compaction).

== v2.3.24.82 — 2026-06-04

=== Added

* **Phase 2 — typographic quality approaches LaTeX.** Four
  correlated blocks delivered in the same session after putting
  in place a visual test suite (53 baseline gold PDFs).
  Non-regression verification: no project loses a page, 2
  (`asciiart`, `beryl-recipes`) gain one.

  **Block A — `tools/compare-pdfs-gold.sh`**: comparison
  script between 2 gold PDF folders. Compares manifest
  SHA-256s to identify changed projects, then for each
  extracts word bbox (`pdftotext -bbox-layout`) and counts
  words whose position has moved. Exits `0` if identical,
  `1` otherwise — usable in CI / pre-merge.

  **Block B — Re-enable Liang hyphenation in `first-fit`.**
  Silent regression discovered before K-P: hyphenation wasn't
  acting, `Penalty.flagged` produced by `tokenize` were
  ignored by `compose_first_fit`. The breakpoint-selection
  loop now walks `current` backwards to find the rightmost
  breakpoint — a `Penalty.flagged` (hyphenation) wins over the
  preceding `Glue` if more to the right. The hyphen is
  suffixed to the line's last segment via
  `ends_on_hyphen=true` / `hyphen_w`. `finalize_line`
  extended to accept these two parameters (symmetry with
  `finalize_kp_line`).

  Verification on French `pouvoir`: “doit pou-voir
  intervenir” — legitimate hyphenation applied.

  **Block C — Knuth-Plass on by default with emergency
  guardrail.** `wrap_segments` repointed to
  `compose_knuth_plass`. After the optimal backtrace,
  `MAX_KP_RATIO = 3.0` (= 300 % of nominal stretch): if AT
  LEAST ONE line has `|adjustment_ratio|` exceeding that
  threshold, K-P falls back to `compose_first_fit` for the
  whole paragraph (which has its own `max_extra_factor = 2.5`
  guardrail renderer-side). Without this guardrail K-P
  accepted pathological layouts (words scattered across large
  whitespace — regression observed 2026-06-04 on the beryl
  README). With it, K-P brings its global benefits (balanced
  lines, leveraging hyphenation `Penalty`, fewer *rivers*)
  without accepting extreme cases.

  Validation on beryl README:

  - “avant” and “deploy” correctly separated ✓
  - “FreeBSD 15 via mfsBSD-in-QEMU” on one line ✓
  - “bootstrapper” at end of line, no overflow ✓
  - “l'arborescence :” on line 1 ✓

  **Block D — Soft break in long inline codespans.** New
  constant `CODESPAN_HINGE_CHARS = "/-=,"` and helper
  `split_at_codespan_hinges` that fragments a codespan word
  AFTER each hinge character. `tokenize` emits
  `Penalty(width=0, cost=200, flagged=false)` between
  fragments — `compose_first_fit`/`compose_knuth_plass` can
  break a long command like `beryl scan aloli/9783...
  --provider=…` there rather than letting it overflow the
  margin. Cost `200` non-infinite: discourages unnecessary
  breaks but allows them when needed. The breakpoint
  selection loop in `compose_first_fit` now accepts ANY
  `Penalty` with `cost < INFINITY` (flagged → hyphen,
  non-flagged → clean break).

  Corollary: removed Liang hyphenation on `mono` segments.
  An identifier `beryl` must NEVER be hyphenated to
  `be-ryl` (the hyphen would change its meaning). Soft
  hinge break is the only one allowed for codespans.

  Renderer corollary: no `codespan_padding_x` between two
  adjacent `mono` segments — they form the continuation of
  the same codespan split into fragments. Without this
  exception, a 2 pt gap appeared between `aloli/` and
  `9783705f-`, rendering the codespan unreadable.

== v2.3.24.81 — 2026-06-04

=== Fixed

* **URGENT REVERT — `wrap_segments` repointed to
  `compose_first_fit`, `max_extra_factor` guardrail
  reintroduced, “avantdeploy” glued fix.** Visual regressions
  confirmed by the user on 2026-06-04 on the beryl README PDF
  after the J3/J4 commits:

  0. **“avantdeploy” glued** in the note “beryl se place
     `*avant*` `https://.../[deploy]`” — the asciidoctor HTML
     parser produces `<strong>avant</strong><a>deploy</a>`
     WITHOUT a space between tags (collapsed). My v.70 fix had
     removed the artificial inter-segment Glue to solve the
     codespan/punctuation bug, but that removal broke this
     reciprocal case.
  0. **“FreeBSD 15” line-broken**, **large justify gaps**,
     **`bootstrapper` overflowing into margin**, **long
     commands overflowing**: Knuth-Plass without
     `max_extra_factor` guardrail was accepting layouts with
     extreme stretching instead of falling back to left
     alignment for the affected line.

  **Actions**:

  - `wrap_segments` (converter.cr): calls
    `compose_first_fit(tokens, width)` (instead of
    `compose_knuth_plass`). Knuth-Plass code is **preserved**
    in `paragraph_composer.cr` for future clean
    re-introduction with emergency guardrail + visual tests
    suite.
  - `render_segment_line`: reintroduces guardrail
    `max_extra_factor = 2.5`. If extra-per-space exceeds 2.5×
    a normal space width, the line falls back to left
    alignment. Empirically calibrated in v2.3.24.68.
  - `ParagraphComposer.tokenize`: new constant
    `CLINGING_CHARS = ".,;:!?)]}»' " + NBSP` (characters that
    “stick” to the preceding word). At an inter-segment
    boundary (`idx == 0` of a new seg, last token is a Box),
    a Glue is inserted **unless** the new seg's first char is
    in `CLINGING_CHARS`. This handles:

    [horizontal]
    `*avant* deploy`:: → `avant deploy` (letter `d` → Glue)
    `(ex : ` `code` `)`:: → `(ex : code)` (`)` sticky → no Glue)
    `deploy<NBSP>: il`:: → `deploy : il` (NBSP sticky → no Glue
                          before `:`)
    `l'arborescence`:: → `l'arborescence` (`'` sticky)

  Spec updated: `paragraph_composer_spec.cr` gets 3 new cases
  (inter-segment Glue inserted for letter, not inserted for
  sticky punctuation, not inserted for NBSP) replacing the old
  “never inserted” test. 261/261 specs green.

  Visual verification on beryl README:

  - `avant` and `deploy` correctly separated.
  - `FreeBSD 15 via mfsBSD-in-QEMU` on one line.
  - No more large justify gaps (item 7 “(via QEMU) et
    installe…”).
  - `bootstrapper` on its own line, no margin overflow.
  - `l'arborescence :` on line 1 (was on line 2 “qui” before).

  **Acknowledged methodological mistake**: 259 green specs do
  not replace a visual test suite on real documents. Structural
  gap to fill in a dedicated session (gold reference PDFs +
  bbox compare). Without this infrastructure, we can't detect
  visual regressions introduced by algorithm changes.

=== Preserved

All other commits remain active:

* J2 — Liang hyphenation 85 languages + `hyph` CLI
* J4.A — Codespan BG / sticky punctuation fix (rendering)
* J4.B — Native PDF emission `Tw`/`Tj`/`TJ`
* Chantier 1 — CJK justification via `Tc` (single-line CJK)
* Chantier 2 — `hyph available` + 7d cache
* Chantier 3 — 85-language manifest (after `mn-cyrl-x-lmc`
  + `ro` exclusion)
* `hyph install --force` + HTTP redirect follow + GitHub mirror
* True multi-line CJK justification (tokenize_segment_with_cjk)

The composition engine reverts to greedy first-fit (pre-J3
state), but ALL peripheral improvements (native PDF,
hyphenation, CJK, CLI) are preserved.

== v2.3.24.80 — 2026-06-03

=== Added

* **`hyph install <lang> --force` bypasses license-undeclared
  exclusion.** Languages in `EXCLUDED_LANGS` (currently
  `mn-cyrl-x-lmc`, `ro`) can now be installed if the user
  explicitly accepts the legal risk by passing `--force`
  (or `-f`). Without `--force`, the previous refusal stays in
  place (exit `10`).

  The `--force` flag thus widens its meaning: it now signals
  “I accept all risks” — including ambiguous licensing, in
  addition to its historical role of overwriting an existing
  install.

  With `--force` enabled:

  0. Warning on `stderr` recalling the recorded reason.
  0. Download from the effective mirror (GitHub by default,
     see below).
  0. SHA-256 verification **still active** against the hash
     stored in `EXCLUDED_LANGS` — an attacker serving a
     malicious file would still be detected.

  Associated refactor: `EXCLUDED_LANGS` is no longer a plain
  `Hash(String, String)` but a `Hash(String, ExcludedEntry)`
  (new record) with `reason`, `url`, `sha256`, `size`. The
  symmetry with `ManifestEntry` enables the same integrity
  check.

* **HTTP 301/302/303/307/308 redirect-following in
  `hyph install`.** New private function
  `http_get_following_redirects` (up to 5 hops) resolving
  `Location` headers (including relative ones via
  `URI#resolve`). Crystal's stdlib `HTTP::Client.get` does
  not follow redirects automatically — and CTAN mirrors
  `mirrors.ctan.org` typically respond with 307 to a
  geographic mirror.

=== Changed

* **Effective download URL switches to the GitHub mirror by
  default.** The manifest still references the canonical
  CTAN URL (for documentation purposes), but `hyph install`
  rewrites that URL to point to the stable GitHub mirror:
  `https://raw.githubusercontent.com/hyphenation/tex-hyphen/master/hyph-utf8/tex/generic/hyph-utf8/patterns/tex/`.
  The main CTAN mirror (`mirrors.ctan.org`) now returns
  `404` on these paths (observed in this session: redirect
  to `ctan.mines-albi.fr` which returns 404). Users can
  override via env vars `HYPH_MIRROR` or `CTAN_MIRROR`, or
  via `--from` explicit.

== v2.3.24.79 — 2026-06-03

=== Added

* **True multi-line CJK justification.**
  `ParagraphComposer.tokenize` now detects segments containing
  CJK characters (≥ U+3000) and delegates to a dedicated
  `tokenize_segment_with_cjk` helper which splits EACH CJK
  character into a standalone `Box`, separated by a thin
  `Glue` (`width = 0`, `stretch = char_w × 0.5`,
  `shrink = 0`) between two consecutive CJK chars.

  These thin Glues serve two purposes:

  0. **Legal break-point** that `compose_knuth_plass` uses to
     wrap long CJK paragraphs onto multiple lines. Before this
     change, lacking break-points (CJK paragraphs have no
     ASCII space to naturally act as Glue), a CJK paragraph
     stayed unavoidably on a single line — the composer
     couldn't break it, the user saw it extending past the
     margin.
  0. **`Tc` stretch support.** Knuth-Plass stretches these
     Glues in proportion to each line's `adjustment_ratio`;
     the renderer recomputes the same value via
     `extra_per_cjk_glyph = (target_w - natural_w) /
     (n_cjk - 1)` and emits it via
     `page.text(..., char_spacing:)`.

  NO Glue between a CJK char and an adjacent Latin char: the
  Sino-Japanese convention writes `日本2026年` glued; breaking
  here would be a typographic violation.

  Composer-side corollaries:

  - `finalize_line` and `finalize_kp_line` distinguish a
    “space” Glue (`width > 0`, Latin) from a “thin” Glue
    (`width == 0`, CJK). Only space Glues contribute to
    `n_spaces` and trigger a leading space on the following
    Box — thin CJK Glues introduce no space character in the
    rendered output.
  - The measurement block in `wrap_segments` now accepts an
    isolated CJK char: it uses `font_cjk.string_width` when
    the char is ≥ U+3000 and recognized by the CJK font, else
    `font.string_width` (main Latin font). Without that fix,
    each CJK char would be measured at 0 by Noto Sans Latin
    → phantom-width tokens, underestimated `natural_w`,
    badly-broken lines.
  - The block is now captured as a named `Proc`
    (`&measure : ...`) rather than as implicit `yield`.
    Crystal forbids `yield` inside a Proc literal —
    refactor needed so `tokenize_segment_with_cjk` can
    forward the block to a sub-lambda `flush_latin`. (Bonus:
    `Array(Char)` instead of `String::Builder` for the Latin
    buffer, because `String::Builder.to_s` may only be called
    once and `flush_latin` is called multiple times per
    paragraph.)

  Renderer-side corollaries (`render_segment_line`):

  - `seg_w` computation detects CJK chars and splits by run
    to use `font_cjk` on CJK, `font` on Latin. Without it,
    `seg_w ≈ 0` for isolated CJK chars and `current_x`
    wouldn't advance between two adjacent CJK segs: all
    glyphs piled up at the same visual position ≈ 36.
  - The “non-badge” branch (word-by-word) propagates the
    same correction on the `part_w` measured between
    consecutive `draw_text_run` calls.
  - The CJK `Tc` advancement is now added to `current_x`
    AFTER the seg's draw: `current_x += n_cjk_in_seg ×
    extra_per_cjk_glyph` to follow the PDF engine which
    translates its own cursor by `glyph_w + Tc` after each
    glyph.

  Verification on a long Chinese paragraph (~50 Han
  characters + Knuth-Plass + Latin algorithm):

  - The composer correctly breaks between two CJK chars
    (transition `测` / `试`).
  - Both resulting lines are justified to the right edge
    (xMax=559.28 = target_w).
  - 259/259 specs green. Ameba clean. The J4.A fix
    preserved (“186260).” still glued).

== v2.3.24.78 — 2026-06-03

=== Removed

* **Languages with undeclared licenses removed from manifest
  (`mn-cyrl-x-lmc`, `ro`).** Manifest size goes from 87 to
  **85 languages**. The two removed entries carried
  `license: "Unknown"` because their upstream source header
  declares no free-software grant (`NONE` for `mn-cyrl-x-lmc`,
  `[None]` for `ro`). Distributing these patterns via our
  manifest would put our users in a legally ambiguous spot —
  the ALOLI convention being to distribute only
  clearly-licensed free software, we prefer to exclude them.

  New constant `EXCLUDED_LANGS : Hash(String, String)` in
  `manifest.cr` maps each excluded language code to a
  diagnostic message in English (lingua franca of free-software
  licensing discussions). The `hyph install <lang>`
  sub-command consults this table BEFORE the manifest: if the
  language is listed and the user did not pass `--from`,
  install is refused with the explanatory message and exit
  code `10` (manifest missing).

  Users can still install these languages manually after
  independently verifying the legal situation:

  [source,bash]
  ----
  asciidoctor-pdf hyph install ro \
    --from https://example.org/hyph-ro.tex \
    --sha256 <HASH_THEY_COMPUTED>
  ----

== v2.3.24.77 — 2026-06-03

=== Added

* **Hyphenation manifest extended to 87 languages.** The
  manifest `src/asciidoctor_pdf/hyphenation/manifest.cr` now
  lists 87 languages (7 embedded + 80 listable for
  `asciidoctor-pdf hyph install <lang>`) — virtually the full
  upstream hyph-utf8 set (88 files; 1 excluded:
  `grc-x-ibycus`, legacy Ibycus 8-bit encoding redirecting to
  `ibyhyph.tex`, no direct `\patterns{}`).

  Reference source: shallow clone of
  `https://github.com/hyphenation/tex-hyphen.git` (the GitHub
  API was bypassed for stability against the anonymous
  rate-limit). For each language, the manifest records the
  canonical CTAN URL, byte size, SHA-256 (mandatory check on
  install), and the license parsed from the `% licence:` YAML
  block at the source file's head.

  **Added language codes** (80, alphabetical):
  `af`, `ar`, `as`, `be`, `bg`, `bn`, `ca`, `cop`, `cs`, `cu`,
  `cy`, `da`, `de-1901`, `de-ch-1901`, `el-monoton`,
  `el-polyton`, `en-gb`, `eo`, `et`, `eu`, `fa`, `fi`,
  `fi-x-school`, `fur`, `ga`, `gl`, `grc`, `gu`, `he`, `hi`,
  `hr`, `hsb`, `hu`, `hy`, `ia`, `id`, `is`, `ka`, `kk`,
  `kmr`, `kn`, `la`, `la-x-classic`, `la-x-liturgic`, `lt`,
  `lv`, `mk`, `ml`, `mn-cyrl`, `mn-cyrl-x-lmc`, `mr`,
  `mul-ethi`, `nb`, `nn`, `no`, `oc`, `or`, `pa`, `pi`, `pl`,
  `pms`, `rm`, `ro`, `ru`, `sa`, `sh-cyrl`, `sh-latn`, `sk`,
  `sl`, `sq`, `sr-cyrl`, `sv`, `ta`, `te`, `th`, `tk`, `tr`,
  `uk`, `vi`, `zh-latn-pinyin`.

  **Sizes**: min 595 B (`ar`), max 529 279 B (`hu`),
  median ~11 583 B. No file exceeds 1 MB (install sanity-check
  ceiling).

  **Licenses** (count per category): MIT (46), LPPL (20),
  GPL (5), FSF-unlimited (3), Permissive (2), LGPL (2),
  Unknown (2 — `mn-cyrl-x-lmc` and `ro` whose source headers
  declare no license; to audit before any strictly-free
  distribution), plus singletons (BSD-3-Clause,
  BSD-3-Clause-like (`bg`), Knuth-permissive, LPPL-1.3-or-MIT,
  MPL, Public-Domain, Unlicense).

  User-visible effect: `asciidoctor-pdf hyph available` now
  lists all 87 languages with local status (`embarqué` /
  `installé` / `absent`), and `hyph install <lang>` accepts
  any of the 87 codes without an additional `--from` /
  `--sha256` parameter (expected SHA-256 is read from the
  manifest).

  259/259 specs green. Ameba clean.

== v2.3.24.76 — 2026-06-03

=== Added

* **`asciidoctor-pdf hyph available` sub-command.** Completes
  the `hyph` suite (which already ships `list`, `install`,
  `remove`, `update`, `show`, `help`). Queries CTAN — more
  precisely the GitHub API of `hyphenation/tex-hyphen` — to
  list ALL upstream Liang patterns, then cross-checks against
  the embedded manifest and the user's local state.

  Three flags (each with a short alias, ALOLI convention):

  - `-r` / `--refresh-cache`: force a re-download
  - `-j` / `--json`: structured JSON output instead of the
    table
  - `-o` / `--offline`: explicit fallback to the local
    manifest, no HTTP request

  Default table output with columns:

  - **langue** (language): short code (`fr`, `ja`, etc.)
  - **source CTAN**: `✓` if found upstream, `—` otherwise
  - **manifest**: `✓` if in the embedded manifest, `✗`
    otherwise
  - **local**: `embarqué`, `installé`, or `absent`

  Disk cache at
  `${XDG_CACHE_HOME:-~/.cache}/asciidoctor-pdf/hyph-available.json`
  with a 7-day TTL (aligned with `doc/CLI_HYPH_SPEC.adoc`).
  Source URL configurable via `HYPH_MIRROR` or `CTAN_MIRROR`;
  default is the GitHub API (JSON listing); naive HTML regex
  fallback for classic CTAN mirrors.

  Network error: stderr warning + fallback to the embedded
  manifest + exit `0` (CI-usable). 16 unit + integration
  specs via `Process.run` against the binary (flag parsing,
  cache reads, fallback, output formats).

== v2.3.24.75 — 2026-06-03

=== Added

* **CJK justification via `Tc` (“両端揃え / 两端对齐”).**
  Pure Chinese / Japanese / Korean paragraphs — those
  containing no ASCII space — are now justified to the right
  edge by stretching the space between each glyph via the
  native PDF `Tc` operator (Character Spacing,
  ISO 32000-1 § 9.3.2), per the canonical Sino-Japanese
  typographic convention.

  Detection: the line contains no ASCII space (`n_spaces ==
  0`), `target_w` is set, `font_cjk` is loaded, and at least 2
  CJK glyphs are recognized by `font_cjk.has_glyph?`.
  Justification stays active even when the received `align` is
  `left` (case of single-line CJK paragraphs, hence “last
  paragraph line” from the composer's view — explicit
  exception to the “last line left-aligned” rule, to honor the
  CJK convention).

  Computation: `extra_per_cjk_glyph = (target_w - natural_w)
  / (n_cjk - 1)`. Guardrail at 75 % of a typical CJK glyph
  width (`中`) to prevent pathologies (2 glyphs alone on a
  very wide line). 75 % is more permissive than the 50 % of a
  first POC — the CJK convention routinely accepts stretches
  up to ~100 %, and the cap was blocking legitimate short
  Japanese lines.

  Concurrent fix to `natural_w` computation: the main font
  (Noto Sans Latin) doesn't contain CJK glyphs and returned
  0 / ε for them, underestimating `natural_w` and skewing all
  mixed-justify computations. We now delegate to
  `text_with_emoji_segments` for per-font segmentation, then
  sum widths with the right font (`font_cjk` for `:cjk` runs,
  `font` for `:text` runs).

  `draw_text_run` accepts an optional `cjk_char_spacing`
  parameter (default `0.0`) which propagates to
  `page.text(..., char_spacing:)` in the `:cjk` branch. The
  Crystal-side cursor advancement compensates the PDF
  engine's accumulated `Tc`: `cursor += string_width + (n - 1)
  × cjk_char_spacing` so the cursor points to the VISUAL end
  of the last glyph (the `Tc` added by PDF after the last
  glyph is invisible — automatically reset by the `pdf` shard
  at the end of `page.text`).

  Visual verification (`/tmp/cjk-test.adoc`):

  - Before: Chinese line xMax=476 (far from right edge 559),
    Japanese line xMax=386.
  - After: Chinese line xMax=559.28 (= right edge `target_w`),
    Japanese line xMax=559.28 (= right edge). Perfectly
    justified.

  Acknowledged limitation: the current
  `ParagraphComposer.tokenize` treats each CJK string as a
  single Box (split on ASCII space only), forcing long CJK
  paragraphs to a single line (composer can't break). True
  multi-line CJK justification would require tokenize-level
  per-glyph splitting with soft Glues between each — major
  refactor planned if real need arises. Current `Tc` rail
  covers most practical cases (a short paragraph in Chinese
  or Japanese).

== v2.3.24.74 — 2026-06-03

=== Added

* **J4.B: native PDF emission for justified text — `TJ` for
  composite fonts, `Tw` for simple fonts.** Bumped
  `aloli-crystal/pdf` dependency from `~> 0.6.4` to `~> 1.2`,
  which now exposes `page.text(word_spacing:, char_spacing:)`
  (`Tw` + `Tc` operators, ISO 32000-1 §§ 9.3.3 and 9.3.2) and
  `page.text_positioned(runs)` (`TJ` operator, § 9.4.3) with
  automatic font-type detection via `page.composite_font?`.

  Implementation in `render_segment_line`:

  0. For each non-badge segment with `extra_per_space > 0`:
     try the native path via the new helper
     `try_native_justified_text`.
  0. If the segment is natively renderable (no country flag,
     no emoji, no CJK run — checked by `native_renderable?`):

     - **Simple font (Type1, AFM)**: `page.text(content,
       word_spacing: extra)` emits native `Tw`. The PDF engine
       applies the extra to every `0x20` byte.
     - **Composite font (TrueType, CID — our default with Noto
       Sans)**: `Tw` does not apply (ISO 32000-1 § 9.3.3:
       “word spacing shall not apply to occurrences of the
       byte value 32 in multiple-byte codes”). Instead we emit
       `TJ` with explicit between-word offsets:
       `[word1, " ", -extra_tj, word2, " ", -extra_tj, word3] TJ`.
       The negative delta in text space units translates the
       cursor rightward (inverse PDF convention).
  0. If the segment contains an inline-mixed element (flag,
     emoji, CJK run): fall back to manual word-by-word
     advancement via `draw_text_run` — behavior preserved.

  Quantitative verification on the test PDF
  (`/tmp/codespan-test.adoc`): 58 `TJ` operators now emitted
  (was 0), 30 % `Tj` operators saved (~200 → 139). Visual
  rendering is strictly pixel-identical — the optimization
  happens at the PDF content stream level, not glyph placement.

  Typographic bonus for CJK: the `TJ` path is the
  infrastructure needed for upcoming CJK justification (`Tc`
  character-spacing stretching all glyphs rather than spaces,
  the canonical “中文两端对齐” convention).

=== Documentation correction

* **Rectified the v2.3.24.73 note about native `Tw`.** The
  previous changelog claimed the `aloli-crystal/pdf` shard
  didn't support `Tw`. That was a false negative on my side —
  a grep against `lib/pdf/` (installation frozen at `0.6.4`)
  missed the new methods added in `1.1.0` and `1.2.0`. The
  dependency bump above restores ground truth: `Tw` / `Tc` /
  `TJ` are all three available in `pdf 1.2.0`, with automatic
  font-type detection to emit the right operator.

== v2.3.24.73 — 2026-06-03

=== Fixed

* **J4: residual visual gap between codespan and clinging
  punctuation.** Symptom: `(ex : `186260`).` visibly rendered
  as `(ex : `186260` ).` with a ~1-2 pt space before the
  closing parenthesis. Same for ``prepare_rescue`,`` which
  rendered as ``prepare_rescue` ,``.

  Cause: the “badge” branch of `render_segment_line` (mark /
  codespan / kbd / button segments) drew the text in one shot
  then added `extra_per_space * count(' ')` to `current_x`.
  The next segment (punctuation) therefore started
  `extra_per_space` points too far right.

  Fix: when the segment carries a leading space prefix (Glue
  rendered by the composer) AND we're in justify mode, the
  leading space is now stretched **BEFORE** the draw — we
  advance `current_x` by `prefix_w + extra_per_space`, then
  draw `seg.text[1..]` (without the prefix). At the end of
  the draw, `current_x` matches exactly the visual end of the
  badge text; the following punctuation glues naturally. The
  background rectangle (`badge_x`) is also offset by
  `extra_for_leading` to stay aligned with the actually-drawn
  text.

  Verification: `pdftotext -bbox-layout` now extracts
  `186260).` as a single word (before: `186260` and `).`
  separated with 4.72 pt gap).

=== Documented

* **J4.B (native PDF `Tw`) deferred.** Ideal would have been
  relying on PDF's `Tw` operator (Word Spacing,
  ISO 32000-1 § 9.3.3), which applies the extra to *all*
  ASCII spaces of a `Tj` string at the PDF engine level — no
  position calculation Crystal-side. But the `pdf` shard
  (aloli-crystal/pdf) doesn't expose it currently: it does
  word-by-word drawing with a `word_spacing` property on its
  Fragments (cf. `lib/pdf/src/pdf/text/formatted/box.cr:310`).
  Adding native `Tw` would require extending the `pdf` gem —
  separate work. The J4.A fix above achieves the same visual
  result via manual cursor advancement.

== v2.3.24.72 — 2026-06-03

=== Changed

* **J3: Knuth-Plass becomes the default composition engine.**
  We stay on the 4th digit for now (`24.71` → `24.72`) —
  alignment with the Ruby asciidoctor-pdf 2.3.24 gem
  maintained, pending an explicit decision on whether this
  shard — which is progressively diverging from the gem's
  behavior (native Knuth-Plass, Liang hyphenation, custom
  CLI, etc.) — should switch to autonomous versioning (3rd
  digit bump or even a new SemVer scheme).

  `ParagraphComposer.compose_knuth_plass` implements the
  Knuth & Plass algorithm (*Breaking Paragraphs into Lines*,
  Software Practice & Experience vol. 11, p. 1119-1184, 1981),
  the historical TeX algorithm:

  0. Identify legal breakpoints (each `Glue` after a `Box`,
     each non-infinite `Penalty`, end of paragraph).
  0. For each candidate breakpoint B, dynamic programming over
     all preceding breakpoints A: compute the `badness` of the
     A→B line (`100 · |adjustment_ratio|³`, TeX formula), the
     `demerits` (`(1 + badness)² + penalty² +
     double_hyphen_demerits`), memoize the optimal path.
  0. Backtrace to reconstruct the optimal sequence.
  0. Graceful fallback on first-fit if no admissible path
     (pathological paragraph: word too long, target_w too small).

  Visible benefits: balanced lines (similar `adjustment_ratio`
  across the paragraph), exploitation of J2 hyphenation
  Penalties (K-P prefers hyphenation over extreme stretching),
  fewer rivers, double-hyphen penalty (10000 demerits) to
  avoid two consecutive hyphen-ended lines.

* **Removed the `max_extra_factor = 2.5` guard** in
  `render_segment_line`. This heuristic silently fell back to
  `left` alignment when first-fit's `extra_per_space` exceeded
  2.5× a normal space's width — a band-aid on first-fit's bad
  behavior of maxing out each line without looking ahead.
  With Knuth-Plass, stretching stays reasonable by construction
  (K-P minimizes badness² globally), and the user always gets
  the justification their theme requests. Rare pathological
  paragraphs (single overlong word on a wide page) now produce
  visible-but-correct stretching rather than a silent
  fallback to `left` that broke document appearance.

== v2.3.24.71 — 2026-06-03

=== Added

* **J2: `Hyphenation` module (Liang algorithm).** New
  implementation of Liang's pattern-based hyphenation
  (Franklin Mark Liang, *Word Hy-phen-a-tion by Com-pu-ter*,
  Stanford thesis, 1983) — the historical TeX algorithm.
  Three sub-modules: `Trie` (Liang pattern storage),
  `Hyphenator` (main algorithm with `LEFT_MIN = 2`,
  `RIGHT_MIN = 3` defaults, exceptions support), `Loader`
  (cascade project / XDG / embedded, class-level cache,
  `en` → `en-us` and `de` → `de-1996` aliases).

* **7 Liang patterns embedded at build via `read_file`**:
  FR (~ 30 KB), EN-US (~ 33 KB), DE-1996 (~ 268 KB),
  ES (~ 42 KB), IT (~ 5 KB), PT (~ 5 KB), NL (~ 86 KB).
  Total: ~ 470 KB embedded in the binary. Sources: CTAN
  (hyph-utf8 package). Licenses vary per file (MIT /
  LPPL-1.3 / Knuth-permissive / BSD-3-Clause) — see
  `src/asciidoctor_pdf/hyphenation/manifest.cr`.

* **Manifest of known CTAN languages**: URL, expected SHA-256,
  size, license, `embedded` flag per language. 7 entries
  initially (the embedded ones); to be extended toward the
  ~80 hyph-utf8 languages in a later iteration.

* **Integration in `ParagraphComposer.tokenize`**: composer
  takes an optional `hyphenator` parameter. Each word whose
  alphabetic core is at least `LEFT_MIN + RIGHT_MIN` letters
  long is fragmented into sub-Boxes connected by `Penalty`
  (cost 50, flagged, hyphen width). Head/tail punctuation
  stays glued to first/last syllable. In `compose_first_fit`
  (J1) Penalties are ignored (rendering identical to
  monolithic word) — value comes at J3 with Knuth-Plass.

* **Document language detection** in `Converter`: AsciiDoc
  `:lang:` attribute. Stored in `@document_lang`, used by
  `wrap_segments` to resolve the hyphenator via
  `Hyphenation::Loader.for(@document_lang)`. No implicit
  default: if `:lang:` is not set, no hyphenation (graceful
  composition).

* **CLI `hyph` sub-command**
  (`bin/crystal-asciidoctor-pdf hyph <sub>`). 7 sub-commands
  matching `doc/CLI_HYPH_SPEC.adoc` and ALOLI conventions:
  `list`, `available`, `install`, `remove`, `update`, `show`,
  `help`. Numbered exit codes (0 / 2 / 3-10) for scripting.
  `tlmgr` inspiration: no automatic runtime download, always
  explicit action, mandatory SHA-256 verification.

=== Tests

* 17 new specs: 7 on `Hyphenation::Trie/Hyphenator/Loader`,
  5 on `ParagraphComposer.tokenize` × hyphenator integration.
  Total: 237 / 237 green.

== v2.3.24.70 — 2026-06-03

=== Fixed

* **Codespan background bled over the leading space.** When the
  `ParagraphComposer` renders a codespan (or kbd / button / mark)
  preceded by a justification Glue, the segment carries a leading
  ASCII space prefix (` code` rather than `code`). The badge's
  background rectangle covered that prefix, making the gray
  background overflow to the left of the first real character.
  The rectangle is now offset by a `leading_space_w` computed in
  the current font: the background starts at the first character
  and ends at the last, the leading space stays white and plays
  its visual separator role.

* **Phantom space inserted between `` `code` `` and clinging
  punctuation (`)`, `.`, `,`, `;`, `:`, `!`, `?`, `]`, `}`, `»`).**
  Two causes in cascade:
+
  0. `ParagraphComposer#tokenize` was injecting an artificial
     Glue between two Boxes from contiguous segments
     (“bug-for-bug compatible” with the old `wrap_segments`).
     That branch is removed: asciidoctor's HTML already contains
     the necessary spaces in segment texts, we mustn't fabricate
     any. Segments without an inter-frontier space are now
     rendered contiguously (`<strong>foo</strong>bar` → `foobar`).
  0. `render_segment_line` was adding `codespan_padding_x` (and
     kbd / button / mark equivalents) to `current_x` after each
     badge to prevent the next word from overlapping the badge's
     background. That padding is now conditional: it's skipped
     when the next segment starts with a space (separation
     already provided) or with clinging punctuation. Before:
     `` `code` ). `` (visible). After: `` `code`). ``.

  Dedicated spec in `paragraph_composer_spec`: new case
  “preserves the `)` glued after a codespan” validating the
  expected token sequence for `(ex : ` `` `code` `` `)`.

  Note: a residual gap of ~1-2 pt may persist on heavily
  justified lines (the first-fit `extra_per_space` is applied
  to the codespan's leading space, shifting the following
  punctuation). Will be addressed by J3 (Knuth-Plass) which
  removes this heuristic in favor of optimal global distribution.

== v2.3.24.69 — 2026-06-03

=== Fixed

* **French NBSP wiped out before reaching the wrapper.** The
  whitespace-normalization regex in `InlineRenderer.parse`
  (`gsub(/\s+/, " ")`) treated U+00A0 (NBSP) as ordinary
  whitespace (PCRE2 in Unicode mode classifies NBSP under `\s`).
  Result: every NBSP freshly inserted by
  `apply_french_typography` (before `:`, `;`, `!`, `?`, `»`) was
  immediately replaced with an ASCII space one step later, and
  `wrap_segments` would happily break the line on it. The
  Imprimerie nationale rule wasn't honored — `deploy : il`
  could be split into `deploy` on one line and `: il` on the
  next.

  The regex is now `gsub(/[^\S\x{00A0}]+/, " ")` — “all
  whitespace EXCEPT NBSP”. NBSPs are now preserved through the
  pipeline and reach `wrap_segments` (which already only split
  on strict ASCII space).

=== Added

* **Structured paragraph composition engine
  (`ParagraphComposer`).** Refactor of `wrap_segments` which
  now delegates to a new module
  `src/asciidoctor_pdf/paragraph_composer.cr` modelling the
  text stream as TeX-style tokens: `Box` (a word or NBSP-bound
  group, fixed non-breakable width), `Glue` (elastic space,
  legal break-point), `Penalty` (forced/forbidden/conditional
  break). The `compose_first_fit` algorithm faithfully
  reproduces the historical greedy behavior — 217/217 specs
  pass. Immediate benefit: NBSP non-breakability is guaranteed
  by construction (NBSPs are absorbed into the neighboring Box
  and can no longer be break-points).

  Reference: Knuth & Plass, *Breaking Paragraphs into Lines*,
  Software Practice & Experience, vol. 11, p. 1119-1184, 1981.

  This structure is the typed foundation for the upcoming
  milestones: J2 (Liang hyphenation patterns for FR/EN/DE/ES/
  IT/PT/NL embedded + `hyph` CLI sub-command for additional
  languages), J3 (Knuth-Plass algorithm — global paragraph
  optimization minimizing badness), J4 (PDF output via the
  native `Tw` operator from ISO 32000-1 § 9.3.3).

=== Documentation

* **`doc/CLI_HYPH_SPEC.adoc`**: specification of the upcoming
  `hyph` CLI sub-command (7 sub-commands: `list`, `available`,
  `install`, `remove`, `update`, `show`, `help`). `tlmgr` (TeX
  Live Manager) inspiration — no automatic runtime download,
  mandatory SHA-256 verification against an embedded manifest,
  resolution cascade project → XDG → embedded. Implementation
  later, independent of the composer chantier.

== v2.3.24.68 — 2026-06-02

=== Fixed

* **Admonition (NOTE/TIP/…) : first line shifted right & not
  justified.** When `crystal-asciidoctor` renders an admonition
  body, it wraps the inline HTML in
  `<div class="paragraph">\n<p>…</p>\n</div>`. The `\n` between
  `<div>` and `<p>` was kept by `InlineRenderer.parse_html` as a
  whitespace-only initial segment, which then bled into
  `wrap_segments` as a leading space — visually shifting the
  first word (e.g. `beryl`) by ~one space-width vs. the
  subsequent lines. `parse_html` now drops a whitespace-only
  buffer when it is **the very first segment** (no impact on
  inter-word spacing later in the stream).

* **Admonition body not justified.** `convert_admonition` was
  calling `render_segment_line` without `target_w` / `align`,
  so the justify pipeline (`max_extra_factor`,
  per-space distribution) never fired. It now passes
  `content_w` and `@theme.base_text_align`, with the last line
  forced to `"left"` (matches `render_inline_lines`).

* **Justify garde-fou too strict on narrow content widths.**
  `max_extra_factor` raised from `1.5` to `2.5` × normal
  space width. The 1.5 ceiling blocked legitimate justification
  on lines with few spaces or long words (e.g.
  `un serveur bare-metal…`, candidate 5.16 pt > limit 4.77 pt).
  The 2.5 ceiling still catches the pathological cases (single
  isolated word, broken layout) without rejecting real paragraphs.

== v2.3.24.67 — 2026-06-02

=== Changed

* `crystal-asciidoctor` dependency bumped to **`~> 2.0.26.8`** :
  fix of the `HardLineBreakRx` regex that captured only one hard
  break per paragraph (Crystal interprets the `m` flag as
  « `.` matches `\n` too » → `(.*)` greedy across multiple lines).
  All trailing `+` line breaks within a paragraph are now properly
  converted to `<br>`.

== v2.3.24.66 — 2026-06-02

=== Changed

* `crystal-asciidoctor` dependency bumped to **`~> 2.0.26.7`** —
  removes the `0.`-repeated hack for ordered lists. The canonical
  AsciiDoc convention (`.` repeated for auto-numbering, or manual
  numbering `1.` `2.` `3.`) becomes the only path. See ALOLI memo
  `feedback_asciidoc_ordered_lists.md`.

== v2.3.24.65 — 2026-06-02

=== Added

* **Inline code rendering (codespan).** New `codespan_*` theme
  category ported from the `asciidoctor-pdf` Ruby spec 2.3.24 :
  grey background + optional border + text colour + configurable
  padding behind `` `text` `` inline segments. Before this port,
  inline code was rendered in mono font but without any visual
  framing — `_default.yml` looked like ordinary text.

* **French typography (`x-` extension).** New theme property
  `x_french_typography` (`true` by default in the `fr` theme,
  `false` elsewhere). When enabled, the converter replaces the
  regular space before `:`, `;`, `!`, `?`, `»` and after `«`
  with a non-breaking space U+00A0 — follows the Imprimerie
  nationale typography rules and avoids line breaks splitting
  two-part punctuation.
+
Code blocks (`[source,...]`, ` ```), literal blocks and
attributes (style, language) are **preserved** since they do
not go through the inline parser pipeline.

* **Inline markup preserved in list items.** Previously
  `render_list` passed each item's text through `strip_inline_markup`
  + raw `draw_text_run` — result : every italic, bold, inline code,
  link, etc. was *stripped* and rendered as flat text. The full
  pipeline (`InlineRenderer.parse` + `wrap_segments` +
  `render_segment_line`) is now used, identical to paragraphs.
  Justify also applies to long items (non-terminal lines).
+
Exposed theme properties (defaults match upstream asciidoctor-pdf) :
+
[cols="2,2,3"]
|===
| Property | Default | Effect

| `codespan_background_color`
| `f5f5f5`
| Very light grey background behind the code

| `codespan_border_color`
| _empty_
| Border disabled by default

| `codespan_border_width`
| `0.0`
| Same

| `codespan_font_color`
| _empty_
| Inherits the current text colour

| `codespan_padding_x`, `codespan_padding_y`
| `2.0`, `1.0`
| Space around the text (points)
|===

* **Justified paragraphs by default** (`base_text_align: "justify"`).
  Rendering distributes the unused space on a line between
  inter-word spaces ; the last line of a paragraph stays
  left-aligned so an orphan line isn't stretched. Configurable
  through the `base_text_align` theme property (values: `"left"`,
  `"center"`, `"right"`, `"justify"`). Conforms to upstream
  asciidoctor-pdf where `base_align: justify` is the default.
+
Algorithm : for every line except the last, compute the natural
width of segments, count internal spaces, then distribute
`(target − natural) / n_spaces` after each segment proportionally
to how many spaces it contains. Simple approximation (spaces
*between* segments are stretched, those internal to a single
segment stay fixed) — sufficient for typical paragraphs with light
markup (italic, inline code, etc.).

=== Changed

* `crystal-asciidoctor` dependency bumped to **`~> 2.0.26.6`** —
  hybrid `[source,LANG]` + `\`\`\`` blocks (form accepted by
  Asciidoctor Ruby in Markdown compatibility mode) now properly
  carry the `language` attribute. As a result : syntax highlighting
  + grey background + `language-LANG` class are applied just like
  for the canonical `[source,LANG] ----` form.

== v2.3.24.64 — 2026-06-02

=== Changed

* **CJK fonts now render natively.** Bumped the `pdf` dependency to
  `~> 0.6.4`, which adds native OpenType/CFF (`.otf`) subsetting and
  embedding. All Noto CJK fonts ship as `.otf`, so until now they
  triggered `UnsupportedFontFormat` and CJK characters fell back to
  `?`. With pdf 0.6.4 a cached Noto CJK font is loaded lazily on the
  first CJK character and embedded as a `CIDFontType0` subset — the
  ideographs render and the text is extractable.
* The lazy `font_cjk` accessor comments were updated : the
  `rescue UnsupportedFontFormat` is now a safety net for a genuinely
  corrupt / exotic font, no longer the expected path for CJK.

=== Fixed

* Integration spec `non_winansi_chars_spec` no longer hard-codes the
  obsolete "CJK → `?`" expectation. It now asserts the behaviour
  conditional on a Noto CJK font being present in the cache (rendered,
  no `?`) or absent (`?` fallback). The integration text-extraction
  helper's Identity-H caveat is documented (it reads raw GIDs, not
  ToUnicode, so it cannot reproduce exact CJK text — only the
  presence / absence of the `?` substitution).

== v2.3.24.63 — 2026-06-02

=== Fixed

* **List-item attached blocks now render correctly.** When a list
  item carried an attached block via `+` (typically a source
  block `[source,console] ----`, an admonition, a table, another
  paragraph), `render_list` only iterated on *sub-lists* and
  silently dropped every other child block type.
+
Result : in "1. Step with its command / 2. Next step with its
command" recipes (very common in aloli-crystal READMEs), only
the step labels appeared in the PDF — the commands vanished.
+
Fix : all child blocks of list items are now routed through the
general dispatcher (`dispatch(sub_block, sub_block.context.to_s)`)
which calls the right `convert_*` based on type.

== v2.3.24.62 — 2026-06-01

=== Changed

* `crystal-asciidoctor` dependency bumped to **`~> 2.0.26.5`** —
  now accepts the ALOLI `0.`-repeated convention for auto-numbered
  ordered lists :
+
[source,asciidoc]
----
0. First item
0. Second item
0. Third item
----
+
Renders as "1. First item", "2. Second item", "3. Third item".
The author can reorder items without renumbering each line, and
the `0.` marker stays visible in the editor (unlike the canonical
`.` repeated form, which is more discreet).

== v2.3.24.61 — 2026-06-01

=== Changed

* `crystal-asciidoctor` dependency bumped to **`~> 2.0.26.4`** —
  parser warnings on ordered lists now include the source line
  number where they were detected :
+
[source]
----
asciidoctor: WARNING: list item index: expected 2, got 1 (line 6)
asciidoctor: WARNING: list item index: expected 3, got 1 (line 7)
----
+
Previously these warnings were anonymous and the author had to
hunt manually through the document for the offending list —
painful on long documents (READMEs, audit manuals, etc.).

== v2.3.24.60 — 2026-06-01

=== Fixed

* **Truly lazy CJK font loading.** In v2.3.24.59 the `font_cjk`
  accessor was lazy, but `text_with_emoji_segments` called it on
  the very first text run of the document — which triggered the
  `UnsupportedFontFormat` warning (`.otf` CFF unsupported) for
  *every* document, including those with no CJK character at all.
* Now `font_cjk` is only consulted for *plausibly CJK* characters
  (`ord >= 0x3000` — start of the CJK Symbols and Punctuation
  block). Below that we are on Latin or European typographic
  punctuation (curly apostrophe `'`, em dash `—`, ellipsis `…`,
  etc.) which the CJK font would not cover anyway — no point
  loading it just to find that out. As a result, European
  CJK-free documents no longer emit the warning.
* The warning is still emitted **once** when an actual CJK
  character (Japanese, Chinese, Korean, ...) is encountered in
  the document.

== v2.3.24.59 — 2026-06-01

=== Added

* **`signature` field type** in `[x-form]` — now fully functional
  on the PDF side. Emits a valid AcroForm `/Sig` widget with
  `/SigFlags 3` on the root form. The slot is clickable in PDF
  viewers (Acrobat, Preview, Foxit). The digital signature itself
  (PKCS#7, PAdES) remains the responsibility of
  `aloli-crystal/pdf-signature` (in progress).
* **ISO 27001 audit demo** : `examples/x-form-audit-iso27001.adoc`
  + its interactive PDF rendering (`.pdf`). Covers text, email,
  date, radio (Hash options), textarea, checkbox, select,
  select-multi types, plus multi-column layout on the summary
  section.

=== Changed

* `pdf` dependency bumped to **`~> 0.5.9`** (brings AcroForm
  `signature_field` + Dropdown/RadioGroup custom appearances +
  OutputIntent ICCBased — J1 ISO PDF roadmap).
* **Lazy CJK font loading** : `noto-cjk` is now loaded only on the
  first CJK character encountered in the document. Benefits : zero
  cost for CJK-free documents (99 % of ALOLI use cases), avoids
  the immediate `UnsupportedFontFormat` raise for `.otf` files
  (CFF not yet subsettable — see memo `roadmap_pdf_cff_subsetting.md`).

== v2.3.24.58 — 2026-06-01

=== Added

* **`[x-form]` extension** — interactive PDF forms (AcroForm,
  ISO 32000-1 §12.7) described in YAML inside an AsciiDoc literal
  block. Built for the ALOLI use cases : ISO 27001 audits, offline
  Quiméo quizzes, RGPD / consent forms.
* Full grammar documented in `doc/x-form-spec.adoc` :
** 10 field types : `text`, `email`, `url`, `tel`, `password`,
   `textarea`, `number`, `date`, `checkbox`, `radio`, `select`,
   `select-multi` (the `signature` type is *rejected* in v1,
   pending `pdf-signature`).
** Layout : `fields:` flat *OR* `sections:` titled (mutually
   exclusive).
** Options : simple list `[A, B, C]` or Hash `{code: label}`
   (native YAML Hash, order preserved, /Opt as `[export, display]`
   pairs).
** *Multi-column layout* : `columns: N` at form or section level,
   `cols: M` (span) at field level, auto-flow left → right.
** Atomic validation : regex, min/max range (`required_if` and
   `visible_if` postponed to v2 — require PDF JavaScript).
** Buttons : `submit:` (single), `reset:`, custom `buttons:`
   (rejected in v1 — pushbutton support needs pdf v0.6+).
* Implementation : modules `AsciidoctorPDF::FormBuilder` (YAML
  parser + validation), `AsciidoctorPDF::FormRenderer` (layout
  helpers), integrated in `convert_listing` (detects
  `attributes["style"]? == "x-form"`).
* Coverage : 92 specs added (66 FormBuilder + 13 FormRenderer +
  13 end-to-end PDF with /AcroForm validated).

=== Changed

* Dependency `pdf` pinned to **`~> 0.5.7`** (provides AcroForm +
  clean rejection of OpenType/CFF fonts, prerequisite for
  `[x-form]`).
* CJK font (`noto-cjk`) now tolerates OpenType/CFF (`.otf`)
  fonts that aren't yet subsettable : a warning is emitted and
  `@font_cjk` stays nil — the converter falls back to `?` for CJK
  characters. See ALOLI memo `roadmap_pdf_cff_subsetting.md` (CFF
  subsetting planned).

== v2.3.24.57 — 2026-05-08

=== Added

* **User configuration (XDG Base Directory)** : the CLI now reads
  `${XDG_CONFIG_HOME:-~/.config}/crystal-asciidoctor-pdf/config.yml`
  to set persistent preferences once for all documents:
** `theme:` — default theme (builtin name, file in `themes/`, or
   absolute path)
** `attributes:` — AsciiDoc attributes injected by default
   (`title-page-toc`, `toc`, `pdf-page-size`, ...)
** `author:`, `email:`, `organization:` — default author metadata
* Documented precedence cascade: CLI flags > document attributes >
  user config > compiled defaults. The document always wins over
  the user config.
* New `-N` / `--no-user-config` flag to reproduce the vanilla
  behaviour (CI, debug).
* `themes/` subdirectory inside the XDG directory: allows
  referencing a user theme by name (e.g. `theme: aloli` →
  `~/.config/crystal-asciidoctor-pdf/themes/aloli.yml`).
* `data/config.example.yml` : ready-to-copy template.
* _User configuration_ section in the README.
* XDG convention to generalise to all other aloli-crystal shards
  that store user-level configuration (notably crystal-beryl).

== v2.3.24.56 — 2026-05-08

=== Fixed

* **Invisible bold-italic text (bump `pdf` ~> 0.5.5).** The `pdf`
  shard v0.5.5 fixes an `F<n>` key collision between Type1 and
  TrueType fonts on the same page: Type1 text added after a TTF was
  invisible because Acrobat interpreted its key as the TTF and tried
  to decode its WinAnsi bytes as TTF glyphs.
+
  Typical symptom: a `*_mécanique_*` word (bold-italic) disappeared
  from the rendered PDF, because the DejaVu Sans theme does not
  include `BoldOblique` and the `Helvetica-BoldOblique` (Type1)
  fallback collided with DejaVu (TTF) already active on the page.
+
  Bump: `pdf` ~> 0.5.4 → ~> 0.5.5.

== v2.3.24.55 — 2026-05-07

=== Changed

* **`VERSION` read at compile-time from `shard.yml`.** Previously the
  `VERSION` constant was hardcoded to `"2.3.24.54"` while `shard.yml`
  was already at `2.3.24.54` — a silent desync. It is now read via
  the `read_file` macro directly from `shard.yml`, making any desync
  impossible. Pattern generalized to the `aloli-crystal/*` ecosystem.

== v2.3.24.54 — 2026-05-01

=== Fixed

* **`pass:[<br>]` honored in the subtitle.** The subtitle was
  rendered in one block via a direct `draw_text_run`, without going
  through `InlineRenderer.parse` — so `pass:[<br>]` came out
  literally as `<br>` instead of breaking the line.
+
  The subtitle is now handled like the title: segments parsed via
  `InlineRenderer`, wrapping via `wrap_segments` (which honors
  `line_break` segments), rendered via `render_segment_line` line by
  line with consistent alignment (centering if requested).
+
  The theme color `subtitle_font_color` is still applied: a new
  private helper `force_color(seg, color)` injects the theme color
  into every segment that does not define one explicitly, otherwise
  `render_segment_line` would fall back to `base_font_color`.
+
  Typical usage:
+
  ----
  :subtitle: Marie Dupont pass:[<br>]01/05/2026 12:25
  ----

== v2.3.24.53 — 2026-05-01

=== Fixed

* **Consistent alignment of the subtitle and the byline.** In
  `:title-page: false` mode (`render_inline_doctitle`), when
  `title_page_align: center` (or `right`) is set, the `:subtitle:`
  and the byline `:author: · :revdate:` now follow the same
  alignment as the title. Previously they stayed strictly
  left-aligned, producing a visual dissonance (centered title ↔
  left-aligned satellites).
+
  Generic consistency: "when the title is centered, its satellites
  follow". Not specific to any use case.
+
  NOTE: v2.3.24.51 also contained a smart-fold of the subtitle on a
  dash, reverted in v2.3.24.52 to stay out of business logic — the
  subtitle splitting strategy belongs to the calling application.

== v2.3.24.52 — 2026-05-01

=== Changed

* **Revert of v2.3.24.51** ("smart subtitle + consistent
  alignment"). The smart-fold of the subtitle on a dash is removed:
  the subtitle splitting strategy belongs to the calling
  application, not the shard. The consistent subtitle alignment is
  reintroduced cleanly in v2.3.24.53.

== v2.3.24.51 — 2026-05-01

=== Added

* **Smart subtitle + consistent alignment** (`:title-page: false`
  mode, `render_inline_doctitle`).
+
  - **Subtitle aligned like the title.** If `title_page_align:
    center` is set, the `:subtitle:` (and the byline `:author: ·
    :revdate:`) follow the same alignment — previously strictly
    left-aligned.
  - **Smart fold of the subtitle on a dash.** When the subtitle
    exceeds `@content_width` AND contains a dash separator surrounded
    by spaces (` — ` em-dash, ` – ` en-dash), we try to break there
    in priority rather than at the next space. If the break yields
    two halves each fitting on 1 line → 2 balanced lines; otherwise →
    standard word wrap. Typical case:
    `:subtitle: Marie Dupont — 01/05/2026 10:50` becomes
    `Marie Dupont` + `01/05/2026 10:50` instead of cutting between
    first and last name or in the middle of the date.
+
  Private helper `smart_subtitle_lines` extracted to be tested
  independently.
+
  NOTE: fully reverted in v2.3.24.52.

== v2.3.24.50 — 2026-04-30

=== Added

* **Centerable H1 title and `<br>` line break respected.** Two
  related features on doctitle rendering.
+
  0. **Line break in the title.** `crystal-asciidoctor` already
     produces `<br>` when an AsciiDoc `+\n` or a `pass:[<br>]`
     appears in a title, but our `InlineRenderer` treated `<br>` as a
     plain space — the break was invisible. New flag
     `InlineSegment.line_break : Bool` marking an empty segment as a
     "forced break point"; the HTML parser emits it on `<br>`;
     `wrap_segments` closes the current line regardless of how full
     it is. Syntax: `= First part pass:[<br>]Second part`. A plain
     `+\n` at the end of a title line does NOT work (the title is
     single-line in source): prefer `pass:[<br>]`.
  0. **H1 title centering.** New theme property `title_page_align`
     (`left` / `center` / `right`, default `left`), overridable per
     document via `:title-page-align:`. Applied to all three
     rendering modes: standard title page
     (`render_title_page_standard`), title page + integrated TOC
     (`render_title_page_with_toc`) and inline doctitle
     (`render_inline_doctitle`). Private helpers
     `resolve_title_align`, `title_line_x` and `force_bold` (clone
     segments to bold before wrapping so the measure uses
     Helvetica-Bold metrics, otherwise the title overflows).
  0. **Footer / header: strip HTML tags.** `{document_title}`
     injected into `footer_left` etc. carried the `<br>` literally.
     `resolve_page_vars` now strips inline HTML tags and collapses
     spaces before substitution.

== v2.3.24.49 — 2026-04-30

=== Fixed

* **`:pdf-theme:` resolved on the API side, not only the CLI.** An
  AsciiDoc source containing `:pdf-theme: fr` had its attribute
  ignored as soon as it was converted via the direct API (apps that
  instantiate `Converter.new("pdf", theme)` without going through the
  CLI) — resolution was implemented only in `src/cli.cr`. Symptom: a
  `[.x-score-a-revoir]` label came out "NEEDS REVIEW" (English)
  instead of "À REVOIR" (the expected French).
+
  Resolution now happens at the `convert_document` level, applied to
  both usages (CLI and API):
+
  - If the caller passes an explicit theme (`@theme_provided =
    true`), its choice is respected and the document attribute
    ignored (full backward compatibility).
  - Otherwise (`Converter.new("pdf")` or `Converter.new("pdf",
    nil)`), `convert_document` reads `node.attr("pdf-theme")` and
    calls `ThemeLoader.resolve(...)`; fonts and page size are
    reapplied.
+
  The user has nothing to change in their Crystal code:
  `:pdf-theme: fr` in the source is enough (label verified
  `À REVOIR` / `ATTENTION`).

== v2.3.24.48 — 2026-04-30

=== Added

* **Boxed mode (`boxed`) with drop shadow.** Reinforced emphasis for
  admonitions and `x-score` blocks: full rectangular frame, discreet
  drop shadow, label integrated into the frame. Activable via two
  cumulative options (logical OR):
+
  - **A — per block**: AsciiDoc `boxed` role on the node, e.g.
    `[CAUTION,role=boxed]` or `[.x-score-excellent.boxed]`.
  - **B — per theme**: YAML properties `admonition_boxed: true` /
    `x_score_boxed: true` — all concerned blocks become boxed without
    touching the source.
+
  12 theme properties added (6 admonitions + 6 x-score):
  `<kind>_boxed`, `<kind>_box_background_color`,
  `<kind>_box_border_color`, `<kind>_box_border_width`,
  `<kind>_box_shadow_enabled`, `<kind>_box_shadow_color`,
  `<kind>_box_shadow_offset`. New private function
  `render_box_chrome` (shadow + background + border in that order),
  shared between admonitions and x-score.
+
  Limitation: no transparency on the shadow (the `pdf` shard does not
  expose `/CA` in a public API); a pale gray color is used instead.

== v2.3.24.47 — 2026-04-30

=== Added

* **Index: page ranges (`12-15`) instead of flat lists.** The index
  grouped all the pages of a term via `pages.map(&.to_s).join(", ")`,
  giving hard-to-read entries like "12, 13, 14, 15, 17, 20, 21".
  Contiguous sequences are now compressed into ranges: "12-15, 17,
  20-21". Format matching the upstream Ruby asciidoctor-pdf behavior
  (`index_pagenum_sequence_style: range`, default).
+
  New private function `format_page_ranges` that iterates over the
  sorted list and emits a lone number outside a sequence, a range as
  soon as 2+ pages are contiguous. Unit tests: `[12,13,14,15]` →
  "12-15", `[12,13,15,16,17]` → "12-13, 15-17", `[5]` → "5",
  `[1,3,5,7]` → "1, 3, 5, 7", `[1,2,5,6,7,9,10]` → "1-2, 5-7, 9-10".

== v2.3.24.46 — 2026-04-30

=== Added

* **`[abstract]` block presented distinctly.** The `[abstract]` block
  was rendered as a generic paragraph, with no visual distinction for
  what is typically a document's summary or lead-in. It is now detected
  in `convert_paragraph` via `node.style == "abstract"` and styled:
+
  * indented (24pt left and right, shorter lines);
  * italic (forced on segments that are not explicitly italic, without
    flipping those already italic);
  * color `#555555` (discreet gray) rather than `base_font_color`;
  * slightly larger font size (+1pt).
+
  Inline markup (bold / italic / mono / links) remains preserved in the
  abstract. Closes the "Abstract block" item of the ROADMAP.

== v2.3.24.45 — 2026-04-30

=== Added

* **`[%collapsible]` blocks flagged with ▾ in the title.** The
  `[%collapsible]` block has no semantics in PDF (no interactivity),
  but ignoring its intent would be a waste. Now, when an example block
  carries the `collapsible` option, its title is prefixed with the ▾
  glyph to visually signal the "expandable" nature on the HTML side —
  purely informational in PDF.
+
  No structural change, just a title prefix, and no external library
  added: the visual rendering stays consistent with other examples.
  Closes the "Collapsible blocks" item of the ROADMAP.

== v2.3.24.44 — 2026-04-30

=== Added

* **Placeholders for `audio` / `video` / `stem` / `pass`.** The four
  dispatchers silently returned `""`: any `audio:[]`, `video:[]`,
  `stem:[]` or passthrough block vanished from the PDF without a word.
  Now:
+
  * **`audio:[url]`**: an "▶ Audio" banner with the source shown; if
    the source is http(s), a clickable `/URI` annotation is laid down
    to open the URL;
  * **`video:[url]`**: same with the "▶ Vidéo" label;
  * **`stem::[expr]`**: an "∑ Math" banner with the source expression
    in monospace (no actual math rendering — that would require
    embedding a MathJax / katex subset, deferred);
  * **`pass::[html]`**: the raw HTML is stripped via
    `Sanitizer.sanitize` then rendered as a normal paragraph.
+
  Uniform rendering (pale gray background + border + colored label)
  through a shared utility function `render_media_placeholder`, with no
  external library. Closes the "Audio / video / stem / pass" item of
  the ROADMAP.

== v2.3.24.43 — 2026-04-30

=== Added

* **`TextTransformer` module (uppercase, smallcaps, capitalize).** Port
  of upstream `text_transformer.rb` (Ruby asciidoctor-pdf 2.3.24).
  Three transformations exposed:
+
  * `TextTransformer.uppercase(s)`: everything uppercased;
  * `TextTransformer.capitalize_words(s)`: capital on the first letter
    of each word (`"hello world"` → `"Hello World"`);
  * `TextTransformer.smallcaps(s)`: small caps via Unicode small-caps
    characters (`f` → `ғ` and `q` → `ǫ`, better supported by Helvetica
    / DejaVu than `ꜰ` / `ꞯ`).
+
  A dispatch function `TextTransformer.apply(s, kind)` routes by name
  (`"uppercase"` / `"smallcaps"` / `"capitalize"` / other or `nil` =
  identity). Two theme properties added: `heading_text_transform` (all
  headings h0-h6, numbered sections included) and
  `title_page_text_transform` (title-page title and inline doctitle).
  Closes the "Text transformer" item of the ROADMAP.

== v2.3.24.42 — 2026-04-30

=== Added

* **Internal `Sanitizer` module (`sanitizer.rb` parity).** 1:1 port of
  upstream `sanitizer.rb` (Ruby asciidoctor-pdf 2.3.24), an internal
  utility module for the text → PDF pipeline:
+
  * `Sanitizer.sanitize(s)`: strips XML/HTML tags, resolves entities
    (named + numeric decimal + hex), collapses whitespace. The
    converter's naive `gsub(/<[^>]+>/, "")` is replaced by this call
    (entity coverage raised from ~10 to ~14 named + numeric);
  * `Sanitizer.escape_xml` / `unescape_xml`: encode / decode the
    standard XML entities (`<`, `>`, `&`, `"`, `'`);
  * `Sanitizer.escape_amp`: encodes lone `&` without touching
    already-valid entities;
  * `Sanitizer.encode_quotes`: `"` → `&quot;` for injection into an
    HTML attribute.
+
  WARNING: this is NOT a security sanitizer (no protection against XSS
  / CSS injection / hostile HTML). The dedicated security shard is
  documented in `CRYSTAL-SANITIZER-HTML-SPECS.adoc`. Closes the
  "Dedicated sanitizer" item of the ROADMAP.

== v2.3.24.41 — 2026-04-29

=== Added

* **Open block — `[.x-box]` role.** The AsciiDoc open block
  (`-- ... --`) is neutral by design: it groups without typesetting. To
  make it framed without hijacking `[sidebar]` (`****`) or `[example]`
  (`====`), which have their own semantics, the `[.x-box]` role is
  added, turning the open block into a framed panel (background, border,
  padding, optional title):
+
  ----
  [.x-box]
  .Optional title
  --
  Framed content, with *bold*, _italic_, lists, tables… markup.
  --
  ----
+
  Implementation: routes to `render_panel(node, :sidebar)` — same
  visual rendering as a sidebar, without the "ancillary content"
  semantics. The `x-` prefix is consistent with the shard's
  "non-standard extension" convention (cf. `x-score-*`,
  `x-title-page-toc`). Last item of the "secondary gaps" sprint: all
  the relevant ROADMAP items are closed.

== v2.3.24.40 — 2026-04-29

=== Added

* **Description list — `qanda` and `horizontal` styles.**
  `convert_dlist` only handled the default mode (bold term, definition
  below). Two additional styles, often seen in documentation, are added
  at parity with Ruby asciidoctor-pdf:
+
  * **`qanda`**: FAQ style. Each term becomes a numbered question
    ("Q1.", "Q2."…) in bold, and the sub-blocks become the answers
    indented under the number;
  * **`horizontal`**: term on the left in a fixed column (~25 % of the
    content width), definition on the right; for multi-line
    definitions, the term stays aligned on the first line.
+
  Refactor: `convert_dlist` split into three private functions
  (`render_dlist_default`, `render_dlist_qanda`,
  `render_dlist_horizontal`), the dispatch reading `node.style`.
  `horizontal` limitation: sub-blocks other than paragraphs (listings,
  nested tables) are flattened into inline text.

== v2.3.24.39 — 2026-04-29

=== Added

* **Document-level `pdf-*` attributes (page-size, page-layout).** The
  converter respected `theme.page_size` / `theme.page_layout` but
  ignored the eponymous document attributes. The `:pdf-page-size:` and
  `:pdf-page-layout:` attributes read in `convert_document` now
  override the theme defaults for the current document (Ruby
  asciidoctor-pdf parity):
+
  ----
  = My poster
  :pdf-page-size: A3
  :pdf-page-layout: landscape
  ----
+
  Supported sizes (`PAGE_SIZES` table, case-insensitive): A0, A1, A2,
  A3, A4, A5, A6, B5, Letter, Legal, Tabloid. Size + orientation
  resolution is extracted into `apply_page_size(size, layout)`, called
  at converter construction then re-called at the start of
  `convert_document` when an attribute overrides. Verified: MediaBox
  `[0 0 1190 841]` for A3 landscape.

== v2.3.24.38 — 2026-04-29

=== Fixed

* **`quote` / `verse` distinguished (markup + pre-wrap + citetitle).**
  `convert_quote` and `convert_verse` shared `render_indented_block`,
  which stripped inline markup and did not distinguish the two
  line-break modes: `*bold*` / `_italic_` disappeared and a poem
  (verse) lost its visual structure. Refactor:
+
  * **inline markup preserved**: `InlineRenderer.parse` +
    `wrap_segments` + `render_segment_line` (same pattern as admonitions
    / x-score). Italic is forced on segments that are not explicitly
    italic, without flipping already-italic text;
  * **pre-wrap for `verse`**: with `kind: :verse`, each `\n` of the
    source becomes a line break in the PDF (wrap only applies to lines
    that overflow `content_w`), whereas `kind: :quote` keeps the
    continuous flow;
  * **citetitle**: `[quote, Author, Title]` now renders
    `— Author, Title` as attribution (before: author only).

== v2.3.24.37 — 2026-04-29

=== Fixed

* **Callouts + colist (code annotations).** `convert_inline_callout`
  and `convert_colist` returned `""`: the classic "code + numbered
  list of explanations" teaching pattern did not work, the `<1>`, `<2>`
  annotations disappeared. Implementations:
+
  * **inline_callout**: produces `<b class="conum">①</b>` (fallback
    `(N)` beyond 20), via the Unicode enclosed digits U+2460..U+2473,
    with no icon dependency;
  * **colist**: iterates the items and renders each as "bold ① marker
    on the left + indented text on the right", inline markup preserved
    (`InlineRenderer.parse` + `render_segment_line`).
+
  Known limitation (deferred): inside a `[source]` block, the code's
  `<1>`, `<2>` appear as plain text (substitution in the code =
  integration into the crystal-rouge syntax highlighter pipeline, too
  invasive here). The post-listing colist remains fully functional.

== v2.3.24.36 — 2026-04-29

=== Added

* **Extended PDF metadata: `keywords` and `creator`.** The converter
  only set `title`, `author`, `subject` and `producer`. Added the two
  missing entries for parity with Ruby asciidoctor-pdf and better
  indexing by search engines / DAMs:
+
  - `:keywords:` on the AsciiDoc source side — a comma-separated list of
    keywords (`/Keywords` in the PDF).
  - `:creator:` on the source side — the name of the application that
    produced the content, distinct from `producer` (which remains the
    PDF engine, `crystal-asciidoctor-pdf X.Y.Z`).
+
  Verified: `strings` on the output PDF shows `/Keywords` and `/Creator`
  correctly set, encoded in UTF-16BE for accented characters.

=== Note

Two metadata items still await an extension of the `pdf` shard:
`:lang:` (`/Lang` of the Catalog, not yet exposed by `PDF::Document`)
and customizable dates (`:revdate:` → `/CreationDate`, for now fixed at
generation time).

== v2.3.24.35 — 2026-04-29

=== Added

* **Page break: `[%recto]` and `[%verso]` options.** The plain `<<<`
  always created a new page (which already covers `[%always]`, accepted
  for Ruby parity without changing behavior). Added the two options
  useful for printable documents:
+
  - `[%recto]`: aligns the next content page onto an odd page (recto,
    right-hand page). If the break lands on an even page, an extra page
    is generated — the intermediate one stays empty. Typical use:
    starting each chapter on a recto.
  - `[%verso]`: symmetric, forces the next page onto an even page
    (left-hand page of an open book).
+
  Aligned with Ruby asciidoctor-pdf 2.3.24
  (`page-break-after-recto` / `-verso`).

== v2.3.24.34 — 2026-04-29

=== Added

* **Tables: `colspan`, `valign` and factoring of `render_table_row`.**
  First salvo of the ROADMAP's "7 secondary gaps" sprint.
+
  - `colspan`: the cell occupies N columns from the current position;
    its effective width sums the consecutive `col_widths`. Bounded to
    the number of remaining columns to avoid overflow.
  - `valign`: `top` (default) / `middle` / `bottom` — vertical
    positioning of the text block within the row per the spec.
  - Factoring: the three sections (head / body / foot) each had their own
    copy-pasted render loop; refactored into a shared `render_table_row`
    — fewer lines, consistent behavior.
+
  Known limitations: `rowspan` is read but not materialized visually
  (the cell occupies a single row), and `autofit` (auto widths from
  content) is not implemented — only explicit `colpcwidth` or the
  default uniform distribution.

=== Dependency

* `crystal-asciidoctor` ~> 2.0.26.2 (parser fix on `CellSpecEndRx` to
  recognize multi-line specs `3+|`).

== v2.3.24.33 — 2026-04-29

=== Added

* **Recto-verso header / footer.** The shard only had a single set of
  header/footer templates; added Ruby asciidoctor-pdf's recto-verso
  convention: overriding the three zones (left/center/right) according
  to the page parity.
+
  12 new theme properties (all empty by default, so no change for
  existing documents): `header_recto_left/center/right`,
  `header_verso_left/center/right` and their `footer_*` equivalents.
+
  For each page, `header_templates_for` / `footer_templates_for`
  selects the recto variant (odd number) or verso variant (even
  number); if it is non-empty, it overrides the neutral default.

=== Note

Closes the last 🟠 of the ROADMAP (parity with "section_info_by_page":
the `{section_title}` variable was already populated, only the
recto/verso routing was missing). All priority items (🔴 + 🟠) of the
sprint are closed.

== v2.3.24.32 — 2026-04-29

=== Added

* **Chapter break `heading_chapter_break_before`.** Theme property
  (default `0`): when `> 0`, triggers an automatic page break before any
  heading of level ≤ value. "Book" mode: set to `1` so that an H1 always
  starts on a new page. The rule skips already-blank pages so as not to
  create two blank pages.

* **Roman numbering of the front matter
  (`:pdf-front-matter-numbering: roman`).** Distinct numbering of the
  front-matter pages (title page + TOC) in roman numerals "i, ii, iii…"
  then the body in arabic "1, 2, 3…", each counter restarting from 1.
+
  - `PageMeta` becomes a class (instead of a record) with a mutable
    `displayed_number` field and three `numbering` styles: `:none` (no
    footer, e.g. title page), `:roman`, `:arabic`.
  - New `assign_displayed_numbers` function walking `@page_metas` after
    rendering to assign the right number per style.
  - New module `AsciidoctorPDF::RomanNumeral` (port of
    `roman_numeral.rb`) for the integer → "i, ii, iii…" conversion.
  - `resolve_page_vars` translates `{page_number}` via
    `format_page_number(meta)`; a `{page_number_pdf}` variable is
    exposed for the raw PDF index. `render_toc` points to the logical
    number ("1") rather than the PDF index ("3"). `render_title_page`
    marks the title page `numbering: :none` so that arabic starts at 1.

* `h0_font_size` and `heading_chapter_break_before` are also exposed in
  the YAML loader (parsed as `Float64` / `Int32`).

== v2.3.24.31 — 2026-04-29

=== Added

* **Visual framing of sidebar and example blocks.** `convert_sidebar`
  and `convert_example` iterated their sub-blocks without any visual
  rendering — these blocks were invisible (plain paragraphs in the
  flow). Closes a 🔴 of the ROADMAP.
+
  - New private method `render_panel(node, kind)` shared by sidebar and
    example: draws background + border ahead of time (based on
    `estimate_block_height`) then the content on top, so the text isn't
    masked.
  - Optional title (`.My title`) rendered in bold above the content.
  - Internal indentation via temporary `@margin` / `@content_width`,
    restored after iteration so as not to pollute the global state.
  - Page-break pre-flight if the block fits on a full page but not in
    the remaining space (parity with `[%unbreakable]`).
  - 16 new theme properties: `sidebar_background_color`,
    `sidebar_border_color/width`, `sidebar_padding`,
    `sidebar_margin_top/bottom`, `sidebar_title_font_color/size` and
    their `example_*` equivalents.
+
  Known limitation: for a block whose real height noticeably exceeds the
  estimate, the background may be a bit too short — acceptable for short
  sidebars/examples.

== v2.3.24.30 — 2026-04-29

=== Added

* **Inline images and icons (`image:path[]`, `icon:name[]`).**
  `convert_inline_image` returned `""` — any `image:logo.png[]` or
  `icon:warning[]` macro was silently erased. Closes a 🔴 of the
  ROADMAP.
+
  - `InlineSegment` gains `image_path`, `image_width`, `image_height`;
    an image segment is treated as atomic at wrap (no split per word)
    and at render time.
  - `render_segment_line` routes to `render_inline_image`, which
    dispatches on the extension: `.svg` → `page.svg` (viewBox, ratio
    preserved), otherwise → `page.image` (`PDF::Images::Image.load`). On
    error (file not found, SVG parse), fallback to the alt-text — no
    crash.
  - `InlineRenderer.parse_html` recognizes self-closing tags
    `<img src="…" width="…" height="…" alt="…"/>`.
  - The wrap respects the image width (no horizontal overflow).
+
  `icon:name[]`: text fallback `[<name>]` for now (a real embedded icon
  library is reserved for a future iteration). Limitation: an image
  taller than the line overflows onto the next one; for full-width
  photos, keep the standalone `image::…[]` macro.

== v2.3.24.29 — 2026-04-29

=== Added

* **Extended inline quoted: `sub`, `sup`, `mark`, `kbd`, `btn`,
  `menu`.** `convert_inline_quoted`, `convert_inline_button`,
  `convert_inline_kbd` and `convert_inline_menu` returned `""` — all
  technical formatting (`H~2~O`, `E=mc^2^`, `#mark#`, `kbd:[Ctrl+C]`,
  `btn:[OK]`, `menu:…` paths) disappeared at conversion. Closes a 🔴 of
  the ROADMAP.
+
  - Dispatchers added to the converter, producing the Ruby-parity inline
    HTML: `<sub>`, `<sup>`, `<mark>`, `<kbd>`, `<span class="keyseq">`,
    `<b class="button">`, `<span class="menuseq">`, `<b
    class="menu|menuitem|caret">`.
  - `InlineSegment` gains 6 flags (`sup`, `sub`, `mark`, `kbd`,
    `button`, `menu`), each with its own typographic rendering: sup/sub
    in offset 75% font, `mark` on a pastel yellow background (`#fff59d`),
    `kbd` in a pale gray bordered box (`#cccccc`) at 90%, `button` in a
    solid gray box (`#e0e0e0`), `menu` inheriting the bold.
  - `InlineRenderer.parse_html` extended: recognizes `<sub>`, `<sup>`,
    `<mark>`, `<kbd>` and distinguishes the `<b class="…">` (button /
    menu / generic bold). Additional HTML entities decoded: `&#8249;`,
    `&#8250;`, `&hellip;`, `&laquo;`, `&raquo;`, `&mdash;`, `&ndash;`.

* **Latent inline-wrap fix.** `render_inline_lines` re-created
  `InlineSegment` during the wrap but only propagated
  bold/italic/mono/color/link — losing sup/sub/mark/etc. Refactored to
  reuse `wrap_segments`, which clones faithfully.

== v2.3.24.28 — 2026-04-29

=== Fixed

* **Inline markup preserved in admonitions and x-score blocks.**
  `convert_admonition` and `render_x_score_block` called
  `strip_inline_markup`, which erased the inline HTML generated by
  crystal-asciidoctor (bold, italic, mono, color, link):
  `*Quizz non validé*` rendered as plain text.
+
  - New private function `wrap_segments(segments, width, font_size)`
    that splits a stream of `InlineSegment` into lines according to
    width, preserving style attributes (where `wrap_text` works on plain
    text).
  - `convert_admonition` now parses `node.content` via
    `InlineRenderer.parse`, wraps via `wrap_segments`, renders via
    `render_segment_line` line by line.
  - `render_x_score_block` gets the same treatment (same visual
    structure, same fix).
+
  Result: `*bold*`, `_italic_`, `` `mono` `` render correctly in
  admonitions and score blocks.

== v2.3.24.27 — 2026-04-29

=== Changed

* **`x-score-*` labels in English by default.** The default theme now
  follows the "English default, overrides via embedded theme" convention
  of the rest of the shard (NOTE / TIP / WARNING / CAUTION / IMPORTANT).
  Before, the `x-score-*` labels were mixed (EXCELLENT in English but
  TRÈS BIEN, INSUFFISANT, À REVOIR in French) — the default theme served
  French that wasn't requested.
+
  Mapping: `excellent` → EXCELLENT (unchanged), `tres-bien` → VERY GOOD,
  `bien` → GOOD, `insuffisant` → INSUFFICIENT, `a-revoir` → NEEDS
  REVIEW. The embedded `fr` theme (`themes/fr.yml`) overrides these five
  labels with their French equivalents.

=== Note

The **level identifiers** in the AsciiDoc role stay in French
(`[.x-score-tres-bien]`, etc.): they are technical identifiers carved
into the syntax, left unchanged so as not to break existing sources.

== v2.3.24.26 — 2026-04-29

=== Added (built-in `fr` theme + `[.x-score-*]` score blocks)

Two related features: a French theme shipped with the shard, and five
reusable colored score levels for quiz grading scales.

* **Built-in themes via `ThemeLoader.builtin(name)`.** The YAML is inlined
  into the binary at compile time (`{{ read_file(...) }}`) — no runtime file
  dependency. First theme shipped: `fr` (aliases `francais`, `french`).
+
Three selection channels, in decreasing priority CLI > document attribute >
built-in default:
+
- CLI: `--theme fr` (also accepts a YAML path, via `ThemeLoader.resolve`).
- AsciiDoc source: `:pdf-theme: fr` in the header.
- Crystal API: `ThemeLoader.builtin("fr")`.
+
The `fr` theme translates the admonitions (NOTE / ASTUCE / AVERTISSEMENT /
ATTENTION / IMPORTANT), the `toc_title` (« Table des matières ») and the
`index_title` (« Index »).

* **Score blocks `[.x-score-<level>]`** (extension): five predefined
  qualitative levels for quiz grading scales and evaluation sheets, enabled
  via the AsciiDoc role on an example (`====`) or open (`--`) block:
+
- `excellent` (dark green, EXCELLENT)
- `tres-bien` (light green, TRÈS BIEN)
- `bien` (yellow, BIEN)
- `insuffisant` (orange, INSUFFISANT)
- `a-revoir` (red, À REVOIR)
+
Colors and labels are configurable through the YAML theme
(`x_score_<level>_color/label`). Rendered like admonitions: colored vertical
band + bold label + indented text. The `x-` prefix follows the shard's
"non-standard extension" convention, with no equivalent in AsciiDoc / Ruby
asciidoctor-pdf.

== v2.3.24.25 — 2026-04-29

=== Added (admonition label translation, Ruby parity)

* **Admonition labels (NOTE / TIP / WARNING / CAUTION / IMPORTANT) are now
  resolved through a cascade** instead of being hard-coded as `name.upcase`,
  allowing translation without patching the code. Resolution order:
+
0. AsciiDoc attribute `:<name>-caption:` (when it differs from the title-case
   default injected by crystal-asciidoctor).
0. Theme property `admonition_<name>_label`.
0. Ultimate fallback: `name.upcase`.

* Five new theme properties with English defaults for Ruby asciidoctor-pdf
  parity: `admonition_note_label`, `admonition_tip_label`,
  `admonition_warning_label`, `admonition_caution_label`,
  `admonition_important_label`.
+
To translate a quiz into French, override via the attributes `:note-caption:`,
`:tip-caption: ASTUCE`, `:warning-caption: AVERTISSEMENT`,
`:caution-caption: ATTENTION`, `:important-caption: IMPORTANT`.

* Implementation note: crystal-asciidoctor injects document-level title-case
  defaults (`Warning`, `Caution`, …) for HTML rendering. The cascade detects
  and ignores them, otherwise the theme would be short-circuited in every doc
  without a user override.

== v2.3.24.24 — 2026-04-29

=== Added (documentation: recipes and attribute conventions)

* The `README.adoc` now explains the three document templates produced:
+
- Quiz / short sheet with a compact title page (logo + title + integrated
  contents on a single page).
- Short document with no title page — header banner with logo, title,
  author · date byline.
- Long document with title page + separate TOC (default).

* Also documents the `:title-logo-image:` format (`image::PATH[opts]`, with
  `align`, `pdfwidth`, `alt` options), the pitfall of the `image::PATH[]`
  macro placed before `= Doctitle` which demotes the title to a level-0
  section ("part"), and the `x-` convention for non-standard attributes /
  properties (including the `:x-title-page-toc:` extension).

== v2.3.24.23 — 2026-04-29

=== Changed (`x-` prefix for extensions, full inline doctitle)

* **`x-` prefix (non-standard extension).** Renamed to signal the feature's
  non-standard nature (convention inherited from `X-*` in HTTP/MIME):
  `:title-page-toc:` → `:x-title-page-toc:` and `theme.title_page_with_toc`
  → `theme.x_title_page_with_toc`. The README documents the convention: any
  `x-`-prefixed attribute is a shard-specific extension with no native
  equivalent in AsciiDoc / Ruby asciidoctor-pdf.

* **`heading_font_size(0)` returned `h6_font_size` (10pt).** When the doctitle
  ended up demoted to a level-0 section (AsciiDoc "part", typical of a source
  where an `image::` precedes the `=`), `heading_font_size` fell into the
  `else` branch and applied the smallest size, hence tiny titles. Fix: new
  `h0_font_size: 26.0` property and level-0 handling in `heading_font_size`,
  `heading_margin_top` and `heading_margin_bottom`.

* **Full `render_inline_doctitle`.** When `:title-page: false` disables the
  title page, the doctitle is now rendered at full size (28pt, like the
  standard cover title) and preceded by the `:title-logo-image:` logo if set
  (centered logo, 28pt bold title, author · revdate byline in gray, separator,
  content). Preferable to the `image::...[]` hack placed before the `=`, which
  demotes the doctitle to a level-0 section.

== v2.3.24.22 — 2026-04-29

=== Added (`[%unbreakable]` option on open blocks)

* **Prevents a group from being split by a page break** (e.g. text orphaned
  from its table). Standard AsciiDoc syntax `[%unbreakable]` on an open block
  (`-- ... --`).
+
Implementation in `convert_open`:
+
0. When `node.option?("unbreakable")` is true, the block's cumulative height
   is estimated before rendering via `estimate_block_height` (paragraphs,
   tables, lists, listings, admonitions, images, nested opens).
0. If remaining space is insufficient but the block fits on a full page,
   `new_page` before starting.
0. If the block exceeds a full page, fall back to normal pagination ("if
   possible" from the standard).
+
The estimate is deliberately pessimistic. Current limitation: only *open
blocks* carry the option; not yet supported on sidebar / example / quote.

== v2.3.24.21 — 2026-04-29

=== Added (`:title-page: false` + `:title-page-toc:` docs)

Two complementary additions to 2.3.24.20.

* **Title page disabling** (Ruby asciidoctor-pdf parity). The shard ignored
  the `:title-page:` attribute until now. Henceforth:
+
- `:title-page:` ⇒ title page forced.
- `:title-page: false` ⇒ no title page (functional equivalent of the standard
  `:!title-page:`, undetectable on the crystal-asciidoctor side since the
  negation leaves no trace in `attr?` / `attr`).
- absent ⇒ theme default (`title_page_enabled`, `true` by default, unchanged
  behavior).

* **Doctitle at the top of page 1** when there is no title page (standard
  AsciiDoc `article` mode): the doctitle becomes an H1 at the top of the first
  content page, followed by an "author · date" byline in gray, a separator,
  then the preamble. Implemented via `render_inline_doctitle`.

* The README documents the title page and marks `:title-page-toc:` as a
  crystal-asciidoctor-pdf-specific extension, with no native equivalent.

== v2.3.24.20 — 2026-04-29

=== Added (SVG, `:title-logo-image:`, `:title-page-toc:`)

Three additions related to title-page layout and vector images.

* **Routing `.svg` images to `page.svg`** (`convert_image`).
  `PDF::Images::Image.load` only reads bitmaps (PNG/JPG); for `.svg` files,
  the text content is read and passed to `page.svg(...)` (the pdf shard's
  native SVG renderer, which turns SVG elements into PDF primitives). The
  aspect ratio is preserved even when only `width` is specified.

* **`:title-logo-image:`** (Ruby asciidoctor-pdf 2.3.24 parity).
  `render_title_page` never read this attribute, hence the missing cover logo
  compared to upstream. Format accepted identically:
  `:title-logo-image: image::path/logo.svg[align=center,pdfwidth=200]`. The
  logo is placed at the top of the cover, without affecting the title position
  (centered-low).

* **`:title-page-toc:` / `theme.title_page_with_toc`.** New title-page mode:
  title at the top (not centered), separator, integrated contents (title +
  entries with leaders and page numbers), author + date at the bottom.
  Enabled per-document via the attribute or by default via the theme property.
  `render_title_page` routes between `render_title_page_standard` (existing)
  and `render_title_page_with_toc` (new); in cover-TOC mode no separate TOC
  page is allocated — the TOC is drawn in post-processing from the memorized
  Y. `render_toc` gains two optional parameters: `y_start` (starting Y) and
  `render_title : Bool` (lets the caller draw the "Contents" title itself).

== v2.3.24.19 — 2026-04-29

=== Added (title page without header / footer / footnotes)

* **Typographic convention: the title page is "bare".** No header line, no
  page number, no footnotes.
+
Implementation:
+
- `PageMeta` gains a `chrome : Bool = true` field. When `chrome = false`,
  `render_headers_footers` skips the page (header, footer AND footnotes).
- `new_page` accepts an optional `chrome` argument that propagates the flag
  into the created `PageMeta`.
- `render_title_page` calls `new_page(chrome: false)` to mark the cover.
+
Consistent side effect: content starts on numbered page 2, the cover not
being counted in the footer since it carries none.

== v2.3.24.18 — 2026-04-29

=== Fixed (title-page isolation when TOC is inactive)

* **Content collided with the cover title when `:toc:` was absent.**
  `render_title_page` draws the title at mid-height (`title_y =
  page_height * 0.55`). With TOC active, two `new_page` calls then isolated
  the content; but without `:toc:` (typical of quizzes, job sheets, short
  memos), no new page was created and content started at the top of the cover,
  overlapping the title.
+
Fix: trigger a `new_page` after the title page whenever no TOC already does
it for us. Result: page 1 = isolated cover, page 2+ = content.

== v2.3.24.17 — 2026-04-29

=== Changed (requires by shard name + `flags` → `country-flags`)

* **Migration to short requires by shard name.** The
  `require "<shard>/src/<file>"` requires only work while the shard name and
  the file name match; with the renaming cascade (`CrystalX` → `X`) and the
  `src/<shard>.cr` entry-point convention, these explicit paths broke in CI.
  New requires:
+
- `pdf/src/pdf` → `pdf`
- `flags/src/crystal_flags` → `country-flags`
- `emojis/src/crystal_emojis` → `emojis`
- `noto-cjk/src/crystal_noto_cjk` → `noto-cjk`
- `crystal-asciidoctor/src/crystal-asciidoctor` → `crystal-asciidoctor`

* Renamed the `flags` dependency to `country-flags` (`~> 0.1.4`) following the
  `aloli-crystal/country-flags` repo. Client module updated: `Flags.svg` →
  `CountryFlags.svg` at both call sites.

== v2.3.24.16 — 2026-04-29

=== Changed

* **Module references updated** following the upstream rename of
  internal modules (Aloli convention: drop the `Crystal` prefix on
  internal module names):
** `CrystalEmojis::*` → `Emojis::*`
** `CrystalFlags::*` → `Flags::*`
** `CrystalNotoCJK::*` → `NotoCjk::*`
* `shard.yml` deps re-pinned :
** emojis `~> 0.2.2`, flags `~> 0.1.2`, noto-cjk `~> 0.1.2`.
* No behavioural change.

== v2.3.24.15 — 2026-04-29

=== Changed

* **Dependencies updated** to follow the level-0 rename cascade
  (Aloli convention: original creations drop the `crystal-` prefix on
  the GitHub URL):
** `pdf` → `pdf`
** `flags` → `flags`
** `emojis` → `emojis`
** `noto-cjk` → `noto-cjk`
* `shard.yml` dependency keys, internal `require` paths, and
  documentation URLs updated. No behavioural change.
* The crystal-asciidoctor-pdf shard itself is a Ruby-gem port and
  *keeps* its `crystal-` prefix per the naming convention.

== v2.3.24.14 — 2026-04-26

**CJK glyph rendering** via the new
https://github.com/aloli-crystal/noto-cjk[noto-cjk]
shard. When the user has populated the CJK cache (via
`noto-cjk pull` or `CrystalNotoCJK::Cache.pull`), the
converter automatically detects CJK characters in text runs,
switches the active font to Noto Sans CJK for those segments
only, and switches back to the main font for the rest. Same
pattern as emojis switching to `page.svg` for individual glyphs.

* New runtime dependency : `noto-cjk ~> 0.1`.
* `text_with_emoji_segments` now returns three kinds of
  segments : `:text` (default font), `:emoji` (page.svg), and
  `:cjk` (font switch to @font_cjk). Mid-run font switching is
  done inline in `draw_text_run`.
* `load_theme_fonts` opportunistically loads
  `CrystalNotoCJK.font_path` if a variant is in the cache. No
  download is triggered automatically — the user explicitly
  populates the cache.
* End-of-conversion bilan now also points to `noto-cjk pull`
  with concrete commands for each variant + the current cache
  status.
* No regression : 94 tests, all green.

== v2.3.24.13 — 2026-04-26

**Emoji pipeline reaches paragraphs** plus end-of-run hint
message that tells the author exactly what to do when characters
have been substituted by `?`.

* `render_segment_line` (the path used for paragraph text) now
  routes through `draw_text_run` instead of calling `page.text`
  directly. Net effect : emojis in paragraph text get rendered
  in colour via emojis (previously only headings and a
  few other code paths were emoji-aware).
* End-of-conversion bilan : when `@warned_chars` is non-empty,
  a multi-line message is printed to STDERR with the count, a
  sample of substituted characters, and the exact commands to
  run to get full coverage (`emojis pull`, plus a note
  about future `noto-cjk` for CJK).
* Dependency renamed `emojis-lite` → `emojis`
  (~> 0.2). Same module name `CrystalEmojis`, same API surface,
  plus the new `Cache` module that lets the host application
  populate the on-disk cache programmatically.
* No regression : 94 tests, all green.

== v2.3.24.12 — 2026-04-26

**Better font coverage detection** — fixes a v2.3.24.10
over-aggressive substitution that turned valid characters
(Cyrillic Привет, Greek Καλημέρα, Hebrew שלום, simple dingbats
✓ ✗ ★) into `?` even when DejaVu Sans (the default embedded
font) could render them just fine.

The old `safe_text` was a static WinAnsi check assuming Type1
Helvetica/Courier — but the converter actually defaults to
embedded DejaVu Sans TrueType, which covers a much larger
codepoint range. The new pipeline asks the active font directly
via `pdf v0.3.5`'s new `TrueTypeFont#has_glyph?(char)`
method, falling back to the static WinAnsi table only for Type1
fonts (rare — only when DejaVu is intentionally disabled in the
theme).

* Bump dep `pdf` to `~> 0.3.5`.
* `Converter#safe_text(text, font_name = @fn_body)` now takes
  the font name and routes per-character coverage through the
  TrueType `has_glyph?` query.
* `font_object_for(font_name)` extracted from the previous
  duplicated `case` in `set_font` — single source of truth for
  resolving a font name to its `PDF::Fonts::Base` instance.
* Warning message updated : « caractère « X » (U+NNNN) sans
  glyphe dans la police active » (was : « absent de WinAnsi »).
* Tests still all green : 94 examples, no regressions.

== v2.3.24.11 — 2026-04-26

**Coloured emoji rendering** for the ~208 emojis covered by
`emojis-lite`. Glyphs like ✅ ❌ ⚠ 🔴 🟢 🟡 🔵 💻 🔑 ⚙ are
now rendered as embedded SVG (Twemoji, in colour) instead of being
substituted with `?`. Emojis outside the lite set still fall back
to `?` + warning (the v2.3.24.10 behaviour) — for full coverage,
swap `emojis-lite` for `emojis-full` in the
dependency tree.

* New runtime dependency :
  https://github.com/aloli-crystal/emojis-lite[emojis-lite]
  ~> 0.1.
* New private helper `Converter#text_with_emoji_segments(text)`
  splits a string into alternating `:text` and `:emoji` segments,
  letting `draw_text_run` route each kind to the right pipeline
  (text → `page.text` + WinAnsi sanitize ; emoji → `page.svg`
  with the same sizing as country flags from `flags`).
* Updated regression test for the silent-tofu bug : ✅ and ❌
  no longer end up as `?` in the WinAnsi text stream ; CJK
  characters and emojis outside the lite set still do.
* **94 tests, all green.**

== v2.3.24.10 — 2026-04-26

Stops the silent tofu boxes for characters outside the WinAnsi
encoding range. Type1 standard fonts (Helvetica, Courier — the
defaults of this converter) only cover ~220 codepoints (ASCII +
Latin-1 + a few Windows-1252 extras). Anything beyond — emojis
(✅ ❌ 🔴), dingbats (✓ ✗ ★), CJK ideographs, etc. — used to
render as empty rectangles with no warning to the author.

The fix :

* New `AsciidoctorPDF::WinAnsi` module with `representable?(char)`
  and `sanitize(text) { |char| … }` helpers.
* New `Converter#safe_text(text)` substitutes every non-WinAnsi
  character with `?` and emits a deduplicated warning to STDERR
  («  Avertissement : caractère « ✅ » (U+2705) absent de WinAnsi,
  remplacé par « ? » dans le PDF. »). One warning per character
  per conversion — no spam if the same emoji appears N times.
* `draw_text_run` runs every text run through `safe_text` before
  passing it to `page.text`.
* New tests : 11 unit specs for `WinAnsi`, 2 integration specs
  proving emojis no longer crash the converter and pure-WinAnsi
  text is left untouched. **93 tests total, all green.**

This is a conservative fix that removes the silent failure mode.
The next step (planned as a separate shard) is `emojis`,
which will render real **coloured emojis** as embedded SVG glyphs
(same pattern as `flags` for country flags).

== v2.3.24.9 — 2026-04-25

Clickable TOC entries. Each line of the table of contents is now a
PDF link annotation that jumps the reader to the matching section
title — no more scrolling through 80 pages to find chapter 12. The
PDF outline (bookmark panel in viewer sidebars) was upgraded the
same way: clicking a bookmark now lands on the title position, not
just on the top of the page.

* `convert_section` registers a PDF named destination for every
  section, keyed on the AsciiDoc section id (auto-generated by the
  parser, e.g. `_overview`, or supplied via `[[anchor]]`). The
  destination uses `/XYZ` so the viewer scrolls precisely to the
  top of the heading.
* `render_toc` emits one `/Subtype /Link` annotation per entry
  covering the full row (title + dot leaders + page number),
  pointing to the named destination. No visible border — the
  annotation is invisible visually, only the cursor changes.
* `generate_pdf_outline` now uses `/XYZ` destinations instead of
  `/Fit` so bookmark navigation lands on the heading, matching the
  behaviour of the in-document TOC.
* Bonus fix: TOC entries were registered *before* the
  `check_page_break` that may force a new page, which produced
  off-by-one page numbers for sections that overflowed. The entry
  is now registered after the page break, so the TOC always shows
  the page the title is actually drawn on.
* 4 new integration tests in `spec/integration/toc_anchors_spec.cr`
  asserting on the byte-level presence of `/Subtype /Link`
  annotations and `/XYZ` destinations. **34 integration tests,
  46 unit tests, 80 total — all green.**

== v2.3.24.8 — 2026-04-23

End-to-end integration test suite. The pipeline adoc → PDF is now
exercised through `spec/integration/*_spec.cr`, which round-trip
small AsciiDoc snippets through the real converter and inspect the
produced PDF via `PDF::Reader`.

* 7 spec files covering: sanity, NBSP handling, section headings,
  tables, admonitions (NOTE/TIP/IMPORTANT/WARNING/CAUTION + block
  form), lists (unordered, ordered, definition), inline emphasis
  (bold/italic/mono), country flags (including the BR regression
  that used to loop), and document title metadata. **30 integration
  tests, 46 unit tests, 76 total — all green.**
* New helper `IntegrationHelper` in `spec/integration/spec_helper.cr`
  that:
  - converts an inline AsciiDoc string through the real
    `AsciidoctorPDF::Converter`,
  - extracts readable text from PDF content streams (supports both
    Type1 WinAnsi literal `(…)` strings and TrueType Identity-H
    hex `<…>` strings, tolerant byte-level scan),
  - reads the PDF `/Info` `/Title` field and decodes UTF-16BE-with-BOM
    when needed,
  - forces Type1 standard fonts in test runs so the extracted text
    is ASCII-readable (the converter's real font pipeline is still
    tested — it's just swapped to an extractable encoding).
* Pure Crystal, zero external tool dependency (no `pdftotext`,
  `unzip`, …) — the CI runner doesn't need anything beyond what
  `shards install` provides.

== v2.3.24.7 — 2026-04-23

Extend non-breaking-space handling to every renderer that was still
emitting raw HTML entities or raw U+00A0 characters.

* **Document title** (`@document_title`) is now decoded through
  `decode_html_entities` once in `convert_document`, so consumers
  (title page, running headers / footers, PDF `/Title` metadata,
  index title, …) all see a plain-text title. An explicit
  `{nbsp}` in the AsciiDoc title used to surface as the literal
  string `&#160;` everywhere — most visibly on the cover page
  and the footer.
* **Header / footer templates**: `resolve_page_vars` now also
  replaces any surviving U+00A0 with an ASCII space, so the
  running header/footer (which doesn't go through `draw_text_run`)
  no longer draws a tofu box for an NBSP.
* **Cover page**: `render_title_page` now calls `draw_text_run`
  for the title, subtitle, author and revdate rows instead of
  `page.text` directly ; any NBSP carried from the source is
  rendered as a visual space, and future inline flags in those
  fields would work too.
* **Floating titles, TOC entry titles, table captions**: same
  switch to `draw_text_run` for consistency.

Net effect: `{nbsp}` and `&nbsp;` in *any* author-facing text —
including the document title and chrome — are now rendered as a
visible space, while `wrap_text` continues to treat them as
non-break separators.

== v2.3.24.6 — 2026-04-23

Preserve the non-break property of `&nbsp;` / `&#160;` in wrap.

The v2.3.24.5 fix normalised every non-breaking space to an ASCII
space *before* wrap, which eliminated the tofu glyphs but also
dropped the "do not break" property of the sequence — users relying
heavily on `&nbsp;` could see a line break exactly where they had
written one to prevent it.

Pipeline is now:

* HTML entities `&#160;` and `&nbsp;` decode to the actual
  character **U+00A0** (non-breaking space). A bare U+00A0 that
  was already in the input is kept as-is.
* `wrap_text` splits on ASCII space only (regex `/\s/` does not
  match U+00A0 in Crystal), so a word pair joined by U+00A0 stays
  on the same line.
* Just before each `page.text` call (`draw_text_run` in the
  converter and `InlineRenderer.render_line`), U+00A0 is replaced
  by an ASCII space in the drawn string — the PDF is visually
  clean regardless of whether the active font has a drawable
  NBSP glyph.

Net effect: the visual result is identical to v2.3.24.5 (no tofu)
but line breaks now honour the author's non-breaking spaces.

== v2.3.24.5 — 2026-04-23

Broader flag rendering plus a handful of white-space glyph fixes.

* **Flags in paragraphs, admonitions and lists** too — not just in
  tables. `draw_text_run` is now called from `convert_paragraph`
  (via `InlineRenderer.render_line`), `render_admonition`,
  `convert_dlist` and `render_list`. Any regional-indicator pair
  inside body text now renders as an actual colour SVG flag.
* **Fix tofu glyphs from `<br>`**: the inline renderer translated a
  `<br>` into a literal `\n` in the draw buffer, which showed up as
  a tofu box since Type1 fonts have no glyph for newline. It now
  emits an ordinary space; hard line breaks are effectively
  treated as word separators. A proper multi-line layout for
  `<br>` is left for a future pass.
* **Fix tofu glyphs from soft newlines**: the HTML generated by
  AsciiDoctor preserves line breaks from the source inside
  paragraphs. The inline renderer and `strip_inline_markup` now
  collapse any run of whitespace into a single space before the
  text is drawn.
* **Fix non-breaking-space tofu**: `&#160;`, `&nbsp;` and bare
  U+00A0 are now normalised to an ordinary space (the non-break
  property is lost, which is an acceptable trade-off given Type1
  standard fonts don't always expose a drawable NBSP glyph).
* **Dependency bump**: `pdf` → uses v0.3.3 via `~> 0.3`,
  which adds a document-scoped cache of `SVG::Parser` instances so
  repeated flags on the same document are parsed only once.

== v2.3.24.4 — 2026-04-23

Real country flags in table cells, via a new SVG rendering pipeline.

* **New dependency** `flags` (~> 0.1) — 271 ISO 3166-1 alpha-2
  SVG flags from lipis/flag-icons embedded at compile time.
* **Inline flag rendering in table cells**: pairs of Unicode regional
  indicator characters (e.g. `🇫🇷`, `🇩🇪`) are detected in cell text
  and drawn as actual colour SVG glyphs on top of the text layer, at
  the baseline of the surrounding text. Previously those codepoints
  rendered as tofu boxes (DejaVu Sans has no glyph for them) and
  threw off the cell width calculation.
* **Layout**: `text_width` and `wrap_text` now charge each flag as a
  fixed width derived from the font size, so columns reserve the
  correct amount of space instead of counting the `.notdef` width
  from the text font.
* **Dependency bump**: `pdf` → `~> 0.3` (uses the path-parser
  loop fix and the closed-shape fill fix shipped in pdf
  v0.3.1 and v0.3.2).
* New helper module `AsciidoctorPDF::InlineFlags` with `segments`,
  `flag_width` and `flag_height`.

== v2.3.24.3 — 2026-04-23

Layout and typography fixes observed on real-world documents.

* **Admonitions** (NOTE, TIP, IMPORTANT, WARNING, CAUTION): the label
  now has a proper gap before the text. Previously the label fit in a
  fixed 60 pt slot regardless of its rendered width, which made long
  labels like `IMPORTANT` visually collide with the first word of
  the message.
* **Section titles**: long titles are now wrapped across multiple
  lines instead of being rendered past the right margin (and
  truncated). Uses the existing `wrap_text` helper with the heading
  font.
* **Table cells**: words that do not fit in their column (typical
  for country names in narrow proportional columns, e.g. `Allemagne`
  or `Pays-Bas`) are now broken into sub-chunks. Break priority:
  internal hyphens (`Pays-` + `Bas`), camelCase boundaries, then a
  per-character fallback. No visual overflow into the neighbouring
  column any more.
* **Default body font size**: lowered from 10.5 pt to 10 pt. Mostly
  cosmetic — makes dense tables fit better on A4 without changing
  the heading hierarchy.

== v2.3.24.2 — 2026-04-22

* Fix French accents and apostrophes in the CLI output and help
  messages (`PDF généré`, `thème`, `Générer le document de référence`,
  `l'aide`, `Erreur : aucun fichier d'entrée spécifié`, etc.).
* Tidy a couple of string concatenations into interpolations.

== v2.3.24.1 — 2026-04-22

* Project restructured to produce a proper native binary. The CLI source
  script moved from `bin/crystal-asciidoctor-pdf` to `src/cli.cr`;
  running `shards build --release crystal-asciidoctor-pdf` now produces
  an ELF/Mach-O binary at `bin/crystal-asciidoctor-pdf` without
  overwriting any source file, making the tool directly usable after
  `shards build`.
* Tighten `.gitignore`: drop the `/crystal-asciidoctor-pdf-bin`
  workaround and simply ignore `/bin/*`.
* Add the *ameba* linter to the CI pipeline with the shared
  `.ameba.yml` config aligned with the Crystal ecosystem.
* Add a bilingual changelog (CHANGELOG.adoc in English,
  CHANGELOG.fr.adoc in French); remove a leftover CHANGELOG.md.
* Reformat `inline_renderer.cr`, `theme.cr`, `theme_loader.cr` via
  `crystal tool format`.

== 0.1.1 (2026-03-16)

* CLI binary renamed from `asciidoctor-pdf` to `crystal-asciidoctor-pdf`.
* Default output behaviour: `mon_document.adoc` now produces
  `mon_document.adoc.pdf`.

== 0.1.0 (2026-03-13)

* Initial release.
* Basic conversion of paragraphs, sections, lists and tables.
