= Roadmap — bugs et gaps connus
:toc:

Suivi des écarts entre `asciicrystal-pdf` (portage Crystal) et la
gem Ruby `asciidoctor-pdf` 2.3.24 (référence). Issu de l'audit du
2026-04-29 + retours utilisateur.

Mise à jour : 2026-04-30 (post v2.3.24.47).
Trois sprints fermés. Tous les 🔴 critiques, 🟠 importants ET 🟡
cosmétiques courts (« S ») sont livrés. Reste 5 🟡 cosmétiques
moyens / longs (M) à plus long terme.

== Légende

[cols="1,3"]
|===
| Sigle | Sens

| 🔴 | Critique — bloque l'usage courant.
| 🟠 | Important — fonctionnalité courante, contournable.
| 🟡 | Cosmétique — rarement utilisé, faible impact.
| S | Effort 1-2h
| M | Effort 1 journée
| L | Effort plusieurs jours
|===

== Bugs ouverts

(Aucun bug ouvert connu au 2026-04-29 post v2.3.24.41.)

== Gaps cosmétiques restants (long terme)

[cols="2,1,1,4"]
|===
| Gap | Sév. | Effort | Description

| Icônes (`icon:warning[]`)
| 🟡
| M
| Fallback texte `[<nom>]` actuel. Vraie implémentation = embarquer
  Font Awesome SVG ou subset Twemoji. Effort = surtout sélection
  + embed des assets (~1-2 MB).

| Optimizer (compression Ghostscript)
| 🟡
| M
| `optimizer.rb` non porté. Pas de linearization PDF.

| Cover page (face: front/back) + background
| 🟡
| M
| Pas de cover face arrière, pas de page background, pas de watermark.

| Table rowspan + autofit
| 🟡
| M
| Le `cell.rowspan` est lu mais pas matérialisé visuellement (la
  cellule n'occupe qu'une rangée). `autofit` (largeurs auto sur
  contenu) non implémenté — les largeurs explicites `colpcwidth`
  fonctionnent.

| Callouts dans listings (substitution dans le code)
| 🟡
| M
| La colist post-listing rend les ① ②, mais les `<1>` à l'intérieur
  d'un bloc `[source]` apparaissent textuellement. Substitution
  nécessite intégration côté pipeline `rouge`.

| `:lang:` et `/CreationDate` doc-level
| 🟡
| M
| `:lang:` côté shard pdf : `PDF::Document` n'a pas encore de
  propriété `lang`. Idem `revdate` → `CreationDate` non substituable
  (la date est posée par défaut à l'heure actuelle).
|===

== Items fermés

=== Sprint 1 — v2.3.24.28 → v2.3.24.33 (les 🔴 prioritaires)

. ✅ **v2.3.24.28** — Inline markup dans admonitions / x-score
. ✅ **v2.3.24.29** — Inline quoted étendu (sub, sup, kbd, btn, menu, mark)
. ✅ **v2.3.24.30** — Inline image (raster + SVG)
. ✅ **v2.3.24.31** — Encadrement sidebar / example
. ✅ **v2.3.24.32** — Chapter break + numérotation romaine front-matter
. ✅ **v2.3.24.33** — Header / footer recto-verso

=== Sprint 3 — v2.3.24.42 → v2.3.24.47 (les 🟡 « S »)

. ✅ **v2.3.24.42** — Module `Sanitizer` interne (portage 1:1 du
    `sanitizer.rb` Ruby asciidoctor-pdf — utilitaire texte, *pas*
    sécurité). Le vrai sanitizer de sécurité est documenté dans
    `CRYSTAL-SANITIZER-HTML-SPECS.adoc` à la racine
    `/Users/philippe/prod-crystal/`, à publier sous le nom
    `aloli-crystal/sanitizer-html` quand l'app ISO 27001 démarrera.
. ✅ **v2.3.24.43** — `TextTransformer` (uppercase, smallcaps,
    capitalize) + propriétés thème `heading_text_transform` /
    `title_page_text_transform`.
. ✅ **v2.3.24.44** — Audio / video / stem / pass : placeholders
    visuels (bandeau gris + URL cliquable + caption).
. ✅ **v2.3.24.45** — Collapsible blocks signalés par ▾ dans le titre.
. ✅ **v2.3.24.46** — Abstract block : indenté, italique, gris
    discret, taille +1pt.
. ✅ **v2.3.24.47** — Index : ranges de pages (« 12-15 ») au lieu
    de listes plates.

=== Sprint 2 — v2.3.24.34 → v2.3.24.41 (les 🟠 secondaires)

. ✅ **v2.3.24.34** — Tables : colspan + valign + factorisation
    `render_table_row`. Tiré aussi asciicrystal 2.0.26.2
    (parser `CellSpecEndRx` accepte `\n` avant la spec).
. ✅ **v2.3.24.35** — Page break : options `[%always]` / `[%recto]` /
    `[%verso]`.
. ✅ **v2.3.24.36** — PDF metadata étendues : `keywords`, `creator`.
. ✅ **v2.3.24.37** — Callouts dans listings (`<1>`) + colist (① ②).
. ✅ **v2.3.24.38** — Quote / verse distingués : pre-wrap pour verse,
    markup inline, citetitle.
. ✅ **v2.3.24.39** — Attributs `pdf-*` doc-level (`pdf-page-size`,
    `pdf-page-layout`).
. ✅ **v2.3.24.40** — Description list : styles `qanda` et
    `horizontal`.
. ✅ **v2.3.24.41** — Open block : rôle `[.x-box]` pour panneau
    encadré.

== Bilan post sprints

Le portage couvre maintenant **tous les cas d'usage courants**
(quiz, fiches, rapports, livres, FAQ, doc technique, abstract,
text-transform, index avec ranges, audio/video placeholders,
collapsible). Reste uniquement les 🟡 d'effort moyen :

  * Iconographie complète (Font Awesome embarqué) — pertinent quand
    on aura un cas d'usage concret avec icônes inline
  * Optimisation PDF (compression Ghostscript) — pertinent quand
    les PDF générés deviennent volumineux (rapports >500 pages)
  * Cover page back + page background + watermark — pertinent pour
    les rapports confidentiels (« usage interne »)
  * Table rowspan + autofit — pertinent pour les grilles ISO
    complexes
  * Callouts dans le code source des listings — pertinent pour la
    doc technique annotée (intégration rouge)
  * `:lang:` et `/CreationDate` doc-level — extension shard `pdf`
    nécessaire

À reprendre lorsque les besoins concrets émergent — pas de
priorité immédiate.
